# 運用手順書（Claude 用）

[English](OPERATION.md) | **日本語** | [中文](OPERATION.zh.md) | [한국어](OPERATION.ko.md)

サブエージェントを起動・完了させるたびに、この手順で状態を更新する。
**書き換えは必ず `update_state.py` 経由で行い、`state.json` を直接編集しないこと。**
時刻・世代・ログ・サマリー集計はすべて CLI が自動処理するので、手で書くと必ず崩れる。

## このファイル内の表記について

コマンド例は `dash` と省略して書いている。実体は**このファイルと同じディレクトリ**にある
ランチャ（Windows は `dash.cmd`、macOS / Linux は `./dash`）で、次の3つはすべて同じ動作をする。

```bash
dash add --id SCOUT-A ...                     # ランチャ経由
python <このディレクトリ>/dash.py add ...      # ランチャの本体を直接
python <このディレクトリ>/update_state.py add  # CLI を直接
```

**実行時に使う完全パス付きのコマンド例は、エージェントのグローバル設定ファイルに書き込まれている。**
そちらをそのまま使うこと（`dash install` が環境に合わせて生成している）。
このディレクトリの実際の場所は `dash projects` の出力先頭にも表示される。

## 対象プロジェクトは自動で決まる

**カレントディレクトリから対象プロジェクトが自動判定される。**
どのプロジェクトで作業していても、同じコマンドをそのまま実行すればよい。
状態は `missions/<スラッグ>/` にプロジェクトごとに分離して保存される。

スラッグは「ディレクトリ名 + フルパスのハッシュ6桁」なので、
別の場所にある同名ディレクトリと混ざることはない。

---

## 0. 起動（1回だけ・全プロジェクト共通）

```bash
dash serve
```

表示された URL をブラウザで開く。**サーバーは1つだけ起動すればよい。**
どのプロジェクトで作業していても、この1つの画面に出る。
ポートが使用中なら自動で繰り上がるので、起動ログに出た URL を使う。

**どのチームを映すかはサーバーが決める。** 切り替え操作は無い。

| 画面 | いつ |
| --- | --- |
| 待機画面 | タブが1枚も無いとき（まだ一度も `start` していない、記録を全部消した） |
| 稼働中のチーム | `start` した瞬間から |
| 完了したチーム | `finish` のあともそのまま映り続ける |
| 次のチーム | 次の `start` で入れ替わる。前のチームは画面から消える |
| **複数チームを縦に積む** | 並列で稼働しているチームは全部、同時に映る（開始が新しい順） |

映すのは「**稼働中のチーム全部 ＋ `missions/.current` が指すチーム**」。`.current` を
書き換えるのは **`start`（と `demo`）だけ**で、`done` や `finish` では動かない。だから
完了した画面が勝手に別のプロジェクトへ飛ぶことはない。

これに加えて、**稼働中のチームが動き始めたあとに完了したチーム**も残す。並列で走らせた
片方が先に `finish` したとき、そのチームはもう `.current` ではないので、これが無いと
完了した瞬間に消えて誰も完了報告を読めない。逆に、順番に作業しているだけの場合
（前のチームが終わってから次を `start`）は前のチームの完了が次の開始より前になるので、
そちらは要件どおり画面から降りる。

`running` のまま放置された記録を延々出し続けないよう、**既定3時間の時間窓**がある。
`state.json` の更新がそれより古い `running` は出さない。窓は環境変数
`AGENT_DASHBOARD_ACTIVE_WINDOW`（秒）で変えられる。長時間まったく通知が来ない
ミッションを扱うなら伸ばすこと。

**まだ一度も `start` していない器（「（ミッション未開始）」）は、他に選べるタブが
1枚も無いときだけ出す。** `dash reset` の直後や、名前だけ作られたプロジェクトがこれになる。
押しても何も出ないタブで、次に `start` するまで消えないので、放っておくとタブ列に溜まる。
他に何も無いときまで隠すとプロジェクトの存在ごと画面から消えるため、そこでは残す
（未開始どうしは「他のタブ」に数えない。数えると2件あるだけで両方消えて待機画面に落ちる）。
`start` 直後で機体が0体のものは `running` なので、この規則には掛からない。

前のミッションの記録は `missions/<slug>/history/<runId>/` に退避される。**画面から降りても
消えない。** タブで選べば後から中身を見返せる（一覧は `dash history`）。

保持はプロジェクトごとに直近 20 件。超えた分は古いものから `trash/` へ移る。件数は環境変数
`AGENT_DASHBOARD_HISTORY_KEEP` で変えられる（`0` にすると退避せず、従来どおり上書きになる）。
`0` にしても既に溜まっている `history/` は掃除しない。設定を一時的に変えただけで
過去の記録が全部ゴミ箱行きになるのを避けるため。

---

## 0.05. VSCode 拡張機能から起動（推奨。人間の操作）

人間が VSCode を使っている場合、これがいちばん手数が少ない。Claude 側で何かする必要はない。

```bash
dash ext install     # 1回だけ。そのあと VSCode を再読み込み
```

以後は左端のアクティビティバーにあるロボットのアイコンを押すとエディタのタブに出る。
コマンドパレットの「Subagent Dashboard: タブで開く」やステータスバーの 🤖 Subagent Dashboard でも同じ。
左側に細長く置きたいときは、設定 `agentDashboard.sidebarBehavior` を `embed` にする。

人に配るときは `dash ext package`。`dist/` に出る `.vsix` と手順書をメールに添付する。
拡張機能にはダッシュボード本体が同梱されていて、受け取った側で
`~/.claude/agent-dashboard` へ配置される（初回に確認あり）。

**Claude が知っておくべき点だけ:**

- 拡張はサーバーの生死を `GET /api/env` で判定し、`toolRoot` が一致したときだけ「自分たちのサーバー」と見なす
- 拡張は空きポートを自分で決めてから `dash.py serve --port <番号> --no-retry` で起動する。**サーバー側の繰り上げを止めているので、拡張が把握している番号と実際の番号は必ず一致する**
- 拡張が起動したサーバーは VSCode 終了時に止まる。ターミナルで `dash serve` したサーバーには手を付けない
- したがって、**Claude が `dash serve` を立てたあとに人間が拡張から開いても、二重起動にはならない**（拡張が既存を使い回す）

詳細は `EXTENSION_PLAN.md`、拡張の使い方は `extension/README.md`。

---

## 0.1. どのプロジェクトからでも起動（Global Launch）

拡張機能を入れていない場合の導線。任意のプロジェクトフォルダから `Ctrl+Shift+D` でダッシュボードを起動したときの仕組み。

### 用途

- 複数プロジェクトを同時進行している
- agent-dashboard フォルダを毎回開き直すのが面倒
- VSCode のキーボードショートカットで素早くアクセスしたい

### プロセスフロー

```text
ユーザーが Ctrl+Shift+D を押す
        │
        ▼
  VSCode がキーバインディングを実行
        │
        ▼
  python open_dashboard.py
        │
        ▼
  環境変数 AGENT_DASHBOARD_HOME から
  agent-dashboard の場所を検出
        │
        ▼
  python dash.py serve を起動
        │
        ▼
  http://127.0.0.1:3939 へ接続
  ↓ ヘルスチェック（最大5秒まで待機）
        │
        ▼
  ブラウザ / SimpleWebService で開く
```

### 技術詳細

#### 場所の検出

`install.py` が実行された際に、agent-dashboard のパスを以下の優先順で検出・記録します：

1. **環境変数 `AGENT_DASHBOARD_HOME`** が既に設定されている → そのパスを使う
2. **環境変数に未設定** → `install.py` を実行したディレクトリを検出
3. **設定ファイルに埋め込み** → エージェントが自動実行するコマンドに完全パスが含まれる

#### サーバーの起動

`open_dashboard.py` は以下の手順でサーバーを起動します：

```python
# 1. agent-dashboard の場所を確定（環境変数の設定順に探索）
dashboard_home = AGENT_DASHBOARD_HOME or DEFAULT_PATH

# 2. サーバーが既に起動していないか確認
# http://127.0.0.1:3939 に接続できるか試す

if サーバーが応答しない:
    # 3. 新しいサーバープロセスを起動
    subprocess.Popen([
        "python",
        f"{dashboard_home}/dash.py",
        "serve",
        "--detach"  # バックグラウンド起動
    ])
    
    # 4. ヘルスチェック（最大5秒待機）
    for i in range(50):  # 0.1秒 x 50 = 5秒
        if http://127.0.0.1:3939 が応答:
            break
        sleep(0.1)

# 5. ブラウザを開く
webbrowser.open("http://127.0.0.1:3939")
```

#### ポート衝突時の処理

複数ユーザーが同じマシンで作業している場合：

1. ユーザーA が `Ctrl+Shift+D` → ポート 3939 で起動
2. ユーザーB が `Ctrl+Shift+D` → ポート 3939 は使用中
3. ユーザーB のサーバーは 3940 で起動
4. 各ユーザーは自分の設定済みポートに接続

**ダッシュボード内には表示されるため、URL が変わっても動作に支障なし。**

#### IDE 統合の例（VSCode）

`install.py` が `keybindings.json` に登録する内容：

```json
{
  "key": "ctrl+shift+d",
  "command": "agentDashboard.open"
}
```

拡張機能が持つコマンドを直接呼びます。パスにもターミナルにも依存しないので、
ダッシュボードの置き場所が変わっても壊れません。**拡張機能を入れていないと
このキーは何も起こしません**（コマンドが存在しないため）。

書き込み先は VSCode がユーザー設定として実際に読む場所です。

| OS | 場所 |
| --- | --- |
| Windows | `%APPDATA%\Code\User\keybindings.json` |
| macOS | `~/Library/Application Support/Code/User/keybindings.json` |
| Linux | `$XDG_CONFIG_HOME/Code/User/keybindings.json`（既定 `~/.config/Code/User/`） |

`Ctrl+Shift+D` は VSCode 標準で「実行とデバッグ」に割り当てられています。
拡張の `contributes.keybindings` では標準割り当てとの優先順位が環境依存になるため、
確実に勝つユーザー設定側に書いています。

> **0.2.4 までの版について**
> 書き込み先を `~/.vscode/keybindings.json` にしていました。そこは拡張機能の置き場で、
> VSCode はキーバインドを読みません。つまり `Ctrl+Shift+D` は効いていませんでした。
> 0.2.5 の `install.py` は、この古い場所に自分が書いたエントリがあれば片付けます
> （利用者が自分で書いたエントリには触りません）。

---

## 0.2. AI コーディング CLI への書き込み

`install.py` はマシンにインストール済みのすべての AI コーディング CLI を検出し、それぞれの設定ファイルに
同じ運用ルールを書き込みます。以下は組み込み対応の CLI リスト。

| CLI | 書き込み先 |
| --- | --- |
| Claude Code | `~/.claude/CLAUDE.md` |
| Codex CLI | `~/.codex/AGENTS.md` |
| Gemini CLI | `~/.gemini/GEMINI.md` |
| GitHub Copilot CLI | `~/.copilot/copilot-instructions.md` |
| opencode | `~/.config/opencode/AGENTS.md` |
| Amp | `~/.config/amp/AGENTS.md` |
| Cline | `~/Documents/Cline/Rules/subagent-dashboard.md` |
| Roo Code | `~/.roo/rules/subagent-dashboard.md` |
| Windsurf | `~/.codeium/windsurf/memories/global_rules.md` |
| Qwen Code | `~/.qwen/QWEN.md` |

**この表に無い CLI でも対応できます。** `--agent-file` オプション（下記参照）で任意のファイルを登録してください。

> **Cursor と Aider について**
> これらのツールはユーザーレベルの設定ファイルを読まないため、上の表に含まれていません。
> Cursor はリポジトリごとの `AGENTS.md` を読み、Aider は設定で明示的にファイルを指定する必要があります。
> これらのツールに対応させるには、リポジトリ内のファイルに `--agent-file` で指定してください。

### 詳細なコマンド

```bash
python install.py --list-agents           # 見つかった全ての CLI、書き込み先、状態を表示
python install.py                         # このマシンにインストール済みの CLI に書き込む
python install.py --agent codex           # 特定の CLI（この例では Codex CLI）のみに書き込む
python install.py --agent all             # 全ての既知の CLI に書き込む（インストール状況に関わらず）
python install.py --agent-file <パス>     # 指定したファイルにも書き込む（対応していない CLI 用）
python install.py --uninstall             # すべての CLI から運用ルールを削除する
```

### agents.json

`--agent-file` で登録したファイルは `agents.json`（記録の保存先の隣）に永続的に記録されます。
以降は `--list-agents` に表示され、次に `install.py` を実行するときに更新され、`--uninstall` で削除されます。

`agents.json` を手で編集することもできます。各エントリには以下のフィールドがあります：

- `key` — `--agent` で使う短い ID
- `label` — 画面に表示される名前
- `home_env` — ホームフォルダ位置を指定する環境変数（無い場合は空文字列）
- `home` — フォルダパス（`~` で始められる。環境変数はサポートしない）
- `file` — ファイル名（サブフォルダを含められる）

組み込みの設定と同じ `key` を持つエントリは、組み込みの設定を上書きします。
これにより、CLI が設定ファイルの場所を変えた場合、更新を待たずに自分のコピーで対応できます。

### 新しい CLI のインストール

セットアップは、**その時点でマシンにインストール済みの CLI を検出します。** セットアップ後に新しい AI CLI を
インストールした場合、それには運用ルールが書き込まれていません。サブエージェントを起動してもダッシュボードに
表示されません。

**解決策：** `python install.py` をもう一度実行してください。

ツールは新しく見つかった未設定の CLI を自動で検出します。以下のタイミングで警告が出ます：

- `dash start` を実行したとき
- サーバーを起動したとき
- `python diagnose.py` を実行したとき（インストール済みだが未設定の CLI がある場合は失敗扱いになります）

---

## 1. ミッション開始時

サブエージェントを1体でも起動する**前**に、作業中のプロジェクトのディレクトリで1回だけ実行する。
それまでの内容は `history/<runId>/` へ退避され、指令塔だけの状態から始まる
（他のプロジェクトには影響しない）。**退避されるので、前のミッションは後から見返せる。**
前のミッションで残った孫の自己申告ファイル（`agents/*.json`）は、退避先へ一緒に移る。
持ち越さないのは、完了通知が来なかった孫が以降すべてのミッションで
「稼働中」のまま並び続けてしまうため。

退避に失敗しても `start` は止まらない（警告を出して続ける）。記録が残っているのに
開始できない方が困るため。

```bash
dash start --title "リファクタ影響範囲調査" --model claude-opus-5
```

**同じディレクトリで2本目のミッションを同時に走らせるときは、`--project` で記録先を
分けること。** そのまま `start` すると1本目が稼働中のまま押し出され、以後1本目の記録は
残せない。→ 「6.1. 同じディレクトリで2本以上を同時に走らせるとき」

---

## 2. サブエージェントを起動した直後

Agent ツールを呼んだ**直後**に実行する。あとでまとめて書くのではなく、起動ごとに1回。

```bash
dash add --id SCOUT-A --name "偵察A" --model claude-sonnet-5 --mission "src/ 配下のAPI呼び出し箇所を洗い出す"
```

| オプション | 省略時 | 備考 |
| --- | --- | --- |
| `--id` | **必須** | `SCOUT-A` `A-1` など短い識別子。ログとカードに出る |
| `--name` | id と同じ | 画面に大きく出る名前 |
| `--parent` | `COMMAND` | 親のID。孫を作らせた場合は親エージェントのIDを指定 |
| `--model` | 空（画面には「不明」と出る） | 実際に使ったモデルID |
| `--mission` | 空 | 任務内容。カードに2行で表示される |
| `--status` | `running` | 出動待ちで並べておくだけなら `standby` |
| `--project` | カレントディレクトリ | 別プロジェクトを対象にしたいときだけ指定 |

**自由記述は日本語で書く**（`--title` / `--name` / `--mission` / `--headline`）。書いたとおりに記録され、
記録したとおりに画面へ出る——あとから翻訳はされない。会話の言語ではなく、この手順書の言語に合わせること。
その言語を変えるには `dash lang <en|ja|zh|ko>` を実行する。**各 AI CLI の設定ファイルの記述もその場で新しい言語に
書き直される**ので、設定した言語がそのままチームを組む言語になる（反映されるのは次のセッションから。
設定ファイルは起動時に読まれる）。**既にある記録は書かれたときのまま**で、あとから訳し直されることはない。

世代（何列目か）は `--parent` から自動算出されるので指定しない。
**「報告待ち」も `--parent` から自動で付く**ので、指令塔が待ちに入ったことを申告する必要はない
（`--parent` を正しく指定することが、そのまま報告待ちの精度になる）。

**複数体を同時起動した場合は、体数ぶん `add` を実行する。** 1回にまとめる方法はない。

---

## 3. 完了通知を受け取ったら

```bash
dash done --id SCOUT-A --sec 42 --tokens 18400 --tools 11 --headline "呼び出し箇所を7ファイル23件で特定"
```

| オプション | 省略時 | 備考 |
| --- | --- | --- |
| `--id` | **必須** | |
| `--sec` | `startedAt` からの実経過秒 | 通知に所要時間があればそれを指定。無ければ省略 |
| `--tokens` | `null` → 画面は「—」 | **通知に無ければ必ず省略する** |
| `--tools` | `null` → 画面は「—」 | **通知に無ければ必ず省略する** |
| `--headline` | 空 | 結果の一行要約。カード下部に緑文字で出る |

### 実測値についての絶対ルール

**完了通知に含まれていなかった数値は、推定せず省略する。** 省略すれば `null` が入り、
画面は「—」と表示する。それが正しい状態であり、埋めるべき欠損ではない。
「だいたい2万トークンくらいだろう」で `--tokens 20000` と書くのは、
このダッシュボードの目的（実際に何が起きたかを見る）を壊す行為なので絶対にしない。

同じ原則が指令塔（ミッション開始者）のモデル ID にも適用される。`--model` を指定しなければ、
画面のモデル欄は「不明」になる（数値欄の「—」とは別の表示）。これは故障ではなく、指令塔の実際のモデル ID が分からないという正しい表示だ。

---

## 4. 全員完了したら

```bash
dash finish --headline "影響範囲を特定し、修正対象を12ファイルに確定"
```

- フェーズが「完了」になり、サマリーが出る。**画面はこのまま完了状態で残る**（次の `start` まで）
- 機体数・合計トークン・所要時間は CLI が集計する（`--headline` 以外を渡す必要はない）
- 合計トークンは `--tokens` を渡した機体の合計。全機が未取得なら「—」になる
- `standby` のまま残っていた機体は「未出動のまま終了」として完了扱いになる

### 締め忘れの見張り

`finish` を打ち忘れても**何も壊れないし、何も止まらない**。だから気づけない。
放っておくと画面には「稼働中」と出続け、次の `start` で稼働中のまま履歴へ流れ、
以後ずっと「未完」の記録として残る（**あとから完了にする方法はない**）。

締め忘れが起きるのは決まって、最後の1体を `done` にした直後に報告をまとめる作業へ
意識が移る瞬間なので、そこを含む4か所で知らせる。**どれも直さずに知らせるだけ**。
あとから `finishedAt` を書けば、それは実測ではなくなる。

| どこで | いつ出るか | 出るもの |
| --- | --- | --- |
| `done` | 最後の1体を完了にした直後 | `★ これで N 体すべてが帰還しました。…` ＋ そのまま貼れる `finish` の行 |
| `status` | 締め忘れの間ずっと | フェーズが「稼働中（**全機帰還済み・未締め**）」／最後の帰還からの経過時間 |
| `start` | 締め忘れたまま次を開始したとき | `⚠️ 退避した「…」は N 体すべて帰還済みでしたが、finish が打たれていません。` |
| 画面 | 締め忘れの間ずっと | ミッションバーとタブの札が琥珀色に変わり、「**全機帰還・未締め**」「**未締**」と点滅する |

「締め忘れ」と判定するのは、次のすべてが揃ったときだけ。

- ミッションが `running`（`done` は締め済み、`standby` はまだ始まっていない）
- サブエージェントが**1体以上**いる（0体なら「開始しただけ」で、締める段階にない）
- そのサブエージェントが**全員 `done`**（1体でも稼働中なら、まだ待っている最中）

指令塔は数えない。`finish` を打つまで `running` のままなのが正しい姿なので、
数に入れると条件が永久に揃わず、警告が一度も出なくなる。

未帰還の機体を残したまま次を `start` した場合は「締め忘れ」ではなく**途中で放棄**なので、
`start` は `⚠️ …稼働中のまま終わりました（未帰還の機体があります）` と言い分ける。
これは同じディレクトリで2本を並行させようとしたときにも必ず起きるので、その場合の
対策（`--project` で記録先を分ける）まで続けて出す。→ 「6.1. 同じディレクトリで2本以上を
同時に走らせるとき」

---

## 5. 補助コマンド

実況を1行だけ足す。判断や方針転換をログに残したいときに使う。

```bash
dash log --who "指令塔" --text "偵察Bの再実行を判断"
```

現在のプロジェクトの状態を表形式で表示する。書き込みが効いているかの確認用。
孫の書き込み先の絶対パスも末尾に出る。

```bash
dash status
```

登録済みプロジェクトを一覧表示する。`→` がカレントディレクトリの対象。

```bash
dash projects
```

表示テスト用データを入れる（待機中・稼働中・報告待ち・完了・孫・実測値なしが全部出る）。
見た目の調整中はこれを使う。

```bash
dash demo
```

初期化する。`--purge` を付けると孫の自己申告ファイルも削除する。対象は現在のプロジェクトのみ。
`reset` は `missions/<slug>/` を残す。いま映しているチームを `reset` すると、そのまま
**待機画面に戻る**（`.current` は動かさないので、別のプロジェクトへ飛んだりはしない）。
`reset` の後に残るのは「（ミッション未開始）」の器だけなので、**他に選べるタブがあれば
そのタブは画面に出ない**（0. の末尾を参照）。過去の記録がある場合、そちらのタブは残る。

```bash
dash reset
dash reset --purge
```

記録ごと削除する。既定では `trash/<slug>-<日時>/` へ移すだけなので、
フォルダを元の場所に戻せば復旧できる。`--force` を付けたときだけ完全に削除する。
いま映しているチームを消した場合は、次の `start` まで待機画面になる。

```bash
dash remove              # 対話で確認する
dash remove --yes        # 確認を省く（非対話環境では --yes が必須）
dash remove --yes --force  # ゴミ箱へ移さず完全に削除する
```

---

## 5.1. 記録を消す

`start` するたびにタブが1つ増えるので、放っておくと溜まる。消し方は3通りある。

| 操作 | 何が起きるか | 元に戻せるか |
| --- | --- | --- |
| 画面のタブの「✕」 | そのタブ1件を `trash/` へ移す | ゴミ箱のフォルダを戻せば復旧できる |
| 画面の「完了した記録を削除」 | 「完了」と出ているタブをまとめて `trash/` へ移す | 同上 |
| `dash remove` | `missions/<slug>/` を丸ごと（`history/` ごと）`trash/<slug>-<日時>/` へ移す | 同上 |
| `dash remove --yes --force` | 完全に削除する | 戻せない |

- 過去の記録は `dash history` で一覧できる。プロジェクトの一覧は `dash projects`。
- **画面から消せるのは「過去の記録」と「完了した現在のミッション」。** 稼働中・待機中の
  ミッションには「✕」が付かず、要求が届いてもサーバーが断る。
- 現在のミッション（`state.json`）が `history/` へ移されるのは、同じプロジェクトで次の
  `start` を打ったときだけ。**1度きりのミッションは完了しても現在の枠に残り続ける**ので、
  そのままでも画面から消せるようにしてある。
- そのプロジェクトに過去の記録が1件も残らない場合は、空のフォルダを残さずプロジェクトごと
  `trash/` へ移す（結果は `dash remove` と同じ）。
- いま映しているプロジェクトを `dash remove` で消すと `.current` も外れ、
  次の `start` まで待機画面になる。
- 削除は `POST` だけで受ける。`GET` では 405 を返して絶対に消さない。
- 画面からの削除に完全削除の経路は無い。`permanent` を送ると 400 で拒否される。
  戻せない操作は CLI（`--force`）でしかできない。

---

## 6. 別のプロジェクトを対象にしたいとき

`--project` にスラッグ（前方一致でよい）を渡す。

```bash
dash status --project learning
dash done --id SCOUT-A --project learning --headline "..."
```

前方一致が2件以上に当たる場合はエラーになるので、`dash projects` で正しいスラッグを確認する。
環境変数 `AGENT_DASHBOARD_PROJECT` でも指定できる（`--project` の方が優先）。

存在しない名前を渡した場合は、その名前で新しいプロジェクトが作られる。
つまり `--project 手動テスト` のようにディレクトリと無関係なミッションも作れる。

---

## 6.1. 同じディレクトリで2本以上を同時に走らせるとき（必須）

ミッションの記録先は**プロジェクト（＝作業ディレクトリ）ごとに1つ**しかない。
同じディレクトリで2本目の `start` を打つと、1本目は**稼働中のまま履歴へ押し出される**。
押し出された記録にはもう書き込めないので、1本目の `done` は「居ません」で弾かれ、
以後の記録は残らない。**あとから完了に直す方法もない**（実測でない値を書かない設計のため）。
事前に記録先を分けることが唯一の対策になる。

```bash
# 同じディレクトリで並行するときは、start に --project で一意な名前を付けて分ける
# （存在しない名前を渡せば、その名前で新しいプロジェクトが作られる）
dash start --project PAVS_ER-issue51 --title "issue51 の影響範囲調査" --model claude-opus-5

# そのミッションの add / done / finish にも、すべて同じ --project を付ける
dash add --project PAVS_ER-issue51 --id SCOUT-A --name "偵察A" --model claude-sonnet-5 --mission "..."
dash done --project PAVS_ER-issue51 --id SCOUT-A --headline "..."
dash finish --project PAVS_ER-issue51 --headline "..."
```

- **`--project` は4コマンドすべてに付ける。** 1つでも付け忘れると、そのコマンドだけ
  カレントディレクトリ側の記録＝もう一方のチームに書き込まれ、2本の記録が混ざる。
- 別々のディレクトリで走るミッション同士は記録先がもともと別なので、`--project` は要らない。
- 分けたぶんはそれぞれ独立したプロジェクトとして扱われる。画面にはタブが2つ並び、
  稼働中のチームは同時に映る。
- 環境変数 `AGENT_DASHBOARD_PROJECT` でも指定できる（`--project` の方が優先）。

押し出しが起きてしまったときは、2か所で知らせる。**どちらも直さずに知らせるだけ**。

| どこで | 出るもの |
| --- | --- |
| `start` | `⚠️ 退避した「…」は稼働中のまま終わりました` に続けて、`--project` で分ける手順 |
| `done` | 「現在のミッション『…』に居ません／直近の履歴『…』に同じ ID が居る」＋ ここで `add` すると記録が混ざるという警告 |

`done` の案内は、**「まだ add していない」のか「押し出された」のかで言い分ける**。
直近5件の履歴に同じ ID が居れば押し出しと見なす（`update_state.find_in_history`）。
区別せずに「先に add してください」とだけ言うと、押し出された人はその通りに `add` して、
**押し出した側のチームに1本目の機体を混ぜてしまう**。だから言い分けている。

---

## 7. 孫エージェント（サブエージェント自身が子を作る場合）

書き込みの衝突を避けるため、孫は**自分の分を自分専用のファイルに書く**。
書き込み先はプロジェクトごとに異なるので、**サブエージェントには絶対パスを渡す**。
パスは `dash status` の末尾に表示される。

サブエージェントへの指示文に、次の内容を含める（`<孫の置き場>` は実際のパスに置き換える）。

> 子エージェントを起動したら、次のJSONを
> `<孫の置き場>/<子のID>.json` に書き出してください。
> 1体につき1ファイルです。他のファイルは絶対に触らないでください。
>
> ```json
> {
>   "id": "A-1-x",
>   "name": "走査A-1-x",
>   "parentId": "A-1",
>   "model": "claude-haiku-4-5",
>   "mission": "分類結果の重複を除く",
>   "status": "running",
>   "startedAt": "2026-07-30T10:30:00+09:00",
>   "log": [{ "at": "2026-07-30T10:30:00+09:00", "who": "分析A-1", "text": "走査A-1-x 誕生" }]
> }
> ```
>
> 完了したら同じファイルを次の形で上書きしてください。
> 実測できなかった値は `null` のままにしてください（推測で埋めないこと）。
>
> ```json
> {
>   "id": "A-1-x",
>   "name": "走査A-1-x",
>   "parentId": "A-1",
>   "model": "claude-haiku-4-5",
>   "mission": "分類結果の重複を除く",
>   "status": "done",
>   "startedAt": "2026-07-30T10:30:00+09:00",
>   "finishedAt": "2026-07-30T10:30:22+09:00",
>   "result": { "elapsedSec": 22, "tokens": null, "toolCalls": null, "headline": "重複3件を除去" },
>   "log": [{ "at": "2026-07-30T10:30:22+09:00", "who": "走査A-1-x", "text": "帰還 — 重複3件を除去" }]
> }
> ```

サーバー側が `state.json` と `agents/*.json` を自動でマージするので、画面側の設定は不要。
孫のカードには「自己申告」バッジが付く。

- `generation` は書かなくてよい（`parentId` から再計算される）
- ID が `state.json` と衝突した場合は `state.json` 側が採用される
- JSONが壊れているファイルは無視され、画面右下に警告が出る（他の機体の表示は止まらない）

---

## 8. 仕組み上の制約（把握しておくこと）

| 項目 | 実際 |
| --- | --- |
| 更新のタイミング | 「起動直後」と「完了通知時」の2点だけ。実行中は無音 |
| 進捗% | 実測ソースが無いため表示しない。進捗バーも置いていない（稼働中かどうかはロボの動きとカードの色） |
| 報告待ち | 「稼働中の子を持つ稼働中の親」を読み取り側で導出して琥珀色で出す。`state.json` には書かれず、報告用のコマンドも無い。親が子と並行して自分の作業をしている場合は実態とズレる |
| 稼働中の経過時間 | 画面側が `startedAt` から毎秒カウントアップして表示している |
| トークン数・ツール回数 | 完了通知に含まれた場合のみ実測値。無ければ「—」 |
| 画面の更新 | 1秒ごとに `/api/state` をキャッシュ無効で取得し、内容が変わったときだけ再描画 |
| 映すチームの決定 | サーバー側だけで決まる。ブラウザは覚えないので、どの端末で開いても同じチームが出る |
| 並列稼働 | 稼働中のチームは全部、縦に積んで同時に映る。並び順は開始が新しい順 |
| 記録先の数 | プロジェクト（＝作業ディレクトリ）ごとに1つだけ。同じディレクトリで2本目を `start` すると1本目は稼働中のまま履歴へ押し出され、以後書き込めない。並行させるなら `--project` で分ける（→ 6.1） |
| 本体と運用ルールの版 | 別々に古くなる。本体を更新しても各 CLI の設定ファイルの運用ルールは `install.py` を実行し直すまで古いまま残る。ずれていれば `start` とサーバー起動時に知らせが出る（`diagnose.py` の「運用ルールの版」でも確認できる） |
| チームの入れ替わり | `/api/state` の `teams` から居なくなったチームは、その場で器ごと片付ける |
| 同じフォルダで start し直したとき | スラッグは変わらないので、`mission.startedAt` の変化を見て前のミッションのログを捨てる |
| 通信断のとき | 直前の表示を保持し、右上の接続インジケータが赤くなる |
| 系統樹の配置 | 親は自分が生み出した子たちの縦の中央に置く。指令塔は木全体の上下中央。木が画面に収まるときだけ縦中央に寄せ、はみ出すときは上寄せ |
| 配置の反映方法 | `.agent` の `top` だけで動かす。DOM の並べ替えはしない（要素を動かすと配下の CSS アニメーションが 0 に戻るため）。`transform` も使わない（誕生アニメーションと衝突する） |

---

## 9. ファイル構成と環境

このディレクトリの中身。パスはすべて実行時に解決されるので、どこに置いても動く。

```text
├─ dash.cmd / dash    ランチャ（Windows / POSIX）
├─ dash.py            統合エントリポイント
├─ server.py          配信サーバー（全プロジェクト共通・1つだけ起動する）
├─ update_state.py    状態更新CLI（状態への唯一の書き込み口）
├─ install.py         初期設定（各 AI CLI の設定ファイルへの書き込み）
├─ dashlib.py         共通ロジック（パス解決・プロジェクト識別・マージ・書式）
├─ build_vsix.py      VSCode 拡張機能を組み立てて入れる（dash ext ...）
├─ test_tabs.py       タブ一覧の動作確認。python test_tabs.py で走る（記録には触らない）
├─ README.md          導入手順（人間向け）
├─ OPERATION.md       この手順書
├─ EXTENSION_PLAN.md  拡張機能化の計画と設計（何を確かめて何を決めたか）
├─ extension/         VSCode 拡張機能のソース。ビルド工程なし・node_modules なし
│  ├─ package.json    拡張マニフェスト（版・コマンド・設定はここが正）
│  ├─ extension.js    拡張本体（素の CommonJS）
│  ├─ test_extension.js  動作確認。node extension/test_extension.js で走る
│  └─ media/          アイコンと、その生成スクリプト
├─ public/
│  ├─ index.html      ダッシュボード画面
│  └─ manual.html     取扱説明書（人間向け・サーバー経由で読む）
├─ dist/              組み立てた .vsix。作り直せるので追跡しない
├─ missions/          プロジェクトごとの記録（保存先は環境変数で変更可）
│  └─ .current        いま画面に映すチームのスラッグ。start と demo だけが書き換える
└─ trash/             削除したプロジェクトの置き場。戻したいときはフォルダを missions/ へ移す
```

### 環境変数

| 変数 | 効果 |
| --- | --- |
| `AGENT_DASHBOARD_HOME` | 記録の保存先を変える（既定はこのディレクトリ。書き込み不可なら OS のユーザーデータ領域） |
| `AGENT_DASHBOARD_PROJECT` | 対象プロジェクトを固定する（`--project` の方が優先） |
| `PORT` | サーバーの既定ポート（`--port` の方が優先） |
| `CLAUDE_CONFIG_DIR` | Claude Code の `CLAUDE.md` の場所 |
| `CODEX_HOME` | Codex CLI の `AGENTS.md` の場所 |
| `GEMINI_CLI_HOME` | Gemini CLI の `GEMINI.md` の場所 |
| `COPILOT_HOME` | GitHub Copilot CLI の設定ファイルの場所 |
| `OPENCODE_CONFIG_DIR` | opencode の `AGENTS.md` の場所 |
| `AGENT_DASHBOARD_AGENTS_FILE` | 自分で追加した CLI のリスト（既定：`agents.json`、記録の保存先の隣） |
| `AGENT_DASHBOARD_LANG` | 出力の言語（`en` / `ja` / `zh` / `ko`）。保存された設定より優先される |

### 表示言語

コマンドの出力は `en` / `ja` / `zh` / `ko` に対応している。決まり方は上から順で、最初に決まったものが使われる。

1. 環境変数 `AGENT_DASHBOARD_LANG`
2. `dash lang <コード>` で保存した設定（`<記録の保存先>/lang`）
3. 環境変数 `LC_ALL` / `LC_MESSAGES` / `LANG`
4. OS の表示言語
5. 英語

```bash
python <ツールの場所>/update_state.py lang        # 今の言語と決まり方を見る
python <ツールの場所>/update_state.py lang en     # 変える
```

画面（ダッシュボードと取扱説明書）の言語は**ブラウザ側**で別に決まる（右上のセレクタ。初回はブラウザの言語設定から自動判定し、以後はそのブラウザに保存される）。**コマンドの言語を変えても画面は変わらない。**逆も同じ。

訳が無い文は英語のまま出る。翻訳の抜けでコマンドが止まることはない。

### API

| エンドポイント | 返すもの |
| --- | --- |
| `/api/state` | 映すチームの状態（`teams`）＋ タブ一覧（`tabs`） |
| `/api/run?slug=<slug>&runId=<runId>` | 過去1回分の完全な状態。`runId` を省くと進行中のもの。slug に日本語が入るのでクエリで受ける |
| `/api/projects` | プロジェクト一覧のみ |
| `/api/env` | 実行環境（各種パス・プラットフォーム・Python バージョン） |
| `POST /api/project/delete` | プロジェクトの記録を削除（既定は `trash/` へ移動）。本文 `{"slug": "..."}`、`Content-Type: application/json` 必須 |
| `POST /api/history/delete` | ミッションの記録を `trash/` へ移す。本文 `{"runs": [{"slug","runId"}, ...]}` または `{"phase": "done"}`。`runId` に `null` を渡すと現在のミッション（`state.json`）が対象になるが、消せるのは完了したものだけ |

`tabs` は1秒ごとに取られるので**軽い一覧**にしてある（`runId` / `title` / `phase` /
`startedAt` / `finishedAt` / `projectName` / `agentCount` / `doneCount` / `isCurrent`）。
孫の自己申告までは開かない。中身が要るのは選ばれた1つだけなので、そこは `/api/run` で取る。

まだ一度も `start` していない器（`runId` が `null` かつ `phase` が `standby` かつ機体0体）は、
他に返すタブがあるときは `tabs` に含めない。**そのとき `teams` からも同じプロジェクトを外す。**
画面側は毎秒「`tabs` に無いチームは片付ける」と「`teams` にあるチームは作る」の両方をやるので、
ここが食い違うと同じチームの器を作っては壊すのを延々と繰り返す。

`doneCount` は締め忘れ（`phase` が `running` のまま全機 `done`）をタブに出すためだけの数で、
**数えられなかったときは `0` ではなく `null`** を返す。過去の記録（`history/`）も常に `null`。
`0` は「1体も帰っていない」という意味を持ってしまうので、「分からない」と区別できないと
画面が嘘をつく。

`runs` に不正な `slug` / `runId` が1つでも混ざっていたら、**何も消さずに 400** を返す。
`..` の混入は「一部失敗」ではなく要求そのものの誤りなので、途中まで消して半端な状態を
残さない。存在しないだけの記録は `failed` に入れて残りは進める。

破壊的な操作は `POST` だけで受ける。`/api/project/*` への `GET` は `405` と `Allow: POST` を返す
（ブラウザの先読みやうっかり踏んだリンクで記録が消えないようにするため）。

### 依存関係

外部ライブラリは一切使っていない。`http.server` / `json` / `pathlib` / `argparse` /
`datetime` / `hashlib` / `unicodedata` / `webbrowser` など、すべて Python 標準ライブラリ。
`public/` の HTML も CDN 参照ゼロの自己完結。Python 3.9 以降、Windows / macOS / Linux で動く。

VSCode 拡張機能も同じ方針。`.vsix` の組み立ては `zipfile` だけで済ませているので npm も
`vsce` も要らず、拡張本体も `require('vscode')` と Node 組み込みしか使わない（`node_modules` なし）。
唯一の例外は `extension/media/make_icons.py` で、絵を描き直すときだけ Pillow が必要。
実行には関わらないので、上の「外部ライブラリゼロ」は崩れていない。
