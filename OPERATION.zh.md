# 运行手册（供 Claude 使用）

[English](OPERATION.md) | [日本語](OPERATION.ja.md) | **中文** | [한국어](OPERATION.ko.md)

每当启动或完成一个子代理时，就按本手册更新状态。
**改写状态必须一律经由 `update_state.py` 进行，绝不可直接编辑 `state.json`。**
时刻、世代、日志、汇总统计全部由 CLI 自动处理，手工写入必定会破坏它们。

## 关于本文件中的写法

命令示例一律简写为 `dash`。它的实体是位于**与本文件相同目录**下的
启动器（Windows 是 `dash.cmd`，macOS / Linux 是 `./dash`），下面这三种写法的动作完全相同。

```bash
dash add --id SCOUT-A ...                    # 经由启动器
python <本目录>/dash.py add ...               # 直接调用启动器的本体
python <本目录>/update_state.py add           # 直接调用 CLI
```

**实际执行时使用的、带完整路径的命令示例，已经写入各编程工具的规则文件中。**
请直接照搬那边的写法（那是 `dash install` 按照当前环境生成的）。
本目录的实际位置也会显示在 `dash projects` 输出的开头。

## 目标项目自动决定

**目标项目由当前目录自动判定。**
无论在哪个项目下工作，直接执行同样的命令即可。
状态按项目分离，保存在 `missions/<slug>/` 中。

slug 是「目录名 + 完整路径的 6 位哈希」，因此不会与位于别处的同名目录混在一起。

---

## 0. 启动（只做一次，全部项目共用）

```bash
dash serve
```

用浏览器打开显示出来的 URL。**服务器只需启动一个。**
无论在哪个项目下工作，都会出现在这一个画面里。
如果端口被占用会自动顺延，因此请使用启动日志中显示的 URL。

**显示哪支小队由服务器决定。** 没有切换操作。

| 画面 | 何时 |
| --- | --- |
| 待机画面 | 一个标签页都没有时（还从未 `start` 过、把记录全部删掉了） |
| 运行中的小队 | 从 `start` 的那一刻起 |
| 已完成的小队 | `finish` 之后也照样继续显示 |
| 下一支小队 | 在下一次 `start` 时替换。上一支小队从画面上消失 |
| **多支小队纵向堆叠** | 并行运行中的小队会全部同时显示（按开始时间由新到旧） |

显示的是「**全部运行中的小队 ＋ `missions/.current` 所指的小队**」。改写 `.current` 的
**只有 `start`（以及 `demo`）**，`done` 和 `finish` 不会动它。所以已完成的画面
不会擅自跳到别的项目去。

在此之上，**在运行中的小队开始活动之后才完成的小队**也会保留。并行跑的两支中
一支先 `finish` 时，那支小队已经不是 `.current` 了，若没有这条规则，它就会在完成的
瞬间消失，谁也读不到完成报告。反过来，只是按顺序依次作业的情况
（上一支小队结束后才 `start` 下一支），上一支的完成时间早于下一支的开始时间，
那一支就会按要求从画面上退下。

为了不让一直停在 `running` 的记录无休止地显示下去，设有**默认 3 小时的时间窗**。
`state.json` 的更新时间比这更早的 `running` 不予显示。时间窗可用环境变量
`AGENT_DASHBOARD_ACTIVE_WINDOW`（秒）修改。如果要处理长时间完全没有通知的
任务，请把它调大。

**还从未 `start` 过的空壳（「（任务尚未开始）」），只在没有任何其他可选标签页时才显示。**
`dash reset` 之后，或者只创建了名字的项目就属于这种。它是点了也什么都不出的标签页，
在下一次 `start` 之前不会消失，放着不管就会堆积在标签行里。
但如果连什么都没有时也隐藏它，项目的存在本身就会从画面上消失，所以那种情况下要保留
（未开始的空壳之间不算作「其他标签页」。若计入，只要有 2 条就会两个都消失、掉回待机画面）。
刚 `start` 完、单元数为 0 的那种是 `running`，不适用本规则。

上一次任务的记录会转存到 `missions/<slug>/history/<runId>/`。**即使从画面上退下也
不会消失。** 用标签页选中就能事后回看内容（列表用 `dash history`）。

每个项目保留最近 20 条。超出的部分从旧到新移入 `trash/`。条数可用环境变量
`AGENT_DASHBOARD_HISTORY_KEEP` 修改（设为 `0` 则不转存，和以前一样直接覆盖）。
即使设为 `0`，已经堆积的 `history/` 也不会被清理。这是为了避免只是临时改了设置，
过去的记录就全部进了回收站。

---

## 0.05. 从 VSCode 扩展启动（推荐。人类的操作）

如果人类在用 VSCode，这是最省事的方式。Claude 这边不需要做任何事。

```bash
dash ext install     # 只做一次。之后重新加载 VSCode
```

此后按左端活动栏里的机器人图标，就会在编辑器标签页中打开。
命令面板的「Subagent Dashboard: 在标签页中打开」或状态栏的 🤖 Subagent Dashboard 也是同样效果。
想把它细长地放在左侧时，把设置 `agentDashboard.sidebarBehavior` 设为 `embed`。

要分发给别人时用 `dash ext package`。把 `dist/` 下生成的 `.vsix` 和说明书作为邮件附件发送。
扩展中已经内置了面板本体，在接收方那边会被放置到
`~/.claude/agent-dashboard`（首次会确认）。

**Claude 只需要知道以下几点:**

- 扩展通过 `GET /api/env` 判断服务器是否存活，只有 `toolRoot` 一致时才认定为「我们自己的服务器」
- 扩展先自行决定一个空闲端口，再用 `dash.py serve --port <编号> --no-retry` 启动。**由于禁止了服务器端的端口顺延，扩展掌握的编号与实际编号必定一致**
- 扩展启动的服务器会在 VSCode 退出时停止。它不会去动在终端里用 `dash serve` 启动的服务器
- 因此，**在 Claude 起了 `dash serve` 之后，人类再从扩展打开也不会重复启动**（扩展会复用已有的）

详情见 `EXTENSION_PLAN.md`，扩展的用法见 `extension/README.md`。

---

## 0.1. 从任意项目启动（Global Launch）

这是没有安装扩展时的入口。也就是从任意项目文件夹按 `Ctrl+Shift+D` 启动面板时的机制。

### 用途

- 同时推进多个项目
- 每次都要重新打开 agent-dashboard 文件夹很麻烦
- 想用 VSCode 的键盘快捷键快速访问

### 处理流程

```text
用户按下 Ctrl+Shift+D
        │
        ▼
  VSCode 执行键位绑定
        │
        ▼
  python open_dashboard.py
        │
        ▼
  从环境变量 AGENT_DASHBOARD_HOME
  检测 agent-dashboard 的位置
        │
        ▼
  启动 python dash.py serve
        │
        ▼
  连接到 http://127.0.0.1:3939
  ↓ 健康检查（最多等待 5 秒）
        │
        ▼
  用浏览器 / SimpleWebService 打开
```

### 技术细节

#### 位置的检测

`install.py` 执行时，按以下优先顺序检测并记录 agent-dashboard 的路径：

1. **环境变量 `AGENT_DASHBOARD_HOME`** 已经设置 → 使用该路径
2. **环境变量未设置** → 检测执行 `install.py` 的目录
3. **嵌入规则文件** → 编程工具自动执行的命令中包含完整路径

#### 服务器的启动

`open_dashboard.py` 按以下步骤启动服务器：

```python
# 1. 确定 agent-dashboard 的位置（按环境变量的设定顺序探索）
dashboard_home = AGENT_DASHBOARD_HOME or DEFAULT_PATH

# 2. 确认服务器是否已经启动
# 试着连接 http://127.0.0.1:3939

if 服务器没有响应:
    # 3. 启动新的服务器进程
    subprocess.Popen([
        "python",
        f"{dashboard_home}/dash.py",
        "serve",
        "--detach"  # 后台启动
    ])

    # 4. 健康检查（最多等待 5 秒）
    for i in range(50):  # 0.1秒 x 50 = 5秒
        if http://127.0.0.1:3939 有响应:
            break
        sleep(0.1)

# 5. 打开浏览器
webbrowser.open("http://127.0.0.1:3939")
```

#### 端口冲突时的处理

多个用户在同一台机器上工作时：

1. 用户A 按 `Ctrl+Shift+D` → 在端口 3939 启动
2. 用户B 按 `Ctrl+Shift+D` → 端口 3939 已被占用
3. 用户B 的服务器在 3940 启动
4. 各用户连接到自己已设定的端口

**因为面板内会显示出来，所以即使 URL 变了也不影响使用。**

#### IDE 集成的示例（VSCode）

`install.py` 写入 `keybindings.json` 的内容：

```json
{
  "key": "ctrl+shift+d",
  "command": "agentDashboard.open"
}
```

它直接调用扩展自带的命令。既不依赖路径也不依赖终端，所以
即使面板的存放位置变了也不会坏。**没有安装扩展时，
这个按键什么都不会发生**（因为命令不存在）。

写入位置是 VSCode 实际作为用户设置去读取的地方。

| OS | 位置 |
| --- | --- |
| Windows | `%APPDATA%\Code\User\keybindings.json` |
| macOS | `~/Library/Application Support/Code/User/keybindings.json` |
| Linux | `$XDG_CONFIG_HOME/Code/User/keybindings.json`（默认 `~/.config/Code/User/`） |

`Ctrl+Shift+D` 在 VSCode 标准配置中被分配给「运行和调试」。
在扩展的 `contributes.keybindings` 里，与标准分配之间的优先顺序会因环境而异，
所以写在必定能取胜的用户设置一侧。

> **关于 0.2.4 及更早的版本**
> 当时把写入位置定为 `~/.vscode/keybindings.json`。那里是扩展的存放处，
> VSCode 并不会从那里读取键位绑定。也就是说 `Ctrl+Shift+D` 当时根本没有生效。
> 0.2.5 的 `install.py` 会清理掉自己写在这个旧位置上的条目
>（不会碰用户自己写的条目）。

---

## 0.2. 支持多种 AI 编程工具

本工具能为以下 AI 编程工具写入运行规则：

| 工具 | 规则文件 |
| --- | --- |
| Claude Code | `~/.claude/CLAUDE.md` |
| Codex CLI | `~/.codex/AGENTS.md` |
| Gemini CLI | `~/.gemini/GEMINI.md` |
| GitHub Copilot CLI | `~/.copilot/copilot-instructions.md` |
| opencode | `~/.config/opencode/AGENTS.md` |
| Amp | `~/.config/amp/AGENTS.md` |
| Cline | `~/Documents/Cline/Rules/subagent-dashboard.md` |
| Roo Code | `~/.roo/rules/subagent-dashboard.md` |
| Windsurf | `~/.codeium/windsurf/memories/global_rules.md` |
| Qwen Code | `~/.qwen/QWEN.md` |

表中故意没有列出 **Cursor** 和 **Aider**，因为这两个工具都没有自动读取的全局规则文件（Cursor 读取项目级别的 `AGENTS.md`，Aider 需要配置中明确指定文件名）。对于这两个工具，可以用 `--agent-file` 指向项目级别的规则文件来实现支持。

**不在此表中的工具也可以支持** — 见下文 `--agent-file` 说明。

### 有三种方式为 AI 工具配置规则：

**方式 1：自动检测与安装（推荐）**

```
python install.py
```

扫描机器上已安装的工具，为每个工具写入规则。

**方式 2：仅为某个工具写入**

```
python install.py --agent codex
```

指定工具名称（`claude` / `codex` / `gemini` / `copilot` / `opencode` / `amp` / `cline` / `roo` / `windsurf` / `qwen`），只为该工具写入，即使它未安装也会尝试。

**方式 3：为所有已知工具写入**

```
python install.py --agent all
```

为所有内置工具写入，无论是否已安装。

**方式 4：支持表中没有的工具或自定义位置**

```
python install.py --agent-file /path/to/rules.md
```

用 `--agent-file` 指定的文件会被永久记录在 `agents.json` 中（与任务记录放在同一位置），之后：
- 会出现在 `--list-agents` 的列表中
- 每次运行 `install.py` 时都会自动更新
- 卸载时会被清理

`agents.json` 也支持手工编辑。每个条目的字段：
- `key` - 在 `--agent` 命令中使用的短标识符
- `label` - 工具的显示名称
- `home_env` - 指向工具主目录的环境变量名（可为空）
- `home` - 工具的主目录路径（可以用 `~` 开头）
- `file` - 规则文件名或相对路径

如果 `agents.json` 中某条的 `key` 与内置工具的 `key` 相同，会**覆盖**内置配置。这样当工具改变规则位置时，可以立即在本地更正，无需等待工具更新。

### 查看已配置的工具和写入状态

```
python install.py --list-agents
```

显示所有已知的工具（内置 + 自定义）、它们的规则写入位置、以及当前的写入状态。

---

## 1. 任务开始时

在启动任何一个子代理**之前**，在正在作业的项目目录下只执行一次。
在此之前的内容会被转存到 `history/<runId>/`，从只有指挥塔的状态开始
（不会影响其他项目）。**因为做了转存，上一次任务事后仍可回看。**
上一次任务残留下来的孙代理自报文件（`agents/*.json`）会一起移到转存位置。
之所以不带到下一次，是因为没有收到完成通知的孙代理会在此后的所有任务中
一直以「运行中」的样子排在那里。

即使转存失败，`start` 也不会中止（发出警告后继续）。因为「记录还在、却无法开始」
才更麻烦。

```bash
dash start --title "重构影响范围调查" --model claude-opus-5
```

**在同一目录下同时跑第二个任务时，提前用 `--project` 把记录位置分开，就不会发生冲突本身。**
即使忘了分开，现在只要有会话想把另一个会话正在运行的记录挤出去，工具就会拒绝并停下
（拒绝信息里会给出可以直接粘贴的 `--project` 用法）。只是在没有主人标记的环境中，
这层保护不起作用，仍会像以前一样被挤出去——只有这种情况下，事先分开才是唯一的对策。
→ 「6.1. 在同一目录下同时跑两个以上任务时」

---

## 2. 启动子代理之后立即

在调用 Agent 工具**之后立即**执行。不要事后一并补写，而是每启动一个就执行一次。

```bash
dash add --id SCOUT-A --name "侦察A" --model claude-sonnet-5 --mission "梳理出 src/ 下所有调用 API 的位置"
```

| 选项 | 省略时 | 备注 |
| --- | --- | --- |
| `--id` | **必填** | `SCOUT-A` `A-1` 之类的短标识符。会出现在日志和卡片上 |
| `--name` | 与 id 相同 | 在画面上大字显示的显示名 |
| `--parent` | `COMMAND` | 父级的 ID。让它生成了孙代理时，指定父代理的 ID |
| `--model` | 空（画面上显示「未知」） | 实际使用的模型 ID |
| `--mission` | 空 | 任务内容。在卡片上显示为两行 |
| `--status` | `running` | 只是先排好等待出动的话用 `standby` |
| `--project` | 当前目录 | 只有想以其他项目为对象时才指定 |

**自由文本用中文书写**（`--title` / `--name` / `--mission` / `--headline`）。会照你写下的样子记录，
照记录的样子显示在画面上——事后不会翻译。请对齐这份手册的语言，而不是对话的语言。
要改这个语言，执行 `dash lang <en|ja|zh|ko>`。**各规则文件里的说明也会当场被改写成新的语言**，
所以设置的语言就是组队时使用的语言（从下一次会话开始生效——规则在启动时读取）。
**已经存在的记录保持写下时的样子**，不会事后重新翻译。

**对已经在运行的会话，这份手册无法告知语言已经改变。** 因此 `start` 每次都会输出一行「自由文本请用这个语言书写」，
并且 `start` / `add` / `done` / `finish` 在你传入的文字看起来不是那个语言时会发出警告。**请相信命令的输出，而不是这份手册**
——输出是此刻生成的，而这份手册是上一次设置语言时写下的。警告不会阻止写入。要更正 `--name` / `--mission` / `--headline`，
就用相同的 `--id` 和改好的文字再执行一次同样的命令（实测值会保留）。任务标题事后无法更正。

世代会从 `--parent` 自动算出，不要指定。
**「等待回报」也会从 `--parent` 自动附加**，所以不需要申报指挥塔进入了等待状态
（把 `--parent` 指定正确，直接就等于等待回报的准确度）。

**同时启动多个时，有多少个就执行多少次 `add`。** 没有一次搞定的办法。

---

## 3. 收到完成通知后

```bash
dash done --id SCOUT-A --sec 42 --tokens 18400 --tools 11 --headline "在 7 个文件中定位到 23 处调用"
```

| 选项 | 省略时 | 备注 |
| --- | --- | --- |
| `--id` | **必填** | |
| `--sec` | 从 `startedAt` 起的实际经过秒数 | 通知里有耗时就填它。没有就省略 |
| `--tokens` | `null` → 画面显示「—」 | **通知里没有就必须省略** |
| `--tools` | `null` → 画面显示「—」 | **通知里没有就必须省略** |
| `--headline` | 空 | 结果的一行摘要。以绿色文字出现在卡片下部 |

### 关于实测值的绝对规则

**完成通知中没有包含的数值，不要估算，直接省略。** 省略了就会填入 `null`，
画面显示「—」。那才是正确的状态，不是应该去填补的缺失。
凭「大概两万 token 左右吧」就写成 `--tokens 20000`，
是在破坏这个面板的目的（看到实际发生了什么），因此绝对不要做。

---

## 4. 全员完成后

```bash
dash finish --headline "已确定影响范围，修改对象定为 12 个文件"
```

- 阶段变为「已完成」，并给出汇总。**画面就保持这个完成状态留着**（直到下一次 `start`）
- 单元数、Token 合计、耗时由 CLI 汇总（除 `--headline` 之外不需要传任何东西）
- Token 合计是传了 `--tokens` 的那些单元的合计。若全部单元都未取得则显示「—」
- 一直停留在 `standby` 的单元会被当作「未出动即结束」计为完成

### 对忘记收尾的监视

即使忘了敲 `finish`，**也不会坏掉任何东西，也不会停下任何东西**。所以察觉不到。
放着不管的话，画面上会一直显示「运行中」，在下一次 `start` 时以运行中的状态流入历史，
此后永远作为「未完成」的记录留下（**没有任何办法事后把它改成完成**）。

忘记收尾这件事，必定发生在把最后一个单元 `done` 之后、注意力转向整理报告的那一瞬间，
所以在包含该处的 4 个地方发出提示。**这 4 处都只是提示，不做任何修正**。
事后再写 `finishedAt`，那就不是实测值了。

| 在哪里 | 何时出现 | 出现的内容 |
| --- | --- | --- |
| `done` | 把最后一个单元置为完成之后立即 | `★ 至此 N 个单元已全部归队。…` ＋ 可直接粘贴的 `finish` 命令行 |
| `status` | 在忘记收尾期间一直 | 阶段显示为「运行中（**全部归队·未收尾**）」／距最后一次归队的经过时间 |
| `start` | 在忘了收尾的状态下开始下一个时 | `⚠️ 转存的「…」中 N 个单元已全部归队，但没有敲 finish。` |
| 画面 | 在忘记收尾期间一直 | 任务栏和标签页的标牌变为琥珀色，闪烁显示「**全部归队·未收尾**」「**未收尾**」 |

判定为「忘记收尾」，只在下列条件全部满足时。

- 任务处于 `running`（`done` 是已收尾，`standby` 是还没开始）
- 存在**1 个以上**子代理（0 个的话就只是「开始了而已」，还谈不上收尾）
- 这些子代理**全部为 `done`**（哪怕有 1 个还在运行中，就是还在等待过程中）

指挥塔不计入。在敲 `finish` 之前一直是 `running` 才是正确的样子，
一旦计入，条件就永远凑不齐，警告一次也不会出现。

如果在还有单元没归队的情况下 `start` 了下一个，那不是「忘记收尾」而是**中途放弃**，
所以 `start` 会区分开来说 `⚠️ …在运行中的状态下结束了（有尚未归队的单元）`。
这条警告会在**同一会话重新打 start**（自己忘记收尾）、**用 `--force` 把别的会话的
记录挤出去**、或者挤出没有主人标记的记录时出现。若在没有 `--force` 的情况下
试图挤出主人不同的记录，则会在出现这条警告之前就被拒绝拦下。
→ 「6.1. 在同一目录下同时跑两个以上任务时」

---

## 4.1. 收尾时固化「记录中没有的机体」

`finish` 和 `autofinish` 在把任务标记为完成**之前**，会把那一刻仍在运行、却不在记录里的机体，固化进 `state.json` 的 `orphans` 里。

没有这一步的话，收尾的瞬间那个机体就会同时从画面和历史记录中消失（因为只有运行中的任务才会去读实测数据）。它存在过这件事，本身就留不下痕迹。

固化下来的机体，无论是在完成后的画面里还是历史记录里，都留在收尾时的位置：本来能定位到系统树里的，还在系统树里；本来定不下位置的，还在下方的区块里。它已经不再运行，所以以**已完成的样子**显示。区块下方的说明文字，只有在全部是固化下来的机体时，才会换成过去式（对应的键是 `live.bay_note_frozen`）。

固化时的实测值（耗时、Token、工具调用次数）也会显示在卡片上。

**不计入单元（机体）数。** 收尾之后不会再重新生成。

`finish` / `autofinish` 的输出也会多出一行：

```text
  无记录的机体          {n} 台（按当时的样子固化到记录中）
```

---

## 4.2. 会话结束时自动收尾（autofinish）

```bash
dash autofinish
dash autofinish --project <名字>
dash autofinish --headline "..."
```

**用途是从 SessionEnd hook 调用。** 会话一结束，就自动把任务收尾。忘记收尾是这个工具里**唯一没法事后补救**的坏法——画面会一直显示「运行中」，下一次 `start` 时这条记录会在运行中的状态下被推入历史，从此再也无法改成完成。催促这件事送不到已经离开座位的人手上，所以把它绑定在会话的结束上，而不是绑定在人身上。

- **没有正在运行的任务时，什么都不输出就结束。**
- **不加 `--project` 时，会把这个目录下所有正在运行的任务都收尾。** 也包括用 `--project` 分开记录、并行跑的那一部分。**不会牵连别的文件夹里的任务**（只处理 `project.path` 与当前目录一致的那些）。
- 明确指定 `--project <名字>` 时，只收尾那一个。
- 省略 `--headline` 时会填入相当于「会话结束时自动收尾」的文字。
- 锁一次只取一把。
- **不会收尾其他会话开的任务。** 同一个文件夹同时开两个 Claude Code 并不罕见，实测中真的出现过**一边的会话刚结束，另一边还在干活的任务就被收尾了**（2026-09-03 在实机上踩到）。`start` 会把开启该任务的会话 ID 记进 `state.json` 的 `mission.sessionId`（取自环境变量 `CLAUDE_CODE_SESSION_ID`），`autofinish` 会对照自己的会话 ID，**主人不同的任务就默默跳过、不收尾**；取不到会话 ID 的环境仍按老样子收尾（「不知道就不收尾」会让这套机制形同虚设——两害相权取其轻，收得过多能靠下一次 `start` 重来，收尾漏掉却补不回来）。这条同样的规则，反过来（不知道时就交给写入方判断）也用在 `start` / `add` / `done` / `finish` / `log` / `demo` 上。→ 「6.1. 在同一目录下同时跑两个以上任务时」
- **注意**：`/clear` 也会触发 SessionEnd，所以工作到一半也可能被收尾。收得过多，可以在下一次 `start` 时重来；但**收尾漏掉是没法补救的**。
- 设置示例（`.claude/settings.local.json`）：

```json
"SessionEnd": [
  {
    "matcher": "",
    "hooks": [
      { "type": "command", "command": "python 'C:\\path\\to\\update_state.py' autofinish; exit 0" }
    ]
  }
]
```

---

## 5. 辅助命令

只追加一行实况。想把判断或方针转变留在日志里时使用。

```bash
dash log --who "指挥塔" --text "决定重新执行侦察B"
```

以表格形式显示当前项目的状态。用于确认写入是否生效。
孙代理的写入位置的绝对路径也会显示在末尾。

```bash
dash status
```

列出已登记的项目。`→` 是从当前目录判定出的对象。

```bash
dash projects
```

放入用于显示测试的数据（待机中、运行中、等待回报、已完成、孙代理、无实测值全都会出现）。
调整外观时用这个。

```bash
dash demo
```

初始化。加上 `--purge` 会连孙代理的自报文件也一并删除。对象仅限当前项目。
`reset` 会保留 `missions/<slug>/`。对正在显示的小队执行 `reset`，画面就会
**回到待机画面**（因为不动 `.current`，所以不会跳到别的项目去）。
`reset` 之后剩下的只有「（任务尚未开始）」的空壳，所以**只要还有其他可选的标签页，
该标签页就不会出现在画面上**（参见 0. 的末尾）。若有过去的记录，那些标签页会保留。

```bash
dash reset
dash reset --purge
```

连记录一起删除。默认只是移到 `trash/<slug>-<日期时间>/`，
把文件夹放回原处就能恢复。只有加上 `--force` 时才会彻底删除。
如果删掉的是正在显示的小队，在下一次 `start` 之前会变成待机画面。

```bash
dash remove              # 交互式确认
dash remove --yes        # 跳过确认（在非交互环境中 --yes 是必需的）
dash remove --yes --force  # 不移入回收站，直接彻底删除
```

---

## 5.1. 删除记录

每 `start` 一次就会多一个标签页，放着不管就会堆积。删除方法有 3 种。

| 操作 | 会发生什么 | 能否还原 |
| --- | --- | --- |
| 画面上标签页的「✕」 | 把该标签页这 1 条移入 `trash/` | 把回收站里的文件夹放回去就能恢复 |
| 画面上的「删除已完成的记录」 | 把显示为「已完成」的标签页一并移入 `trash/` | 同上 |
| `dash remove` | 把 `missions/<slug>/` 整个（连同 `history/`）移入 `trash/<slug>-<日期时间>/` | 同上 |
| `dash remove --yes --force` | 彻底删除 | 无法还原 |

- 过去的记录可用 `dash history` 列出。项目的列表用 `dash projects`。
- **能从画面上删除的只有「过去的记录」和「已完成的当前任务」。** 运行中、待机中的
  任务上不会有「✕」，即使请求送到了服务器也会拒绝。
- 当前任务（`state.json`）被移入 `history/` 的时机，只有在同一项目下敲了下一次
  `start` 的时候。**只做一次的任务即使完成了也会一直留在当前的位置**，
  所以特意做成了那样也能从画面上删除。
- 如果该项目一条过去的记录都不剩，就不留下空文件夹，而是连项目一起移入
  `trash/`（结果与 `dash remove` 相同）。
- 用 `dash remove` 删掉正在显示的项目时，`.current` 也会被解除，
  在下一次 `start` 之前会变成待机画面。
- 删除只接受 `POST`。对 `GET` 返回 405，绝不删除。
- 从画面上删除没有彻底删除的通路。发送 `permanent` 会以 400 被拒绝。
  无法还原的操作只能通过 CLI（`--force`）执行。

---

## 6. 想以其他项目为对象时

给 `--project` 传入 slug（前缀匹配即可）。

```bash
dash status --project learning
dash done --id SCOUT-A --project learning --headline "..."
```

前缀匹配命中 2 条以上时会报错，请用 `dash projects` 确认正确的 slug。
也可以用环境变量 `AGENT_DASHBOARD_PROJECT` 指定（`--project` 优先级更高）。

传入不存在的名字时，会以该名字创建新项目。
也就是说，也可以像 `--project 手动测试` 这样创建与目录无关的任务。

---

## 6.1. 在同一目录下同时跑两个以上任务时

任务的记录位置**每个项目（＝作业目录）只有一个**。2026-09-03，同一目录下实际同时跑了两个
Claude Code 会话，后开始的一方在谁都没察觉的情况下，抢走了先开始那一方的记录。以下说明
的是这之后加入的保护。

**现在 `start` / `add` / `done` / `finish` / `log` / `demo` 会把即将写入的记录的主人
（`mission.sessionId`，即执行了 `start` 的会话的 `CLAUDE_CODE_SESSION_ID`）与调用者
自己的会话 ID 做对照。** 只有在确认二者不同时，才会不写入、**以退出码 1 拒绝**。

```
错误：这个项目的记录目前正被另一个会话使用。
      目标: <项目>
      这里的记录是「<当前记录的标题>」（开始于<经过时间>前）。
      在这里 start 会把那条正在运行的记录挤进历史，
      之后就再也无法把它改成完成了。
      没有开始任何东西。
      请为自己的工作准备专属记录。在下面这 4 条命令上都加上同一个 --project：
        start --project "<唯一名字>" --title "<任务名>" --model <自己的模型ID>
      如果仍要执行，请加上 --force。
```

`add` / `done` / `finish` / `log` 被拒绝时形式相同，只是「挤进…」的部分变成
「写入会导致记录混在一起」。**`<唯一名字>` 由工具自己生成并提示**（由调用者的会话 ID
决定，所以同一个会话不管调用多少次都是同一个名字）。不需要自己想一个唯一的名字，
也不需要费力地让 4 条命令用同一个名字——把错误里给出的那一行原样粘贴 4 次即可。

- **仍要强行执行时，加上 `--force`。** 加在 `start` / `demo` 上，会像以前一样把对方的记录
  在运行中的状态下挤进历史。加在 `add` / `done` / `finish` / `log` 上，会像以前一样写入。
- **被挤出去的记录会留下「被什么、在何时挤出」的痕迹**（`mission.interruptedBy`）。
  从历史打开那条记录时出现的「未完成」标牌，把鼠标移上去就能读到。**这个标记不限于
  使用了 `--force` 的情况。** 自己忘记收尾时（同一个会话没有关闭上一个任务就打了下一次
  `start`）也会同样留下——这是为了让「未完成」的理由事后可以追溯，并不是专门给别的
  会话的挤出用的。
- **子代理不会碰到这个拒绝。** 因为它们原样继承父级的 `CLAUDE_CODE_SESSION_ID`，
  所以子代理对指挥塔用 `start` 开启的任务执行 `add` / `done` / `finish` / `log`，
  依然会被当作「同一个会话」而照常通过。
- **`demo` 即使不被拒绝，也一定会先转存。** 它是插入显示测试用假数据的命令，
  对别人的记录会受到和上面一样的拒绝。除此之外，**即使是对自己会话的记录，
  它也一定会在覆盖之前先转存进历史**（`interruptedBy` 的 `by` 是 `demo`）。因为
  「插入假数据」不等于「可以抹掉之前的记录」。以前这里既没有对照也没有转存，
  正在运行的记录会连历史里都不留就消失。

### 保护不起作用的情况

**只有当挤出的一方和被挤出的一方都带有 `mission.sessionId` 时，才能分辨出主人。**
只要符合下面两条中的任意一条，工具就无法判断「是不是别的会话」，于是**照旧无防备地
放行**（如果改成「不清楚就拦下」，会让工具本身在没有主人标记的环境里根本无法使用，
所以特意选择了放行这一边）。

- 记录没有 `mission.sessionId`（0.9.1 之前 `start` 的记录）
- 这台机器没有拿到 `CLAUDE_CODE_SESSION_ID`（hook 未接好、或者人从别的终端手动敲命令等）

在这种情况下如果发生了挤出，会在 2 个地方提示。**两处都只是提示，不做任何修正**，
事后也没有办法把它改成完成。

| 在哪里 | 出现的内容 |
| --- | --- |
| `start` | 在 `⚠️ 转存的「…」在运行中的状态下结束了` 之后，接着给出用 `--project` 分开的步骤 |
| `done` | 「不在当前任务『…』中／最近的历史『…』中有相同的 ID」＋ 在此处 `add` 会导致记录混在一起的警告 |

`done` 的提示会**区分是「还没 add」还是「被挤出去了」**。若最近 5 条历史中存在相同的 ID，
就视为被挤出（`update_state.find_in_history`）。若不加区分、只说「请先 add」，被挤出去
的人就会照办去 `add`，**把第一个的单元混进挤走它的那支小队里**。所以才要区分着说。
**这条路径在自己忘记收尾时也会走到**（同一个会话的写入本来就不属于拒绝的对象）。
不要把它和主人不同时的拒绝混为一谈。

### 事先用 `--project` 分开是不是必须的

```bash
# 在同一目录下并行时，给 start 加上 --project 起一个唯一的名字来分开
# （传入不存在的名字，就会以该名字创建新项目）
dash start --project PAVS_ER-issue51 --title "issue51 的影响范围调查" --model claude-opus-5

# 该任务的 add / done / finish 也全部加上同样的 --project
dash add --project PAVS_ER-issue51 --id SCOUT-A --name "侦察A" --model claude-sonnet-5 --mission "..."
dash done --project PAVS_ER-issue51 --id SCOUT-A --headline "..."
dash finish --project PAVS_ER-issue51 --headline "..."
```

在这层保护加入之前，本文档把这个写成**必须**。理由是「不事先分开的话，之后一旦有别的
会话在同一目录里开始，记录就会立刻坏掉，而且没有办法修复」。但这个说法本身站不住脚。
**先开始的一方没有办法预测之后会有别的会话开始。** 让人事先防范一件无法预测的事，
这正是事故真正发生的原因。

现在即使忘了分开也不会坏掉。**和别的会话冲突的写入只会被拒绝，记录本身完好无损地
留下来。** 被拒绝的一方，照着工具打印出来、可以直接粘贴的 `--project` 提示重新来一次
就行。**所以它不再是「必须」。** 不过事先分开依然有意义——因为这样冲突本身就不会发生，
省去了被拒绝后再重新来一次的麻烦。**能提前分开的话就提前分开。忘了的话，工具也会拦下来。**

唯一的例外是符合上面「保护不起作用的情况」的环境，在那种环境里，事先分开依然是
唯一的对策，和以前一样。

- **`--project` 要加在全部 4 条命令上。** 哪怕漏掉一条，那一条命令就会写入
  当前目录一侧的记录＝另一支小队，两边的记录就混在一起了。
- 在不同目录下运行的任务之间，记录位置本来就是分开的，所以不需要 `--project`。
- 分开之后的各部分会被当作彼此独立的项目处理。画面上会并排出现两个标签页，
  运行中的小队会同时显示。
- 也可以用环境变量 `AGENT_DASHBOARD_PROJECT` 指定（`--project` 优先级更高）。
- **`autofinish` 可以一次把这两支都收尾。** 不加 `--project` 调用时，会把
  **当前目录下所有正在运行的任务**都收尾（包括分开的那一支）。只需要从
  SessionEnd hook 调用一次即可。

---

## 7. 孙代理（子代理自己再生成子代理的情况）

为了避免写入冲突，孙代理**把自己的部分写进自己专用的文件**。
写入位置因项目而异，所以**必须把绝对路径交给子代理**。
路径会显示在 `dash status` 的末尾。

在给子代理的指示文中，包含以下内容（`<孙代理的存放处>` 替换为实际路径）。

> 启动子代理之后，请把下面的 JSON 写出到
> `<孙代理的存放处>/<子代理的ID>.json`。
> 每个代理一个文件。绝对不要碰其他文件。
>
> ```json
> {
>   "id": "A-1-x",
>   "name": "扫描A-1-x",
>   "parentId": "A-1",
>   "model": "claude-haiku-4-5",
>   "mission": "去除分类结果中的重复",
>   "status": "running",
>   "startedAt": "2026-07-30T10:30:00+09:00",
>   "log": [{ "at": "2026-07-30T10:30:00+09:00", "who": "分析A-1", "text": "扫描A-1-x 诞生" }]
> }
> ```
>
> 完成后请用下面的形式覆盖同一个文件。
> 无法实测到的值请保持 `null`（不要凭推测填补）。
>
> ```json
> {
>   "id": "A-1-x",
>   "name": "扫描A-1-x",
>   "parentId": "A-1",
>   "model": "claude-haiku-4-5",
>   "mission": "去除分类结果中的重复",
>   "status": "done",
>   "startedAt": "2026-07-30T10:30:00+09:00",
>   "finishedAt": "2026-07-30T10:30:22+09:00",
>   "result": { "elapsedSec": 22, "tokens": null, "toolCalls": null, "headline": "已去除 3 处重复" },
>   "log": [{ "at": "2026-07-30T10:30:22+09:00", "who": "扫描A-1-x", "text": "归队 — 已去除 3 处重复" }]
> }
> ```

服务器端会自动合并 `state.json` 与 `agents/*.json`，所以画面一侧不需要任何设置。
孙代理的卡片上会带有「自行申报」徽章。

- `generation` 不写也可以（会从 `parentId` 重新计算）
- ID 与 `state.json` 冲突时，采用 `state.json` 一侧
- JSON 损坏的文件会被忽略，并在画面右下角给出警告（其他单元的显示不会停止）

---

## 8. 机制上的限制（必须掌握）

| 项目 | 实际情况 |
| --- | --- |
| 更新的时机 | 只有「启动之后立即」和「收到完成通知时」这 2 个点。执行期间是静默的 |
| 进度% | 因为没有实测来源所以不显示。也没有放进度条（是否在运行看机器人的动作和卡片的颜色） |
| 等待回报 | 由读取一侧推导出「拥有运行中子代理的运行中父代理」，以琥珀色显示。它不会写入 `state.json`，也没有用于上报的命令。父代理与子代理并行地做自己的工作时，会与实际情况有偏差 |
| 运行中的经过时间 | 由画面一侧从 `startedAt` 每秒递增显示 |
| Token 数、工具次数 | 只有完成通知中包含时才是实测值。没有就显示「—」 |
| 画面的更新 | 每秒以禁用缓存的方式获取 `/api/state`，只有内容变化时才重绘 |
| 显示哪支小队的决定 | 完全在服务器一侧决定。浏览器不记忆，所以在哪台设备上打开都显示同一支小队 |
| 并行运行 | 运行中的小队会全部纵向堆叠、同时显示。排列顺序按开始时间由新到旧 |
| 记录位置的数量 | 每个项目（＝作业目录）只有一个。在同一目录下 `start` 第二个，第一个就会在运行中的状态下被挤进历史，此后无法写入。要并行就用 `--project` 分开（→ 6.1） |
| 本体与运行规则的版本 | 会各自变旧。即使更新了本体，各工具中的运行规则在重新执行 `install.py` 之前仍旧是旧的。若不一致，会在 `start` 和服务器启动时给出提示（也可以在 `diagnose.py` 的「运行规则的版本」中确认） |
| 小队的更替 | 从 `/api/state` 的 `teams` 中消失的小队，会当场连同其容器一起清理掉 |
| 在同一文件夹重新 start 时 | slug 不会变，所以通过 `mission.startedAt` 的变化来丢弃上一次任务的日志 |
| 通信中断时 | 保持中断前的显示，右上角的连接指示灯变红 |
| 谱系树的布局 | 父级放在它所生成的那些子级的纵向正中。指挥塔位于整棵树的上下正中。只有树能放进画面时才纵向居中，放不下时靠上排列 |
| 布局的实现方式 | 只用 `.agent` 的 `top` 来移动。不做 DOM 的重排（因为移动元素会让其下的 CSS 动画回到 0）。也不使用 `transform`（会与诞生动画冲突） |

---

## 9. 文件构成与环境

本目录的内容。路径全部在执行时解析，所以放在哪里都能运行。

```text
├─ dash.cmd / dash    启动器（Windows / POSIX）
├─ dash.py            统一入口点
├─ server.py          分发服务器（全部项目共用，只启动一个）
├─ update_state.py    状态更新 CLI（对状态的唯一写入口）
├─ install.py         初始设置（向各 AI 工具写入运行规则）
├─ dashlib.py         公共逻辑（路径解析、项目识别、合并、格式化）
├─ build_vsix.py      组装并安装 VSCode 扩展（dash ext ...）
├─ test_tabs.py       标签页列表的动作确认。用 python test_tabs.py 运行（不碰记录）
├─ README.zh.md       安装步骤（面向人类）
├─ OPERATION.zh.md    本手册
├─ EXTENSION_PLAN.md  扩展化的计划与设计（确认了什么、决定了什么）
├─ extension/         VSCode 扩展的源码。无构建工序、无 node_modules
│  ├─ package.json    扩展清单（版本、命令、设置以这里为准）
│  ├─ extension.js    扩展本体（纯 CommonJS）
│  ├─ test_extension.js  动作确认。用 node extension/test_extension.js 运行
│  └─ media/          图标及其生成脚本
├─ public/
│  ├─ index.html      面板画面
│  └─ manual.html     使用说明书（面向人类，经由服务器阅读）
├─ dist/              组装好的 .vsix。可以重新生成，所以不做版本追踪
├─ missions/          按项目分开的记录（保存位置可用环境变量更改）
│  └─ .current        当前要显示在画面上的小队的 slug。只有 start 和 demo 会改写它
└─ trash/             已删除项目的存放处。想还原时把文件夹移回 missions/
```

### 环境变量

| 变量 | 效果 |
| --- | --- |
| `AGENT_DASHBOARD_HOME` | 更改记录的保存位置（默认是本目录。若不可写则使用操作系统的用户数据区域） |
| `AGENT_DASHBOARD_PROJECT` | 固定目标项目（`--project` 优先级更高） |
| `PORT` | 服务器的默认端口（`--port` 优先级更高） |
| `CLAUDE_CONFIG_DIR` | Claude Code 的 `CLAUDE.md` 的位置 |
| `CODEX_HOME` | Codex CLI 的 `AGENTS.md` 的位置 |
| `GEMINI_CLI_HOME` | Gemini CLI 的 `GEMINI.md` 的位置 |
| `COPILOT_HOME` | GitHub Copilot CLI 的规则文件的位置 |
| `OPENCODE_CONFIG_DIR` | opencode 的 `AGENTS.md` 的位置 |
| `AGENT_DASHBOARD_AGENTS_FILE` | 自定义添加的工具列表的保存位置（默认为 `agents.json`，与任务记录在同一位置） |
| `AGENT_DASHBOARD_LANG` | 输出的语言（`en` / `ja` / `zh` / `ko`）。优先于已保存的设置 |

### 显示语言

命令的输出支持 `en` / `ja` / `zh` / `ko`。判定顺序自上而下，最先确定的那个会被采用。

1. 环境变量 `AGENT_DASHBOARD_LANG`
2. 用 `dash lang <代码>` 保存的设置（`<记录的保存位置>/lang`）
3. 环境变量 `LC_ALL` / `LC_MESSAGES` / `LANG`
4. 操作系统的显示语言
5. 英语

```bash
python <工具所在位置>/update_state.py lang        # 查看当前语言和判定方式
python <工具所在位置>/update_state.py lang en     # 更改
```

画面（面板与使用说明书）的语言在**浏览器一侧**另行决定（右上角的选择器。首次会
根据浏览器的语言设置自动判定，此后保存在该浏览器中）。**改变命令的语言不会改变画面。**
反之亦然。

没有译文的句子会以英语原文显示。不会因为翻译缺失而导致命令中断。

### API

| 端点 | 返回的内容 |
| --- | --- |
| `/api/state` | 要显示的小队的状态（`teams`）＋ 标签页列表（`tabs`） |
| `/api/run?slug=<slug>&runId=<runId>` | 过去某一次的完整状态。省略 `runId` 则是进行中的那次。因为 slug 中会含有日文，所以用查询参数接收 |
| `/api/projects` | 仅项目列表 |
| `/api/env` | 运行环境（各种路径、平台、Python 版本） |
| `POST /api/project/delete` | 删除项目的记录（默认是移入 `trash/`）。正文 `{"slug": "..."}`，必须带 `Content-Type: application/json` |
| `POST /api/history/delete` | 把任务的记录移入 `trash/`。正文 `{"runs": [{"slug","runId"}, ...]}` 或 `{"phase": "done"}`。给 `runId` 传 `null` 时对象是当前任务（`state.json`），但能删除的只有已完成的 |

`tabs` 每秒都会被取一次，所以做成了**轻量的列表**（`runId` / `title` / `phase` /
`startedAt` / `finishedAt` / `projectName` / `agentCount` / `doneCount` / `isCurrent`）。
它不会去打开孙代理的自报文件。需要内容的只有被选中的那一个，那部分用 `/api/run` 取。

还从未 `start` 过的空壳（`runId` 为 `null` 且 `phase` 为 `standby` 且单元数为 0），
在还有其他可返回的标签页时不会包含进 `tabs`。**这时也要把同一个项目从 `teams` 中剔除。**
画面一侧每秒都会同时做「清理掉 `tabs` 中没有的小队」和「创建 `teams` 中有的小队」两件事，
所以这里一旦不一致，就会无休止地反复创建又销毁同一支小队的容器。

`doneCount` 这个数字只是为了把忘记收尾（`phase` 仍为 `running` 却全部单元 `done`）
显示在标签页上，**数不出来时返回的是 `null` 而不是 `0`**。过去的记录（`history/`）也始终是 `null`。
因为 `0` 会带有「一个都没回来」的含义，如果无法与「不知道」区分开，
画面就会说谎。

只要 `runs` 中混入了一个不合法的 `slug` / `runId`，就**什么都不删除并返回 400**。
混入 `..` 不是「部分失败」而是请求本身的错误，所以不会删到一半、
留下半吊子的状态。仅仅是不存在的记录则放进 `failed`，其余的继续处理。

破坏性操作只接受 `POST`。对 `/api/project/*` 的 `GET` 返回 `405` 和 `Allow: POST`
（这是为了不让浏览器的预读或误点的链接删掉记录）。

### 依赖关系

完全没有使用外部库。`http.server` / `json` / `pathlib` / `argparse` /
`datetime` / `hashlib` / `unicodedata` / `webbrowser` 等，全都是 Python 标准库。
`public/` 下的 HTML 也是零 CDN 引用、自包含的。可在 Python 3.9 以上、Windows / macOS / Linux 上运行。

VSCode 扩展也是同样的方针。`.vsix` 的组装只用 `zipfile` 就完成了，所以既不需要 npm
也不需要 `vsce`，扩展本体也只用 `require('vscode')` 和 Node 内置模块（没有 `node_modules`）。
唯一的例外是 `extension/media/make_icons.py`，只有重画图案时才需要 Pillow。
它与运行无关，所以上面的「零外部库」并没有被打破。
