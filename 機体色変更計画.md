# 機体アイコンのボディ色をモデル別に塗り分ける

## Context

いまダッシュボードに並ぶ機体は、どのモデルで動いていても**全員が白ボディ**です。実装を確認したところ、機体ごとの色分けは一切ありません（`hue-rotate` / `--tint` / `--accent` すべて未使用）。モデル名はカードの `.b-model` バッジを読まないと分からず、系統樹を俯瞰したときに「どこに重い機体を置いたか」が一目で掴めません。

そこで**卵型のボディと腕の色だけ**をモデルごとに塗り分け、俯瞰した瞬間に編成が読めるようにします。目・バイザー・アンテナ・胸ライト・口・状態アニメーション（待機／稼働／報告待ち／完了）は**一切変更しません**——それらは「状態」を表す言語で、同じ場所に「モデル」という第2の意味を足すと2つが競合するためです。ボディはいま状態によって外周のグロー（シアン／琥珀／緑）しか変わらないので、**色という空き地が残っている**のがここです。

判定できないモデルは白（既定色）に落とします。これはこのプロジェクトが全体で貫いている「**モデル名を推測・補完しない**」方針（`update_state.py:1741-1746` のコメント）と同じ判断です。オレンジに見えるのに実は Gemini、という嘘をつくより、白で「分からない」と分かる方が正しい。

---

## 配色プレビュー

**動くプレビューを同じフォルダに置いてあります。ブラウザで開いて確認してください:**

[機体色変更計画.html](機体色変更計画.html)

この HTML は `public/index.html` の SVG・CSS・アニメーションを**実物のまま**貼っており、新規なのは下で説明するグラデーション6本と `.agent.mdl-*` の6行だけです。次のことが確かめられます。

- **4色 × 4状態（待機／稼働／報告待ち／完了）** を動いた状態で見比べる
- **ボタン1つで「現状＝全員白」と切り替え**て、変更の前後を比較する
- **濃さを4段階**（淡い／中／濃い／とても濃い）で切り替えて比較する（既定は③濃い）
- **大きさスライダー**で実寸 94px と拡大を行き来し、その濃さで見分けが付くか判断する
- **モデル名を入力**して、どの色になるかをその場で試す（実データ 22 種の判定結果も一覧）

以下は静止画の抜粋です。上段が拡大、下段が**画面に出る実寸（`--robot-w: 94px`）**です。

<svg width="530" height="400" viewBox="0 0 530 400" xmlns="http://www.w3.org/2000/svg">
  <defs>
    <linearGradient id="pvEggW" x1="0.12" y1="0" x2="0.85" y2="1">
      <stop offset="0" stop-color="#ffffff"/><stop offset="0.5" stop-color="#f4f8fc"/><stop offset="1" stop-color="#d4dfec"/>
    </linearGradient>
    <linearGradient id="pvLimbW" x1="0" y1="0" x2="0" y2="1">
      <stop offset="0" stop-color="#ffffff"/><stop offset="1" stop-color="#dbe5f1"/>
    </linearGradient>
    <linearGradient id="pvEggR" x1="0.12" y1="0" x2="0.85" y2="1">
      <stop offset="0" stop-color="#fff2f4"/><stop offset="0.5" stop-color="#ffbcc6"/><stop offset="1" stop-color="#d9697d"/>
    </linearGradient>
    <linearGradient id="pvLimbR" x1="0" y1="0" x2="0" y2="1">
      <stop offset="0" stop-color="#fff2f4"/><stop offset="1" stop-color="#f4a0ae"/>
    </linearGradient>
    <linearGradient id="pvEggO" x1="0.12" y1="0" x2="0.85" y2="1">
      <stop offset="0" stop-color="#fff6ec"/><stop offset="0.5" stop-color="#ffd39b"/><stop offset="1" stop-color="#dd9440"/>
    </linearGradient>
    <linearGradient id="pvLimbO" x1="0" y1="0" x2="0" y2="1">
      <stop offset="0" stop-color="#fff6ec"/><stop offset="1" stop-color="#f2b76c"/>
    </linearGradient>
    <linearGradient id="pvEggY" x1="0.12" y1="0" x2="0.85" y2="1">
      <stop offset="0" stop-color="#f4fcec"/><stop offset="0.5" stop-color="#c6e79b"/><stop offset="1" stop-color="#7cae4c"/>
    </linearGradient>
    <linearGradient id="pvLimbY" x1="0" y1="0" x2="0" y2="1">
      <stop offset="0" stop-color="#f4fcec"/><stop offset="1" stop-color="#a9d178"/>
    </linearGradient>

    <linearGradient id="pvVisor" x1="0" y1="0" x2="0" y2="1">
      <stop offset="0" stop-color="#0e1724"/><stop offset="1" stop-color="#1b2739"/>
    </linearGradient>
    <radialGradient id="pvEye" cx="0.4" cy="0.34" r="0.72">
      <stop offset="0" stop-color="#ffffff"/><stop offset="0.4" stop-color="#8af2ff"/><stop offset="1" stop-color="#24cdf0"/>
    </radialGradient>
    <radialGradient id="pvBulb" cx="0.36" cy="0.3" r="0.75">
      <stop offset="0" stop-color="#ffd8e7"/><stop offset="0.45" stop-color="#ff8ab5"/><stop offset="1" stop-color="#ef4b87"/>
    </radialGradient>
    <radialGradient id="pvShade" cx="0.5" cy="0.5" r="0.5">
      <stop offset="0" stop-color="#9fb2c8" stop-opacity="0.38"/><stop offset="1" stop-color="#9fb2c8" stop-opacity="0"/>
    </radialGradient>
    <clipPath id="pvEggClip"><path d="M14 106 A46 78 0 0 1 106 106 A46 48 0 0 1 14 106 Z"/></clipPath>

    <g id="pvAnt">
      <path fill="none" stroke="#c9d4e2" stroke-width="2.8" stroke-linecap="round" d="M52 36 L43 17"/>
      <circle cx="43" cy="16" r="5" fill="url(#pvBulb)"/>
    </g>
    <g id="pvFace">
      <g clip-path="url(#pvEggClip)"><ellipse cx="88" cy="130" rx="34" ry="32" fill="url(#pvShade)"/></g>
      <circle cx="60" cy="126" r="6.4" fill="#ccd8e6"/>
      <circle cx="60" cy="126" r="4.4" fill="#b9c6d6"/>
      <rect x="30" y="53" width="60" height="36" rx="18" fill="#c9d5e3"/>
      <rect x="33" y="56" width="54" height="30" rx="15" fill="url(#pvVisor)"/>
      <circle cx="46" cy="71" r="6" fill="url(#pvEye)"/>
      <circle cx="74" cy="71" r="6" fill="url(#pvEye)"/>
      <rect x="36" y="58" width="48" height="9" rx="4.5" fill="#ffffff" opacity="0.07"/>
      <rect x="52" y="92" width="16" height="7" rx="3.5" fill="#ff7fae"/>
    </g>
  </defs>

  <rect width="530" height="400" rx="10" fill="#0d131c"/>

  <!-- 上段：拡大 -->
  <g transform="translate(8,10)">
    <use href="#pvAnt"/>
    <circle cx="21" cy="112" r="13" fill="url(#pvLimbR)"/><circle cx="99" cy="112" r="13" fill="url(#pvLimbR)"/>
    <path d="M14 106 A46 78 0 0 1 106 106 A46 48 0 0 1 14 106 Z" fill="url(#pvEggR)"/>
    <use href="#pvFace"/>
  </g>
  <g transform="translate(138,10)">
    <use href="#pvAnt"/>
    <circle cx="21" cy="112" r="13" fill="url(#pvLimbO)"/><circle cx="99" cy="112" r="13" fill="url(#pvLimbO)"/>
    <path d="M14 106 A46 78 0 0 1 106 106 A46 48 0 0 1 14 106 Z" fill="url(#pvEggO)"/>
    <use href="#pvFace"/>
  </g>
  <g transform="translate(268,10)">
    <use href="#pvAnt"/>
    <circle cx="21" cy="112" r="13" fill="url(#pvLimbW)"/><circle cx="99" cy="112" r="13" fill="url(#pvLimbW)"/>
    <path d="M14 106 A46 78 0 0 1 106 106 A46 48 0 0 1 14 106 Z" fill="url(#pvEggW)"/>
    <use href="#pvFace"/>
  </g>
  <g transform="translate(398,10)">
    <use href="#pvAnt"/>
    <circle cx="21" cy="112" r="13" fill="url(#pvLimbY)"/><circle cx="99" cy="112" r="13" fill="url(#pvLimbY)"/>
    <path d="M14 106 A46 78 0 0 1 106 106 A46 48 0 0 1 14 106 Z" fill="url(#pvEggY)"/>
    <use href="#pvFace"/>
  </g>

  <g font-family="Segoe UI,Meiryo,sans-serif" text-anchor="middle">
    <text x="68"  y="199" font-size="14" fill="#e6eef8">赤</text>
    <text x="198" y="199" font-size="14" fill="#e6eef8">オレンジ</text>
    <text x="328" y="199" font-size="14" fill="#e6eef8">白（既存色）</text>
    <text x="458" y="199" font-size="14" fill="#e6eef8">薄緑</text>
    <text x="68"  y="216" font-size="11" fill="#8ea3bb">Fable</text>
    <text x="198" y="216" font-size="11" fill="#8ea3bb">Opus / Sol</text>
    <text x="328" y="216" font-size="11" fill="#8ea3bb">Sonnet / Terra / Gemini / 不明</text>
    <text x="458" y="216" font-size="11" fill="#8ea3bb">Haiku / Luna</text>
  </g>

  <line x1="16" y1="232" x2="514" y2="232" stroke="#22303f" stroke-width="1"/>

  <!-- 下段：実寸 94px -->
  <g transform="translate(21,246) scale(0.7833)">
    <use href="#pvAnt"/>
    <circle cx="21" cy="112" r="13" fill="url(#pvLimbR)"/><circle cx="99" cy="112" r="13" fill="url(#pvLimbR)"/>
    <path d="M14 106 A46 78 0 0 1 106 106 A46 48 0 0 1 14 106 Z" fill="url(#pvEggR)"/>
    <use href="#pvFace"/>
  </g>
  <g transform="translate(151,246) scale(0.7833)">
    <use href="#pvAnt"/>
    <circle cx="21" cy="112" r="13" fill="url(#pvLimbO)"/><circle cx="99" cy="112" r="13" fill="url(#pvLimbO)"/>
    <path d="M14 106 A46 78 0 0 1 106 106 A46 48 0 0 1 14 106 Z" fill="url(#pvEggO)"/>
    <use href="#pvFace"/>
  </g>
  <g transform="translate(281,246) scale(0.7833)">
    <use href="#pvAnt"/>
    <circle cx="21" cy="112" r="13" fill="url(#pvLimbW)"/><circle cx="99" cy="112" r="13" fill="url(#pvLimbW)"/>
    <path d="M14 106 A46 78 0 0 1 106 106 A46 48 0 0 1 14 106 Z" fill="url(#pvEggW)"/>
    <use href="#pvFace"/>
  </g>
  <g transform="translate(411,246) scale(0.7833)">
    <use href="#pvAnt"/>
    <circle cx="21" cy="112" r="13" fill="url(#pvLimbY)"/><circle cx="99" cy="112" r="13" fill="url(#pvLimbY)"/>
    <path d="M14 106 A46 78 0 0 1 106 106 A46 48 0 0 1 14 106 Z" fill="url(#pvEggY)"/>
    <use href="#pvFace"/>
  </g>
  <text x="265" y="386" text-anchor="middle" font-family="Segoe UI,Meiryo,sans-serif" font-size="11" fill="#66798f">↑ 実寸（--robot-w: 94px）。画面にはこの大きさで並びます</text>
</svg>

### 追加するグラデーションの値

既存の白（`#ffffff → #f4f8fc → #d4dfec`）と**同じ3ストップ構造**のまま、色相と濃さを振っています。構造を揃えてあるので、白機体と並べても同じ材質に見え、状態グロー（シアン／琥珀／緑）とも喧嘩しません。

採用するのは **③濃い**（プレビューの既定）です。実寸 94px でも遠目に判別でき、白機体が「色の抜けた機体」に見えるほどではない位置です。

| トーン | 卵 stop 0 / 0.5 / 1 | 腕 stop 0 / 1 |
| --- | --- | --- |
| 白（既存・変更なし） | `#ffffff` / `#f4f8fc` / `#d4dfec` | `#ffffff` / `#dbe5f1` |
| 赤 | `#fff2f4` / `#ffbcc6` / `#d9697d` | `#fff2f4` / `#f4a0ae` |
| オレンジ | `#fff6ec` / `#ffd39b` / `#dd9440` | `#fff6ec` / `#f2b76c` |
| 薄緑 | `#f4fcec` / `#c6e79b` / `#7cae4c` | `#f4fcec` / `#a9d178` |

**Haiku / Luna は黄ではなく薄緑にします。** 淡い塗りでは黄とオレンジの色相が近すぎて、実寸 94px で見分けが付かなかったためです。緑を選ぶにあたっては色相 95°（黄緑寄り）に置き、**完了状態のミントグリーン（`#5fe6a4`、色相 150°）とは離して**あります。完了した薄緑の機体は「黄緑のボディ＋ミントの外周グロー」になるので、状態の読み取りは潰れません（プレビューの薄緑 × 完了のマスで確認できます）。

プレビューで別の段階を選んだ場合は、その段階の値が画面の「追加するグラデーションの値」に出るので、それを採用してください。実装時に差し替えるのは `stop-color` の12個だけで、構造とセレクタは変わりません。

---

## なぜこの実装なのか（制約）

`#rb-defs` は**ページに1つだけ**置いて全機体が `url(#rbEggGrad)` で共有しています（`public/index.html:1058` のコメント。各ロボに複製すると id が重複するため）。

このため **`stop-color: var(--rb-body)` は使えません**。グラデーションの計算は参照元ではなく `#rb-defs` 内の自分の位置で行われるので、機体ごとに違う変数値を渡せないからです。機体ごとに色を変える手は実質2つで、

- **単色 fill に落とす** → 立体感が消えるので却下
- **色ごとにグラデーションを増やし、CSS で参照先を切り替える** ← これを採る

後者は既存の書き方（defs はすべて 16 進直書き）とそのまま揃います。

---

## 変更箇所

編集対象は **`Subagent Dashboard\Subagent Dashboard\public\index.html` 1ファイルのみ**です。

### 1. `#rb-defs` にグラデーションを6本追加（`public/index.html:1063-1072` の直後）

`rbEggGradRed` / `rbEggGradOrange` / `rbEggGradGreen` と `rbLimbGradRed` / `rbLimbGradOrange` / `rbLimbGradGreen`。座標（`x1/y1/x2/y2`）は既存の `rbEggGrad` / `rbLimbGrad` と同一にし、`stop-color` だけ上表の値にする。白は既存の2本をそのまま使うので追加しない。

### 2. CSS に参照先の切り替えを追加（`public/index.html:477` の直後）

```css
/* --- モデル別のボディ色。塗り以外（目・バイザー・胸ライト・口・状態アニメ）は変えない。
   グラデは #rb-defs に1組しか置けない（id が重複するため）ので、色ごとに定義を増やして
   参照先を差し替える。クラスが付かない機体は既定の白のまま。 */
.agent.mdl-red    .rb-egg { fill: url(#rbEggGradRed); }
.agent.mdl-red    .rb-arm { fill: url(#rbLimbGradRed); }
.agent.mdl-orange .rb-egg { fill: url(#rbEggGradOrange); }
.agent.mdl-orange .rb-arm { fill: url(#rbLimbGradOrange); }
.agent.mdl-green  .rb-egg { fill: url(#rbEggGradGreen); }
.agent.mdl-green  .rb-arm { fill: url(#rbLimbGradGreen); }
```

`.agent.running .rb-egg` 等（`:538 / :550 / :560`）は `filter` しか触っていないので**衝突しません**。状態のグローはそのまま乗ります。

### 3. モデル名→トーンの判定関数を追加（`robotSvg()` の手前、`public/index.html:2684` あたり）

`livefeed.py:169-181` の `model_key()` と同じ考え方（小文字化して部分一致、知らない表記は空）をフロント側に置きます。**サーバーはモデル名を生のまま渡す設計**（`dashlib.py:2024-2039` のコメント「訳すのは表示の直前＝index.html 側の仕事」）なので、判定はフロントに置くのが既存方針どおりです。

```js
/** モデル名からボディ色のトーンを決める。'' は既定の白。
 *
 *  記録側は "claude-opus-5"、実機の meta.json は "opus" と、同じモデルが別表記で入る
 *  （livefeed.model_key と同じ事情）。そのため部分一致で拾う。
 *  **知らない表記は必ず '' に落とす。** 推測して別の色で塗ると、画面が黙って嘘をつく。
 *  Gemini を先に弾いているのは、全モデル白と決まっているため。将来 "sol" を含む
 *  Gemini のモデル名が出ても、下の表に吸われないようにする。 */
const MODEL_TONES = [
  [/fable/,                     'red'],
  [/opus/,                      'orange'],
  [/haiku/,                     'green'],
  [/sonnet/,                    ''],
  [/(^|[^a-z])sol([^a-z]|$)/,   'orange'],
  [/(^|[^a-z])luna([^a-z]|$)/,  'green'],
  [/(^|[^a-z])terra([^a-z]|$)/, ''],
];

function modelTone(model) {
  const low = String(model || '').toLowerCase();
  if (!low || low.includes('gemini')) return '';
  for (const [re, tone] of MODEL_TONES) if (re.test(low)) return tone;
  return '';
}
```

### 4. カードにクラスを付ける（`updateAgent`、`public/index.html:2905` のモデル表示の隣）

`buildAgentDom` ではなく `updateAgent` に置きます。hook 登録の機体は起動時に `subagent_type` しか入らず、完了時に `resolvedModel` で書き換わる（`update_state.py:1041-1044`）ため、**モデルは後から変わる**からです。系統樹（`:3153-3154`）と下部区画（`:4490-4500`）はどちらも生成直後に `updateAgent` を通るので、この1箇所で両方に効きます。

```js
  const tone = modelTone(a.model);
  if (entry.tone !== tone) {
    entry.root.classList.remove('mdl-red', 'mdl-orange', 'mdl-green');
    if (tone) entry.root.classList.add('mdl-' + tone);
    entry.tone = tone;
  }
```

あわせて `buildAgentDom` の `entry` に `tone: null` を足す（`:2871` の `status: null` の並び）。`null` から始めるので、初回の `updateAgent` で必ず1度は適用されます。

---

## 判定結果の検算（実データ 22 種すべて）

記録に実在する `model` 値を上のロジックに通した結果です。

| 値（出現数） | 判定 | |
| --- | --- | --- |
| `claude-opus`(660) / `claude-opus-5`(210) / `claude-opus-5[1m]`(16) / `claude-opus-4-8`(2) / `opus`(1) | オレンジ | ✅ |
| `claude-sonnet-5`(312) / `claude-sonnet-4-5`(3) / `sonnet`(3) / `sonnet-5`(2) | 白 | ✅ |
| `claude-haiku-4-5-20251001`(131) / `claude-haiku-4-5`(37) / `haiku`(2) | 薄緑 | ✅ |
| `gpt-5.6-sol`(35) | オレンジ | ✅ |
| `gpt-5.6-luna`(30) | 薄緑 | ✅ |
| `gpt-5.6-terra`(30) | 白 | ✅ |
| `gemini-3.1-pro-high`(4) / `gemini-3.6-flash-medium`(2) / `Gemini 3.1 Pro`(1) | 白 | ✅ Gemini 全モデル白 |
| `gpt-5.1-codex` / `gpt-5-codex` / `gpt-5.1-codex-mini` / `antigravity` / `claude5` / `""` | 白 | ✅ 検知できない＝白 |
| `不明`(5) | 白 | ✅ `dashlib._model_value` が `""` に寄せる |

`fable` を含む値は記録にまだ存在しませんが、`claude-fable-5-1` / `fable` のどちらでも赤になります。

hook が `model` の代わりに書く `subagent_type`（`Explore` / `general-purpose` / `claude-code-guide` / `Plan` / `statusline-setup`）はいずれもキーワードを含まないため**すべて白**です。これは「実際のモデルが記録されていない＝検知できない」状態の正しい表示です。

---

## 触らないもの

- **`extension/media/robot-standby.svg` / `activity-bar.svg`**（各5箇所）— VSCode のアイコン用の静止画で、特定のモデルを表さないため白のまま
- **`public/manual.html`**（機体4体）— 状態（待機／稼働／報告待ち／完了）の見本であって、モデルの見本ではない。白のままで意味は正しい
- **worktree**（`-bundle` / `-mermaid` / `-livefeed`）— 別ブランチの実体。main をマージしたときに入る
- **`~/.claude/agent-dashboard`（配置版）** — 拡張機能が配置する側。ここを直接編集すると次の配置で消える

---

## 検証

1. **リポジトリのサーバーを直接起動して見る**（配置版を汚さずに確認できる）
   ```
   python "C:\PythonFiles\拡張機能開発\Subagent Dashboard\Subagent Dashboard\server.py"
   ```
   `GET /api/env` の `toolRoot` がリポジトリ側を指していることを確認してから画面を開く（写しが4つあるので、どれを見ているか毎回確かめる）。
2. **デモ記録で全色を一度に出す**。`update_state.py` のデモは `claude-opus-5` / `claude-sonnet-5` / `claude-haiku-4-5` の3種しか使わない（`:1364-1475`）ので、`add --model` で `claude-fable-5-1` / `gpt-5.6-sol` / `gpt-5.6-luna` / `gpt-5.6-terra` / `gemini-3.1-pro-high` / `antigravity` を足した確認用ミッションを `--project` を分けて作る。
3. 待機／稼働／報告待ち／完了の4状態を切り替え、**状態のグローと表情が今までどおり出ること**を確認する（色の変更が状態表示を潰していないこと）。
4. 過去のミッションのタブを開き、`不明` や空文字の機体が**白**で出ることを確認する。

### 書き戻しの注意

`public/index.html` は **LF** です。Python の `Path.write_text()` で書き戻すと全行が CRLF になり、触っていない行まで差分になります（過去に実際に踏んでいます）。Edit ツールで直接編集するか、`write_bytes()` を使ってください。
