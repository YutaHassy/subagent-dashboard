# 記録の奪い合いを止める——同じディレクトリで2つのセッションが走るとき

## Context

2026-09-03 に実際に起きました。同じフォルダで Claude Code のセッションが2つ動き、**あとから始めたほうが、先に始めたほうの記録を奪いました。**

| 時刻 | 何が起きたか | いまの道具の反応 |
| --- | --- | --- |
| 21:18:00 | 甲が `start` | 記録が作られる |
| — | 甲が `add SCOUT-A/B/C` → `done SCOUT-A` → `done SCOUT-B` | 正常 |
| **21:27:16** | **乙が `start`** | **甲の記録が「稼働中」のまま `history/` へ押し出された。あとから完了にする手段は無い** |
| — | **甲が `add PLAN-A`** | **成功した。乙の記録に甲の機体が混ざった** |
| — | 甲が `done SCOUT-C` | `die()` → 終了コード 1 で失敗 |

**いちばん悪いのは4行目です。** 黙って他人の記録を汚し、汚したことが誰にも見えません。3行目もほとんど同じくらい悪い——取り返しがつかないからです。5行目は、失敗として観測できるので、まだ良いほうです。

いま運用文書は「同じディレクトリで並行するときは**事前に** `--project` で分けよ」と書いています。しかし**先に始めた甲は、9分後に乙が始まることを予測できません。** 予測できないことを予測しろという指示になっており、これは道具の側で守るべき問題です。

**この事故の材料は、すでにこのリポジトリの中にあります。** `mission.sessionId`（`CLAUDE_CODE_SESSION_ID` 由来）は `start` のたびに書かれています（`update_state.py:264-266`）。ただし今日のコミット `bf1bf3d` で `autofinish` の持ち主照合のためだけに入ったもので、**`start` / `add` / `done` / `finish` / `log` は誰もこれを見ていません。** 本計画は新しい仕掛けを作るのではなく、**すでに打たれた対策の横展開**です。

編集対象は **`update_state.py` / `dashlib.py` / `i18n_data_update.py` / `public/index.html` / `public/i18n.js` / `check_autofinish.py`**、それに文書 10 ファイル（`install.py` + `i18n_data_install.py`、`OPERATION.*.md` 4本、`README*.md` 4本）です。

---

## 1. 背景と現状

### 1-1. なぜ衝突するのか（設計としてそうなっている）

記録先の名前はカレントディレクトリのフルパスだけで決まります（`dashlib.py:896-906`）。

```python
def slug_for_path(path: Path) -> str:
    p = path.resolve()
    base = sanitize_name(p.name or p.drive.replace(":", "") or "root")
    key = str(p).lower() if CASE_INSENSITIVE_FS else str(p)
    digest = hashlib.sha1(key.encode("utf-8")).hexdigest()[:6]
    return f"{base}-{digest}"
```

**セッションもプロセスも混ざりません。** 同じフォルダ＝同じ `missions/<slug>/` に必ず当たります。これは「そのフォルダの作業は1つの記録に集まる」という設計意図の裏返しであり、欠陥ではありません。欠陥なのは、**当たったときに何も起きないこと**です。

### 1-2. 事故3——`start` は稼働中かどうかも持ち主も見ない

`cmd_start()`（`update_state.py:245`）は、退避の前に前の記録を**読むだけ**です。

```python
# update_state.py:254-257
previous = read_state(project["slug"], project, required=False)
prev_mission = previous.get("mission", {})
prev_running = prev_mission.get("phase") == "running"
prev_pending = unfinished_mission(previous)
...
# update_state.py:285-287
archived = dashlib.archive_current_run(project["slug"])   # ← 無条件に退避
dashlib.write_state(project["slug"], state)               # ← 新しい state で上書き
```

`archive_current_run()`（`dashlib.py:1568`）が退避を**しない**条件は3つだけで、そのどれも「稼働中か」「持ち主が誰か」を見ていません（履歴を残さない設定・初回の start・未開始の器）。

退避の実体は `_move_current_to_history()`（`dashlib.py:1491`）で、`state.json` を `shutil.move` するだけです。**`mission.phase` を書き換える処理はどこにもありません。** したがって稼働中のまま退避された記録は、以後永久に `"running"` のまま固定されます。

`prev_running` / `prev_pending` は使われていますが、行き先は**印刷だけ**です（`update_state.py:320-344`）。しかもこの警告は**押し出した側（乙）の端末にしか出ません。** 押し出された甲は、この時点では自分の記録が消えたことを知る手段を持ちません。

### 1-3. 事故4——`add` / `log` / `finish` に持ち主の照合が無い

- **`add`**（`update_state.py:383`）: 親IDが無ければ警告のみ、同じIDがあれば「上書きします」と言って進む。**「誰が呼んだか」の検証は1行もありません。** 乙の記録に甲の機体が入るのはここです。
- **`log`**（`update_state.py:1117`）: `read_state` → `push_log` → `write_state` の3行。同じ穴です。
- **`finish`**（`update_state.py:595`）: 未完了の機体が残っていても警告だけで締めます。持ち主は見ません。**他人のミッションを締められる**——これは `autofinish` が既に塞いだ穴と同じ形です（`running_missions_here`, `update_state.py:705-755`）。
- **`done`**（`update_state.py:503`）: 対象の機体が現在のミッションに居なければ `die()` → **終了コード 1** で失敗します。ここは既に守られていますが、**IDが両方のミッションで重なっていれば（`SCOUT-A` は毎回使い回される）、他人の機体を完了にしてしまいます。**

`locks/`（`dashlib.py:1959`）は「1プロセスが読んで直して書く」間の直列化だけで、持ち主を守るものではありません。今回の事故は9分離れた逐次操作なので、ロックは最初から無関係です。

### 1-4. 材料はある——`mission.sessionId` は既に書かれている

```python
# update_state.py:696-702
#: いま動いている Claude Code のセッション。**ミッションの持ち主を決めるのはこれだけ。**
#: hook もこの CLI も、同じ実行ファイルの子プロセスとして起動されるので同じ値が渡る。
ENV_SESSION = "CLAUDE_CODE_SESSION_ID"

def current_session() -> str:
    return (os.environ.get(ENV_SESSION) or "").strip()
```

`cmd_start` は毎回これを `mission.sessionId` へ書いています（`update_state.py:261-266`）。読むのは `running_missions_here()` だけです。**本計画は、この読み手を4つ増やすだけです。**

### 1-5. サブエージェントのセッションID（**実測。この設計の成否を分ける点**）

親が `start` した記録に、サブエージェントが `add` できなくなったら、**道具は根本から壊れます。** 運用ルールは「サブエージェントを起動した直後に、1体ずつ `add`」と書いており、その `add` を打つのは司令塔（＝親）ですが、孫を起動する班や、司令塔から作業を委ねられた機体が打つこともあります。したがって「サブエージェントの `CLAUDE_CODE_SESSION_ID` が親と同じか」は、この計画の前提そのものです。

**実測しました。同じでした。**

| 測ったもの | 値 |
| --- | --- |
| 本計画を書いているサブエージェントのシェル `env \| grep -i claude` | `CLAUDE_CODE_SESSION_ID=0fb816ec-833d-4da2-9290-0db5570290db`（併せて `CLAUDE_CODE_CHILD_SESSION=1`） |
| 別のサブエージェント（SCOUT-D）が同じ手順で測った値（調査レポート B.7） | `0fb816ec-833d-4da2-9290-0db5570290db` |
| **親（司令塔）が `start` で書いた** `missions/拡大率と区画の改修/state.json` の `mission.sessionId` | `0fb816ec-833d-4da2-9290-0db5570290db` |

3つとも一致します。3つめが決定的です——これは**親のプロセスが書いた値**であり、サブエージェントの自己申告ではありません。したがって:

> **サブエージェントは親の `CLAUDE_CODE_SESSION_ID` をそのまま受け継ぐ。**
> 親が `start` した記録に、サブエージェントは `add` / `done` / `finish` / `log` を打てる。

参考として、同じ `missions/` には `sessionId: 6debbcfa-f32d-4ad8-b45c-6a75a49ddfa4`（21:27:16 開始）の記録も残っています。**これが事故を起こした乙です。** 甲と乙が別々のIDを持っていたことも実測で裏が取れました＝**この事故は、照合を入れれば確実に止まる形をしていました。**

**測れた範囲**: Claude Code 2.1.259 / `CLAUDE_CODE_ENTRYPOINT=claude-vscode` / Windows 11、1回。**hook から呼ばれたときにこの環境変数が渡るかは測れていません**（§8）。

### 1-6. 画面は「未完」と既に言っている（インシデント文の一部訂正）

`public/index.html:2171-2176` は、履歴タブの `phase === 'running'` を「稼働中」ではなく専用の文言で出します。

```js
function phaseText(phase, isHist) {
  if (!isHist) return phaseShort(phase);
  if (phase === 'running') return tr('phase.hist_running');   // ja: 「未完」
  if (phase === 'standby') return tr('phase.hist_standby');
  return tr('phase.hist_done');
}
```

4言語ぶん用意されています（`public/i18n.js:116, 290, 451, 612`）。**画面は完全な嘘はついていません。** 足りないのは「なぜ未完なのか」だけです。ここは §2-4 で埋めます。

### 1-7. 新しい `phase` 値を足してはいけない理由

`phase` の値は3種類に固定されており、**それ以外は3層が別々の既定値へ黙って丸めます。**

| 場所 | 丸め先 |
| --- | --- |
| `dashlib.py:83-84` `PHASES = ("standby", "running", "done")` | — |
| `dashlib.py:1713`（`_summarize_run`）、`dashlib.py:2308`（`_build_state`） | **`"standby"`（未着手）** |
| `server.py:186-187`（`_phase_of`） | **`"standby"`** |
| `public/index.html:1918-1919`（`normalizeTab`） | `runId` があれば **`"done"`（完了）**、無ければ `"standby"` |
| `public/index.html:2365-2367`（`normalizeState`） | **`"done"`** |

**サーバー側は「未着手」へ、クライアント側は「完了」へ丸めます。** 新しい値を片方だけ対応させると、もう片方で正反対の意味に化けます。**新しい `phase` 値は足しません。**

---

## 2. 変更の設計

### 2-0. 採る方針と、採らなかった方針

**採る:**

- **方針1: `add` / `done` / `finish` / `log` に持ち主の照合を入れる**（事故4を根絶）。§2-2。
- **方針2: `start` を保護する**（事故3を根絶）。`--force` で明示的に押し出せる。§2-3。
- **方針3: 押し出された記録に `mission.interruptedBy` を残す**（新しい `phase` 値は足さない）。§2-4。
- **提案A（追加）: 拒否のときに、貼れば通る `--project` 名を自動で作って出す。** §2-1 の `suggest_project()`。「一意な名前を考えてください」と言われて考えられるのは人だけで、しかも**4回とも同じ名前でなければならない**。名前を道具が決めれば、その両方が要らなくなる。
- **提案B（追加・小さい）: hook の書き先を決めるときにも同じ照合を使う。** §2-6。いまは「このフォルダで2本走っていたら書かない」だが、**そのうち1本が自分のものだと分かるなら、書ける。** いまより取りこぼしが減る方向にしか動かない。

**採らない:**

| 案 | 採らない理由 |
| --- | --- |
| **記録先をディレクトリ＋セッションで自動的に分ける** | slug は `.current`（画面が映すチーム）・タブ・`--project` の前方一致・`_hook_target`・`running_missions_here`・`history/` の置き場所、すべての識別子である。セッションごとに分けると、**同じフォルダで Claude Code を開き直すたびに新しいプロジェクトが1つ増える**。履歴は「プロジェクトごとに直近20件」なので、どのプロジェクトも履歴1件になり、「過去の作業もタブで見返せる」という約束が壊れる。`--project` の意味も黙って変わる。**衝突は年に何度も起きないが、開き直しは毎日起きる。** 割に合わない |
| **印（`sessionId`）が無いときに拒否する** | 「この記録が古い」と「この端末に環境変数が来ていない」を**区別する手段が無い**。拒否側に倒すと、hook からの自動登録や人が手で打つ運用が黙って死ぬ。`running_missions_here` が採っている「分からないときは今までどおり」と同じ理由で通す（§2-5） |
| **`phase` に新しい値を足す** | §1-7 の3層の丸め。片方だけ直すと正反対の意味に化ける |
| **押し出された記録に `finishedAt` を書いて完了にする** | **実測でない値を書かない**というこの道具全体の約束を破る。そのミッションが終わった時刻は誰も測っていない。`cmd_start` のコメント（`update_state.py:251-253`）が「見るだけで直さない」と明記している |
| **環境変数で保護を切れるようにする**（`AGENT_DASHBOARD_NO_SESSION_CHECK` 等） | 一度立てたら以後すべてのコマンドで黙って無効になり、コマンド行を見ても分からない。**それは今回の事故と同じ種類の問題**である。切るなら `--force` と書かせる |
| **`reset` / `remove` にも照合を入れる** | 今回は入れない。どちらも人が明示的に打つ破壊的コマンドで、事故の経路ではない。ただし `reset` が他セッションの稼働中の記録を消せることは事実なので、§8 に残す |

### 2-1. 新しい補助（`update_state.py`。`# ---- コマンド` の手前 `:242` へ置く）

**まず `ENV_SESSION` と `current_session()` を `:696-702` から `:242` の手前へ移します。** 中身は1文字も変えません。いまは `autofinish` の節に置かれていますが、これから `start` / `add` / `done` / `finish` / `log` が使うので、コマンド定義より前にある「補助」に属します。**移動なので `t()` 文字列は増減しません。**

コメントは、役割が広がったことに合わせて書き足します。

**現在（`:696-702`）:**

```python
#: いま動いている Claude Code のセッション。**ミッションの持ち主を決めるのはこれだけ。**
#: hook もこの CLI も、同じ実行ファイルの子プロセスとして起動されるので同じ値が渡る。
ENV_SESSION = "CLAUDE_CODE_SESSION_ID"


def current_session() -> str:
    return (os.environ.get(ENV_SESSION) or "").strip()
```

**変更後（`:242` の手前へ移し、下の3つを続けて置く）:**

```python
#: いま動いている Claude Code のセッション。**ミッションの持ち主を決めるのはこれだけ。**
#: hook もこの CLI も、同じ実行ファイルの子プロセスとして起動されるので同じ値が渡る。
#:
#: **サブエージェントは親と同じ値を受け継ぐ。**（実測 2026-09-03: 司令塔が start で
#: 書いた mission.sessionId と、2体のサブエージェントのシェルで測った値が一致した。）
#: ここが別々の値になる環境では、親が start した記録にサブエージェントが add できなく
#: なり、下の照合が道具そのものを壊す。**受け継がれることが、この照合の前提である。**
ENV_SESSION = "CLAUDE_CODE_SESSION_ID"


def current_session() -> str:
    return (os.environ.get(ENV_SESSION) or "").strip()


def other_session_owns(state: dict) -> str:
    """この記録の持ち主が、いま呼んでいる側と**確かに違う**ならその持ち主を返す。
    違うと言い切れないときは空文字＝通す。

    **両方そろっているときだけ拒否する。** 片方でも空なのは
      - 記録が sessionId を持たない（0.9.1 より前に start されたもの）
      - この端末に CLAUDE_CODE_SESSION_ID が渡っていない（hook 未配線、人が手で打つ）
    のどちらかで、この2つを見分ける手段は無い。ここで「分からないから拒否」に倒すと
    **道具が黙って使えなくなる**——running_missions_here が「分からないときは今まで
    どおり」に倒しているのと同じ理由による。

    取りこぼし（両方とも空で、実は別セッション）は残る。**承知の上の穴である。**
    塞ぐには印の無い記録を拒否するしかなく、その代償のほうが重い。
    """
    mission = state.get("mission") if isinstance(state.get("mission"), dict) else {}
    owner = dashlib.as_str(mission.get("sessionId"))
    me = current_session()
    return owner if (owner and me and owner != me) else ""


def suggest_project(project: dict) -> str:
    """衝突したときに勧める、一意な --project 名。取れなければ空文字。

    **同じセッションからは何度呼んでも同じ名前が出る**こと、それがこの関数の全部で
    ある。start / add / done / finish の4回に同じ名前を渡してもらう必要があるので、
    呼ぶたびに違う名前を出したら案内として成立しない。だからセッションIDから作る。

    **区切りに "-" を使わない。** resolve_project の前方一致は
    `s == hint or s.startswith(hint + "-")` なので（dashlib.py:1864）、
    "<ディレクトリ名>-<8桁>" にすると、あとから誰かが `--project <ディレクトリ名>`
    と打ったとき、cwd から決まる "<ディレクトリ名>-<ハッシュ6桁>" と2件に当たって
    「曖昧です」で弾かれる（exact 一致は優先されない。dashlib.py:1864-1877）。
    "@" は sanitize_name（dashlib.py:889-893、禁止は <>:"/\\|?* と制御文字）が通し、
    is_valid_slug（dashlib.py:961-974）も通り、前方一致にも引っかからない。
    """
    me = current_session()
    if not me:
        return ""
    tail = "".join(ch for ch in me if ch.isalnum())[:8]
    base = dashlib.as_str(project.get("name")) or dashlib.as_str(project.get("slug"))
    return (base or "mission") + "@" + tail


def owner_conflict_message(project: dict, state: dict, *, starting: bool) -> str:
    """持ち主が違う記録に書こうとしたときの案内。**次に打つ行まで出す。**

    ここで止められた人は、たいてい「同じフォルダでもう1つ Claude Code を開いた」
    ことにすら気づいていない（気づけない——気づく手段が無いから事故になった）。
    「別のセッションが使っています」とだけ言うと、次に何をすればよいか分からず、
    --force を打つか、道具を使うのをやめるかのどちらかになる。**貼れば通る行を出す。**
    """
    mission = state.get("mission") if isinstance(state.get("mission"), dict) else {}
    title = dashlib.as_str(mission.get("title")) or t("(untitled mission)")
    when = iso_ago(dashlib.as_str(mission.get("startedAt"))) or t("unknown")
    lines = [
        t("another session is using this project's record right now.")
        if starting else
        t("this record belongs to another session that is using it right now."),
        t("      Target: {name}").format(name=project_label(project)),
        t('      The record here is "{title}" (started {when}).')
        .format(title=title, when=when),
        t("      Starting here would push that record into the history while it is\n"
          "      still running, and it could never be marked finished afterwards.\n"
          "      Nothing was started.")
        if starting else
        t("      Writing here would file your work into that mission and mix the\n"
          "      two records together. Nothing was written."),
        t("      Give your work its own record. Put the same --project on all four:"),
        t('        start --project "{name}" --title "<name of the work>" '
          '--model <your own model ID>').format(name=suggest_project(project)),
        t("      To do it anyway, add --force."),
    ]
    return "\n".join(lines)
```

`suggest_project()` がここで空を返すことはありません——**拒否は「両方の sessionId がそろっている」ときにしか起きない**ので、`current_session()` は必ず値を持っています。それでも空を返す道を残してあるのは、将来この関数を別の場所から呼んだときのためです。

### 2-2. 方針1——`add` / `done` / `finish` / `log` の照合（事故4）

4つとも形は同じです。`read_state(..., required=True)` の**直後**に3行を足します。**書き込みの前**でなければ意味がありません。

**現在（`cmd_add` の頭。`update_state.py:383-387`）:**

```python
def cmd_add(args) -> None:
    project = pick_project(args)
    slug = project["slug"]
    state = read_state(slug, project, required=True)

    parent_id = args.parent or COMMAND_ID
```

**変更後:**

```python
def cmd_add(args) -> None:
    project = pick_project(args)
    slug = project["slug"]
    state = read_state(slug, project, required=True)

    # **持ち主が違う記録には書かない。** ここが無かったので、押し出されたことに
    # 気づいていない側の add が成功し、押し出した側のチームに機体が1体混ざった
    # （実測 2026-09-03）。done は「その機体が居ない」で弾かれるが、ID は
    # SCOUT-A のように使い回されるので、たまたま同じ ID が両方に居れば
    # **他人の機体が完了になる**。add と同じ穴が done にもある。
    if not args.force and other_session_owns(state):
        die(owner_conflict_message(project, state, starting=False))

    parent_id = args.parent or COMMAND_ID
```

`cmd_done`（`:503-506`）・`cmd_finish`（`:595-598`）・`cmd_log`（`:1117-1120`）にも、**同じ3行を** `read_state` の直後へ入れます（コメントは1か所＝`cmd_add` に書き、他は「cmd_add と同じ理由」と1行で参照する。このリポジトリの作法です）。

**入れる位置に2つだけ注意があります。**

- `cmd_finish` では、`snapshot_orphans(slug)`（`:600`）より**前**に置くこと。`snapshot_orphans` は `build_state` を通す重い読み取りで、拒否するなら走らせる必要がありません。
- `cmd_done` では、`find_agent` による `die()`（`:508-510`）より**前**に置くこと。順番を逆にすると、他人の記録に対して「その機体は居ません／履歴に押し出されたのでは」という**当たっていない案内**が出ます。

### 2-3. 方針2——`start` の保護（事故3）

**現在（`update_state.py:251-257`）:**

```python
    # 上書きする前に、いま置き換えようとしているものを見ておく。稼働中のまま
    # 履歴へ流れる記録は、以後ずっと「未完」として残るのに、退避の時点では何も
    # 言われない。ここが締め忘れに気づける最後の場所になる。
    # 見るだけで直さない。あとから finishedAt を書けば、それは実測ではなくなる。
    previous = read_state(project["slug"], project, required=False)
    prev_mission = previous.get("mission", {})
    prev_running = prev_mission.get("phase") == "running"
    prev_pending = unfinished_mission(previous)
```

**変更後:**

```python
    # 上書きする前に、いま置き換えようとしているものを見ておく。稼働中のまま
    # 履歴へ流れる記録は、以後ずっと「未完」として残るのに、退避の時点では何も
    # 言われない。ここが締め忘れに気づける最後の場所になる。
    # 見るだけで直さない。あとから finishedAt を書けば、それは実測ではなくなる。
    previous = read_state(project["slug"], project, required=False)
    prev_mission = previous.get("mission", {})
    prev_running = prev_mission.get("phase") == "running"
    prev_pending = unfinished_mission(previous)

    # **稼働中で、しかも別のセッションのものなら、退避せずに止まる。**
    # 押し出された記録は完了に直せない。取り返しがつかない操作を、押した本人が
    # 「押した」とすら思っていない状態で通してはいけない。
    #
    # **同じセッションのものなら、今までどおり退避する。** finish の打ち忘れは
    # 日常的に起きるので、そこで止めると次の作業が始められなくなる——閉じすぎは
    # 次の start でやり直せるが、始められないのはやり直しようがない。
    # （この非対称は running_missions_here と同じ天秤である。）
    if prev_running and not args.force and other_session_owns(previous):
        die(owner_conflict_message(project, previous, starting=True))
```

`--force` を渡したときは、いままでどおり退避します。**`--force` で押し出した記録にも `interruptedBy` は書かれる**ので（§2-4）、乱暴に押した事実は記録に残ります。

**この判定は `under_state_lock(cmd_start)` の中にあります**（`update_state.py:1750`）。読んで判断して書くまでが1つの錠の中に入るので、判定と退避の間に別プロセスが割り込むことはありません——ただし錠は待ち上限 5 秒で**取れなくても進む**（`dashlib.py:1999-2007`）ので、原理的な穴は残ります。今回の事故は9分離れた逐次操作なので、そこは関係ありません。

### 2-4. 方針3——押し出された記録に「なぜ終わっていないか」を残す

**新しい `phase` 値は足しません**（§1-7）。`mission` に**別のフィールド**を1つ足します。

```json
"interruptedBy": {
  "at": "2026-09-03T21:27:16+09:00",
  "by": "start",
  "title": "Gemini 水色にする計画書とプレビューを作る",
  "sameSession": false
}
```

- `at` — **押し出された時刻**。これは観測できた事実です。**`finishedAt` ではありません**——そのミッションが終わった時刻は誰も測っていない。
- `title` — 押し出した側のミッション名。「何に割り込まれたか」が分かるのが、この記録の値打ちです。
- `sameSession` — `true`（自分の締め忘れ）／`false`（別のセッション。保護が入ったあとは `--force` を使ったときだけ起きる）／`null`（どちらかの sessionId が無くて判定できなかった）。**画面には出しません**（§2-4-3）。記録に残すのは、あとから読む人のためです。

#### 2-4-1. 書く場所（`dashlib.py`）

**現在（`dashlib.py:1568-1600` の要点）:**

```python
def archive_current_run(slug: str) -> dict:
    ...
    try:
        result["runId"] = _move_current_to_history(slug)
    except OSError as e:
        _warn(...)
        return result
```

**変更後:**

```python
def archive_current_run(slug: str, interrupted_by: dict | None = None) -> dict:
    """...（既存の docstring はそのまま。末尾に1段落だけ足す）

    interrupted_by を渡すと、退避した記録が**稼働中のままだったときに限り**
    mission.interruptedBy として書き添える。既定は None＝何も足さない
    （引数を足しただけで、渡さない呼び出しの挙動は1バイトも変わらない）。
    """
    ...
    try:
        result["runId"] = _move_current_to_history(slug)
    except OSError as e:
        _warn(...)
        return result

    if interrupted_by:
        _stamp_interrupted(slug, result["runId"], interrupted_by)
    ...
```

新設する `_stamp_interrupted()`（`_move_current_to_history` の直後、`dashlib.py:1519` あたり）:

```python
def _stamp_interrupted(slug: str, run_id: str, note: dict) -> None:
    """押し出された記録に「なぜ稼働中のまま終わったか」を書き添える。

    **移したあとに書く。** 移す前に書くと、移動が落ちたときに「押し出された」と
    書かれた記録が現役のまま残る。移したあとの history/<runId>/state.json を
    書いているのは、この時点ではもう誰も居ない。

    **書くのは観測できた事実だけ。** phase も finishedAt も触らない——そのミッションが
    「終わった」時刻は誰も測っていない。実測でない値を書かないというこの道具の約束
    （cmd_start の「見るだけで直さない」）は、ここでも同じである。

    **締まっている記録には何も足さない。** 完了した記録が退避されるのは正常な流れで、
    そこに「打ち切られた」と書けば嘘になる。

    失敗しても黙って戻る。start は既に成立していて、これはその付随物である。
    ここで例外を上げると、記録を残せたのに start が落ちる。
    """
    path = run_state_file(slug, run_id)
    ok, value, _err = read_json_safe(path)
    if not ok or not isinstance(value, dict):
        return
    mission = value.get("mission")
    if not isinstance(mission, dict) or mission.get("phase") != "running":
        return
    mission["interruptedBy"] = note
    tmp = path.with_name(path.name + ".%d.tmp" % os.getpid())
    try:
        tmp.write_text(json.dumps(value, ensure_ascii=False, indent=2) + "\n",
                       encoding="utf-8")
        _replace_with_retry(tmp, path)
    except OSError:
        pass
    finally:
        try:
            tmp.unlink()
        except OSError:
            pass
```

**`t()` は1つも使いません。** したがって `i18n_data.py` には1文字も足しません。

#### 2-4-2. 渡す場所（`update_state.py:285`）

**現在:**

```python
    archived = dashlib.archive_current_run(project["slug"])
```

**変更後:**

```python
    # 稼働中のまま押し出すときだけ、なぜ終わっていないかを書き添える。
    # 完了した記録の退避は正常な流れなので、何も足さない（_stamp_interrupted 側でも
    # 弾いているが、渡さないほうが意図が読める）。
    note = None
    if prev_running:
        prev_owner = dashlib.as_str(prev_mission.get("sessionId"))
        me = current_session()
        note = {
            "at": at,
            "by": "start",
            "title": title,
            # 両方そろっていなければ「分からない」。false と混ぜない。
            "sameSession": (prev_owner == me) if (prev_owner and me) else None,
        }
    archived = dashlib.archive_current_run(project["slug"], note)
```

#### 2-4-3. 画面へ渡す（`dashlib.py:2307-2311`）

`_build_state()` の `mission` は**項目を並べて作り直しています**。並べていないキーは画面へ届きません。1つ足します。

**現在:**

```python
        "mission": {
            "phase": mission.get("phase") if mission.get("phase") in PHASES else "standby",
            "title": as_str(mission.get("title")) or t("(untitled mission)"),
            "startedAt": as_str(mission.get("startedAt")) or None,
            "finishedAt": as_str(mission.get("finishedAt")) or None,
```

**変更後:**

```python
        "mission": {
            "phase": mission.get("phase") if mission.get("phase") in PHASES else "standby",
            "title": as_str(mission.get("title")) or t("(untitled mission)"),
            "startedAt": as_str(mission.get("startedAt")) or None,
            "finishedAt": as_str(mission.get("finishedAt")) or None,
            "interruptedBy": _interrupted_note(mission.get("interruptedBy")),
```

新設する `_interrupted_note()`（`_build_state` の手前）:

```python
def _interrupted_note(v) -> dict | None:
    """押し出された理由のうち、**画面に出す2つだけ**を渡す形にする。無ければ None。

    sessionId は渡さない。人が読めない値で、画面に出す用途が無いうえ、ここを通った
    ものはブラウザから見えるところへ出ていく（サーバーがユーザー名を伏せているのと
    同じ考え方）。sameSession も渡さない——出し分ける文言を持たないので、渡せば
    「読まれない項目」が1つ増えるだけになる。
    """
    if not isinstance(v, dict):
        return None
    at = as_str(v.get("at")) or None
    title = as_str(v.get("title")) or None
    return {"at": at, "title": title} if (at or title) else None
```

#### 2-4-4. 画面（`public/index.html:3221`、1行だけ）

`normalizeState()` の `mission` は `Object.assign({}, s.mission)` なので（`:2364`）、**新しいキーはそのまま素通りします。触りません。**

**現在（`public/index.html:3221`）:**

```js
  phaseEl.title = unfinished ? tr('phase.unfinished_tip') : '';
```

**変更後:**

```js
  // 止まった記録が「未完」なのは、締め忘れか、次の start に打ち切られたかのどちらか。
  // 後者は state に理由が残っているので、そこを読んで言い分ける。**札の文字は変えない**
  // ——「未完」は両方に当たっている。変えるのは、その上に出す説明だけ。
  const cut = frozen && s.mission.interruptedBy;
  phaseEl.title = unfinished ? tr('phase.unfinished_tip')
                : cut ? tr('phase.interrupted_tip', {
                          when:  fmtStamp(cut.at) || tr('value.none'),
                          title: cut.title || tr('run.untitled') })
                : '';
```

**画面側の変更はこの1か所だけです。** `normalizeTab` も `phaseText` も `PHASES` も `server.py` も触りません。タブの札は「未完」のままで、それは正しい表示です。

### 2-5. `CLAUDE_CODE_SESSION_ID` が無い環境での挙動（**どこで通し、どこで止めるか**）

**保護が効かないのは仕方がない。道具が使えなくなってはいけない。** この表が本計画の憲法です。

| コマンド | 記録の `sessionId` | 呼び手の環境変数 | 挙動 | 理由 |
| --- | --- | --- | --- | --- |
| `start`（前が**稼働中**） | 有 | 有・**一致** | **退避して進む**（今までどおり。警告も今までどおり） | finish の打ち忘れは日常的に起きる。ここを止めると道具が使えない |
| `start`（前が**稼働中**） | 有 | 有・**不一致** | **拒否（終了コード 1）** | 事故3。`--force` で押し出せる |
| `start`（前が**稼働中**） | 有 | **無** | **退避して進む** | 呼び手の素性が不明。旧い導入・hook 未配線を殺さない |
| `start`（前が**稼働中**） | **無** | 有／無 | **退避して進む** | 0.9.1 より前に start された記録。後方互換 |
| `start`（前が稼働中でない） | — | — | **退避して進む** | 完了・未開始の退避は正常な流れ |
| `add` / `done` / `finish` / `log` | 有 | 有・**一致** | **通す** | 大多数がここ（サブエージェントからの呼び出しも含む。§1-5） |
| `add` / `done` / `finish` / `log` | 有 | 有・**不一致** | **拒否（終了コード 1）** | 事故4。`--force` で書ける |
| `add` / `done` / `finish` / `log` | 有 | **無** | **通す** | 人が別の端末から手で打つ運用を殺さない |
| `add` / `done` / `finish` / `log` | **無** | 有／無 | **通す** | 後方互換 |
| `hook`（自動登録の add / done） | — | — | **照合しない**（§2-6 の絞り込みだけ） | `_hook_target` は「その機体の記録が既にある」「呼び出し元の記録がある」という**実測**で書き先を決めており、推測が無い。ここへ照合を足すと、印を持たない古い記録への書き戻しが止まる |
| `autofinish` | — | — | **今までどおり**（`running_missions_here` の照合。§2-7） | 変更しない |
| `status` / `projects` / `history` / `reset` / `remove` / `lang` | — | — | **照合しない** | 読み取り、または人が明示的に打つ破壊的コマンド（§8） |

**「取れない環境」で保護が無力化することは、この設計が承知している穴です。** 塞ぐ唯一の方法は「印が無ければ拒否」ですが、それは §2-0 の表のとおり採りません。

### 2-6. 提案B——hook の書き先の絞り込み（小さいが、取りこぼしが減る）

`_hook_target()`（`update_state.py:912-968`）は、cwd で稼働中のミッションが2本以上あると**何も書きません**。

```python
    # update_state.py:956-966（現在）
    here = [(slug, st) for slug, st in missions
            if (st.get("mission") or {}).get("phase") == "running"
            and _same_dir(dashlib.as_str((st.get("project") or {}).get("path")), cwd)]
    if len(here) == 1:
        return pick(*here[0])
    if len(here) > 1:
        print(t("Dashboard: {n} missions are running in this folder, so there is no "
                "way to tell which one this unit belongs to. Nothing was recorded "
                "(it will show up as a running unit that is not in the records).")
              .format(n=len(here)), file=sys.stderr)
        return None
```

**変更後（`if len(here) == 1:` の手前に3行）:**

```python
    # 2本以上あっても、そのうち1本だけが自分のセッションのものなら決まる。
    # **推測ではない**——mission.sessionId は start が書いた事実で、いま呼んでいる
    # 側の CLAUDE_CODE_SESSION_ID も事実である。絞れたときだけ絞る（絞れなければ
    # 下の「決めない」に落ちるので、いまより取りこぼす方向へは動かない）。
    me = current_session()
    if len(here) > 1 and me:
        mine = [(s, st) for s, st in here
                if dashlib.as_str((st.get("mission") or {}).get("sessionId")) == me]
        if len(mine) == 1:
            here = mine

    if len(here) == 1:
        return pick(*here[0])
```

**壊しうるもの**: `CLAUDE_CODE_SESSION_ID` が hook のプロセスへ渡らない場合、`me` が空になって `if` が丸ごと素通りします＝**いまと同じ挙動**です。渡る場合だけ良くなります。**この提案は、失敗しても現状に戻るだけの形をしています。**（hook へ環境変数が渡るかは §8 の未確認事項。渡らないと分かったら、`changelog_cli.py:207` と同じく stdin JSON の `session_id` を読む道がありますが、それは本計画の範囲外にします。）

### 2-7. `autofinish` との整合

`running_missions_here()`（`update_state.py:705-755`）の照合規則は次のとおりで、**本計画はこれと同じ規則を使います。**

| | `running_missions_here` | 本計画（`other_session_owns`） |
| --- | --- | --- |
| 記録に印があり、呼び手にも印があり、**一致** | 締める | 通す |
| 記録に印があり、呼び手にも印があり、**不一致** | **締めない** | **拒否** |
| どちらかに印が無い | 締める（今までどおり） | 通す（今までどおり） |

**同じ天秤の、向きが逆の適用です。** `autofinish` は「分からないときは締める」（閉じすぎは次の start でやり直せるが、閉じ忘れは直せない）。本計画は「分からないときは書かせる」（書きすぎは記録の混ざりで済むが、書けないのは道具が使えないということ）。**どちらも「分からないときは、いままでの動きを続ける」**という同じ原則から出ています。

`running_missions_here()` のコードは**1行も変えません。** `cmd_autofinish` が呼ぶのは `under_state_lock(cmd_finish)` なので、`cmd_finish` に足す照合を通ります。ここで**二重に弾かれないか**を確認しておきます:

- `running_missions_here` が返すのは「印が無い」か「印が一致する」ミッションだけ。
- したがって `cmd_finish` の `other_session_owns()` は必ず空を返す＝通る。
- **矛盾しません。** `check_autofinish.py` の [8][9] がそのまま通ることで実測できます（§6）。

### 2-8. `--force` を足す（5コマンド）

`start` / `add` / `done` / `finish` / `log` の5つに同じ引数を足します。`build_parser()`（`update_state.py:1736-1804`）へ。

```python
def add_force_arg(p: argparse.ArgumentParser, *, starting: bool) -> None:
    """持ち主が違っても書く、という明示。

    **既定は「書かない」。** 環境変数で切れるようにしないのは、一度立てたら以後
    すべてのコマンドで黙って無効になり、コマンド行を見ても分からないからである
    ——それは今回の事故と同じ種類の問題になる。切るならコマンド行に書かせる。
    """
    p.add_argument(
        "--force", action="store_true",
        help=(t("push the record that is here into the history even when another "
                "session owns it") if starting else
              t("write to this record even when another session owns it")),
    )
```

呼び出しは `add_project_arg(s)` の隣に1行ずつ（`s` は `starting=True`、他の4つは `starting=False`）。

**`autofinish` には足しません。** `af` のパーサに `--force` が無いので、`cmd_autofinish` が作る `argparse.Namespace(**vars(args))` にも `force` は入りません。`cmd_finish` は `args.force` を読むので、**そのままだと `AttributeError` になります。** `cmd_autofinish` の1行を直します。

**現在（`update_state.py:762-768`）:**

```python
    for slug in running_missions_here(getattr(args, "project", None), current_session()):
        one = argparse.Namespace(**vars(args))
        one.project = slug
        if not one.headline:
            one.headline = t("closed automatically when the session ended")
```

**変更後:**

```python
    for slug in running_missions_here(getattr(args, "project", None), current_session()):
        one = argparse.Namespace(**vars(args))
        one.project = slug
        # running_missions_here が既に持ち主を見ているので、cmd_finish の照合は
        # 必ず通る。それでも属性は要る（無いと AttributeError）。**二重に弾かない
        # ための force ではなく、cmd_finish が読む属性を用意するための force。**
        one.force = False
        if not one.headline:
            one.headline = t("closed automatically when the session ended")
```

`getattr(args, "force", False)` で読む手もありますが、**属性がある前提で書くほうが、次に誰かが `cmd_finish` を別の場所から呼んだときに落ちて気づけます。** 黙って `False` になる書き方は、保護が黙って無効になる書き方でもあります。

### 2-9. `demo` の保護（読み合わせで発覚した穴。§8にも事実として記録する）

**この節は当初の計画（§1〜§2-8）には無かった。** §2-2/§2-3 の実装が終わったあとの読み合わせで、レビュアーが実測した:

> `cmd_demo`（`update_state.py` の 1480〜1641行目付近）は `dashlib.write_state(slug, state)` と `dashlib.set_current(slug)` を直接呼ぶだけで、`other_session_owns()` の照合も `archive_current_run()` による退避も一切ない。甲（`sess-kou`）が `start` → `add SCOUT-A` した**稼働中**の記録に対し、乙（`sess-otsu`）が同じディレクトリで `demo` を打つと、**終了コード0で成功**し、甲の記録（`phase:running, sessionId:sess-kou, agents:[COMMAND, SCOUT-A]`）がダミー表示データ（`sessionId:None`）へ**無条件に上書き**され、`history/` は**0件のまま**。

**`start` の押し出し（§2-3）よりも重い壊れ方である。** `start` は照合を通らなければ拒否し、`--force` で通しても `archive_current_run()` で退避してから上書きする。`demo` はそのどちらも通らずに `write_state()` へ直接書く経路だったので、**稼働中の記録が退避すらされずに消える**。しかも他人のセッションに対してだけでなく、**自分の稼働中の記録に対しても同じことが起きる**——`demo` は「表示テスト用のダミーを入れる」コマンドであって、「それまでの記録を消してよい」という意味では元々なかった。

**直し方は `cmd_start`（§2-3）とほぼ同じ形にした。** `cmd_demo`（`update_state.py:1480`）でダミーの文言を組み立てたあと・`dashlib.empty_state(project)` で上書きを始める前に、`read_state()` を挟む:

```python
    previous = read_state(slug, project, required=False)
    prev_mission = previous.get("mission", {})
    prev_running = prev_mission.get("phase") == "running"

    if prev_running and not args.force and other_session_owns(previous):
        die(owner_conflict_message(project, previous, starting=True))

    at = now_iso()
    state = dashlib.empty_state(project)
    ...
```

`owner_conflict_message()` は新しい原文を1つも足さずにそのまま使えた（`starting=True` を渡す——`demo` は `add`/`done`/`finish`/`log` のように「いまの記録に書き足す」のではなく、`start` と同じく「いまの記録を丸ごと置き換える」ため）。

**`start` と1つだけ違うのは、退避を「同じセッションかどうかで分岐させない」こと。** `cmd_start` は同じセッションなら締め忘れの救済として無条件に退避するが、それは「別のミッションを始めたい」という利用者の意図があるからである。`demo` にはその意図が無い——表示テストのついでに実データを消してよい理由にはならない。だから拒否を通り抜けたあと（＝別セッションの記録が無い、または `--force` を付けた）は、同一セッションかどうかにかかわらず常に `dashlib.archive_current_run(slug, note)` を書き込みの直前に呼ぶ:

```python
    note = None
    if prev_running:
        prev_owner = dashlib.as_str(prev_mission.get("sessionId"))
        me = current_session()
        note = {
            "at": at,
            "by": "demo",
            "title": demo_title,
            "sameSession": (prev_owner == me) if (prev_owner and me) else None,
        }
    archived = dashlib.archive_current_run(slug, note)

    dashlib.write_state(slug, state)
    dashlib.set_current(slug)
```

`interruptedBy.by` には（§2-4 の形式をそのまま流用し）`"start"` ではなく `"demo"` を書く——`by` は退避を引き起こしたコマンド名である。

パーサ側（`update_state.py:2028` 付近）にも `add_force_arg(dm, starting=True)` を配線し、`dm.set_defaults(func=under_state_lock(cmd_demo))` として他の書き込み系コマンドと同じ錠（`under_state_lock`）を通す。**元は錠も掛かっていなかった**（`func=cmd_demo` のみで `under_state_lock` を経由しない唯一の書き込み系コマンドだった）ので、hook が複数体まとめて起動する最中に人が `demo` を打つような重なりに対しても、他の5コマンドと同じ直列化が効くようになった。

**実測は `check_autofinish.py` に [19]〜[21] として足した（§5-3 の形式に合わせる）:**

- **[19]** 他セッションの稼働中の記録に対し `demo`（`--force` 無し）→ 終了コード1、`history/` は増えない、記録（タイトル・機体）は無傷。
- **[20]** [19] に `--force` を付ける → 終了コード0、`history/` が1件増え、退避された記録は `phase: running` のまま（`finishedAt` は書かれない）、`interruptedBy.by == "demo"`、`interruptedBy.sameSession == false`。
- **[21]** **同じセッション**からの `demo`（`--force` 無し）→ 終了コード0（拒否されない。§2-1 の「同じセッションなら通す」規則どおり）。**それでも** `history/` が1件増える——退避そのものは同一セッションでも行う、が`cmd_start` との違いであることの検査。`interruptedBy.sameSession == true`。

3件とも `PYTHONIOENCODING=utf-8 python check_autofinish.py` で実測、EXIT=0（21グループすべて OK）。

---

## 3. 壊してはいけないもの

1. **サブエージェントから `add` / `done` / `finish` / `log` が打てること。** §1-5 の実測（サブエージェントは親の `CLAUDE_CODE_SESSION_ID` をそのまま受け継ぐ）が、`other_session_owns()`（§2-1）の唯一の前提である。新しい照合は `cmd_add`（`update_state.py:383`）・`cmd_done`（`:503`）・`cmd_finish`（`:595`）・`cmd_log`（`:1117`）の4か所、`read_state()` の直後に入る。**この前提が崩れている環境では、司令塔が `start` したミッションへサブエージェントが1体も書き込めなくなる。** 締め忘れよりも重い壊れ方であり、道具そのものが使い物にならなくなる。§1-5 の実測（本計画を書いたサブエージェント自身のシェル・別のサブエージェントのシェル・親が書いた `mission.sessionId` の3点が一致）は Claude Code 2.1.259 / `claude-vscode` / Windows 11 の1回きりなので、§5 の受け入れ試験は**サブエージェントを模した別プロセスから `add` を打つ**手順を必ず含めること。
2. **`sessionId` を持たない既存の `state.json` と `history/` の記録が読めること（後方互換）。** `read_state()`（`update_state.py:90-115`）は `mission` に無い鍵を単に `.get()` で拾うだけで、無ければ `None` になる。`other_session_owns()` は `owner` が空文字なら無条件で通す（§2-1 の docstring）ので、0.9.1 より前に `start` された記録――`mission.sessionId` を持たない――は今までどおり誰からでも書ける。`history/` 側も同様で、`_interrupted_note()`（§2-4-3）は `mission.get("interruptedBy")` が無ければ `None` を返すだけなので、古い履歴を読んでも例外にならない。**新しい鍵が「無いと落ちる」形にしないこと**――`.get()` で拾い、無ければ何もしない、を貫く。
3. **`autofinish` の既存の持ち主照合（`running_missions_here()`、`update_state.py:705-755`）と矛盾しないこと。** `cmd_autofinish`（`:758`）が呼ぶのは `under_state_lock(cmd_finish)` なので、`cmd_finish` に足す照合を必ず通る。§2-7 で確認したとおり、`running_missions_here()` が返す一覧は「印が無い」か「印が一致する」ミッションだけであり、そこに対して `cmd_finish` 側の `other_session_owns()` は必ず空文字を返す＝二重に弾かれない。**この関係を壊す変更（`running_missions_here()` の絞り込み条件を緩める、`other_session_owns()` の判定を厳しくする、など）を、どちらか片方だけに入れないこと。** 崩れていないことは `check_autofinish.py` の [8][9]（他セッションの記録を締めない・素性不明では締める）がそのまま通ることで実測できる（§5）。
4. **`phase` は3値のまま（`dashlib.PHASES`、`dashlib.py:83-84`）。** §1-7 で確認したとおり、`phase` に4つめの値を足すと、サーバー側（`server.py:186-187` の `_phase_of`、`dashlib.py:1713`/`2308`）は「未着手」へ、クライアント側（`public/index.html:1918-1919`/`2365-2367`）は「完了」へ、正反対に丸める。本計画が足す `interruptedBy` は `mission` の**別の鍵**であって `phase` の値ではない――「押し出されたのに完了扱いになる」も「押し出されたのに未着手に戻る」も起こしてはならない。
5. **実測でない値を書かないという道具全体の約束。** `cmd_start` の既存コメント（`:251-253`、「見るだけで直さない」）と `_stamp_interrupted()`（§2-4-1）の docstring が明記するとおり、押し出された記録に `finishedAt` を書いてはいけない――そのミッションが終わった時刻は誰も測っていない。同じ理由で `--force` の拒否メッセージ（§2-1 の `owner_conflict_message()`）や `interruptedBy.sameSession`（§2-4）も、測れなかったものは `null`／案内文で「分からない」とだけ言い、断定しない。**エラーメッセージ1つ、記録の1鍵に至るまで、この約束から外れないこと。**
6. **`hook` 経由の自動登録が黙って死なないこと。** `cmd_hook`（`update_state.py:1057`）→ `_hook_main()`（`:1073`）は `cmd_add` / `cmd_done` を一切呼ばず、`dashlib.write_state()` へ**直接**書く（`:1104-1110`）。したがって本計画が `cmd_add` / `cmd_done` に足す `other_session_owns()` の照合は、構造的に hook の経路を一切通らない――これは意図した設計で、§2-5 の表が「hook: 照合しない」としている理由そのものである。**将来 `_hook_main()` を `cmd_add()` / `cmd_done()` の再利用へリファクタリングするときは、この照合が hook 経由の書き込みへ紛れ込まないよう、明示的に迂回させること。** 紛れ込むと、印を持たない古い記録（後方互換の対象そのもの）への自動登録が、理由も分からず沈黙して失敗するようになる。`cmd_hook()` 自身は「何があっても 0 で終わる」（`:1058-1064` の docstring）ので、この経路が失敗しても `sys.exit(0)` で覆い隠され、失敗にすら気づけない。

---

## 4. 作業の分割

**同じファイルを2人が同時に触らせないこと。** 触るファイルは grep で実際に確定した。**16ファイル**になる。

| 分類 | ファイル | この計画でどこを触るか |
| --- | --- | --- |
| 本体 | `dashlib.py` | `archive_current_run()`（`:1568`）に引数追加、`_stamp_interrupted()` 新設（§2-4-1）、`_interrupted_note()` 新設・`_build_state()` の `mission` に1鍵追加（§2-4-3） |
| 本体 | `update_state.py` | `ENV_SESSION`/`current_session()` の移動、`other_session_owns()`/`suggest_project()`/`owner_conflict_message()` 新設（§2-1）、`cmd_add`/`cmd_done`/`cmd_finish`/`cmd_log` への照合（§2-2）、`cmd_start` の保護と `interruptedBy` の組み立て（§2-3・§2-4-2）、`_hook_target()` の絞り込み（§2-6）、`add_force_arg()` 新設と5サブパーサへの配線（§2-8）、`cmd_autofinish` の1行（§2-8） |
| 文言表 | `i18n_data_update.py` | `owner_conflict_message()` と `add_force_arg()` が使う原文の ja/zh/ko 訳を追加。**この計画で増える `t()` の原文はすべて `update_state.py` の中にしかない**ので、行き先はここだけ。`i18n_data.py`（`lang` コマンドなど共通表）と `i18n_data_install.py`（`install.py` 自身の出力）には、この計画では1文字も足さない |
| 文言表 | `public/i18n.js` | `phase.interrupted_tip` を新設（§2-4-4 が `tr()` で呼ぶ）。en (`:116` 付近) / ja (`:290`) / zh (`:451`) / ko (`:612`) の4ブロックそれぞれに1行ずつ、既存の `phase.unfinished_tip` の隣へ。**`i18n_data_update.py` とは完全に別系統**（画面の JS 側と、CLI の Python 側は合成される表が別。i18n_data_update.py の docstring 参照） |
| 画面 | `public/index.html` | `:3221` の1行のみ（§2-4-4）。**同じファイルを別の改修計画（`拡大率と区画の改修計画.md`）が広範囲に触っている場合があるので、その作業と時間を分けること。同時に開いて Edit すると、どちらかの差分が丸ごと衝突する** |
| 試験 | `check_autofinish.py` | 甲/乙シナリオ（[8][9]）に隣接させて、§5-3 の試験項目を追加 |
| 文書 | `install.py` | `build_block()`（`:194-266`）の「### When you run two or more missions...」節（`:243-264`）に、既定で拒否されるようになったことを短く追記するかどうかを§7で線引きする |
| 文言表 | `i18n_data_install.py` | 上の `install.py` を直した場合、その ja/zh/ko 訳をここに追加（触らないなら、この計画では変更なし） |
| 文書 | `OPERATION.ja.md` / `.md` / `.ko.md` / `.zh.md` | 「6.1. 同じディレクトリで2本以上を同時に走らせるとき」節と、`autofinish` の持ち主照合を説明した節（ja 版で `:440-465` 付近）を実装後の挙動に合わせて書き直す |
| 文書 | `README.ja.md` / `.md` / `.ko.md` / `.zh.md` | 「同じフォルダで2本を同時に走らせるとき」節（ja 版で `:205-220` 付近）を同様に書き直す |

`dash.py` / `dash.cmd` / `dash` ラッパーは触らない。`dash.py:127-129` が `update_state.main()` へ `sys.argv` を渡して委譲するだけなので、`--force` は `update_state.py` 側の変更だけで自動的に通る。

### 4-1. 依存関係

```
dashlib.py（archive_current_run の新シグネチャ）
      ↓
update_state.py（cmd_start が新シグネチャを呼ぶ。other_session_owns 等の新設と
                 --force の配線がここで一度に決まらないと、args.force を読む
                 cmd_add/cmd_done/cmd_finish/cmd_log 側が AttributeError になる）
      ↓                                  ↓
i18n_data_update.py                  check_autofinish.py
（増えた原文を訳す。原文が                （実装の挙動に対する回帰試験。
  確定してから）                            原文とは無関係に書ける）
      ↓
public/i18n.js（interruptedBy のキー名 at/title が
               dashlib.py 側で決まってから。i18n_data_update.py と並行可）
      ↓
public/index.html（1行。public/i18n.js の phase.interrupted_tip が
                   生えてから。ここも並行可）
      ↓
OPERATION.*.md / README.*.md / install.py（挙動と文言が両方確定してから。
                                          先に書くと二度手間になる）
```

**`update_state.py` は1本の手番として扱い、途中で担当を替えないこと。** `other_session_owns()`（§2-1）を呼ぶ側が4か所（`cmd_add`/`cmd_done`/`cmd_finish`/`cmd_log`）、`--force` を読む側も同じ4か所+`cmd_start`、`_hook_target()` の絞り込みも同じファイルの別関数――**どれも同じファイルの中で閉じた依存**なので、分割してもファイル競合が減らない一方、`other_session_owns()` の呼び出し規約（読んだ直後・書く前、§2-2 の「入れる位置に2つだけ注意」）を2人がそれぞれ思い出しながら同じファイルへ書き込むリスクだけが増える。

### 4-2. 手番表

| 手番 | 担当 | やること | 前提 |
| --- | --- | --- | --- |
| 0 | VERIFY | **事故を再現する**（§5-1）。甲/乙シナリオを回し、乙の `start` が甲を「稼働中のまま」履歴へ押し出すこと、甲の後続の `add` が成功して乙の記録に混ざることを、コマンドの出力として残す | 誰も編集していない状態（HEAD のまま） |
| 1 | IMPL-CORE | `dashlib.py`（§2-4-1・§2-4-3）を実装してコミット | 手番0の記録が取れていること |
| 2 | IMPL-CORE（同一人物が望ましい） | `update_state.py` を §2-1 → §2-8（`--force` の配線）→ §2-2 → §2-3 → §2-6 の順に実装してコミット。**この順でなければ `args.force` が無い状態で `other_session_owns()` を読む行ができ、`AttributeError` になる** | 手番1のコミット（`dashlib.archive_current_run()` の新シグネチャに依存） |
| 3 | IMPL-I18N | `i18n_data_update.py` に手番2で増えた原文の ja/zh/ko 訳を追加してコミット | 手番2のコミット（原文が確定してから訳す。`check_i18n.py` が原文と鍵の対応を機械的に見る） |
| 4 | IMPL-UI | `public/i18n.js` の4ブロックと `public/index.html` の1行を実装してコミット。**同じファイルを触っている他の改修計画が無いことを確認してから着手する**（§4 冒頭の注意） | 手番1のコミット（`interruptedBy.at`/`.title` のキー名が決まってから）。手番3とは別ファイルなので並行できる |
| 5 | TEST | `check_autofinish.py` に §5-3 の試験項目を追加してコミット | 手番2のコミット（実装の挙動が固まってから、その回帰試験を書く）。手番3・4とは別ファイルなので並行できる |
| 6 | VERIFY | **手番0とまったく同じ手順**で再実行し、乙の `start` が拒否されること・甲の `add` が拒否されることを示す（§5-2）。あわせて §6 のチェックリストを一通り回す | 手番2・3・4・5がすべてコミット済み |
| 7 | IMPL-DOCS | `OPERATION.ja.md` / `README.ja.md` を実装後の挙動に合わせて書き直す。`install.py` の `build_block()` に追記するかは §7 の線引きに従う | 手番6で挙動と文言（実際のエラー文言・`--force` の効き）が確認できてから。先に書くと実装と食い違う |
| 8 | IMPL-DOCS-TR | 手番7の日本語版から、`OPERATION.md` / `.zh.md` / `.ko.md`、`README.md` / `.zh.md` / `.ko.md`、（`install.py` を直した場合は）`i18n_data_install.py` の訳を作る | 手番7のコミット。**この8ファイルも同時に2人が触らない**（節の対応がずれる） |
| 9 | REVIEW | 差分全体の読み合わせ（§5-4）と検査スクリプトの実行 | 手番0〜8すべて |

**手番1と手番2、手番7と手番8を、それぞれ同時に走らせないこと。** それ以外の同じ手番内（3・4・5、あるいは7の中の日本語版とは別に8の中の6ファイル）は、ファイルが重ならない組み合わせに限り並行してよい。

### 4-3. 何人でやるか

**最小1名**（全手番を順番に1人でこなす）でも成立する。役割を分けるなら**5名程度**が実務的な下限で、VERIFY・IMPL-CORE・IMPL-I18N/UI・TEST・IMPL-DOCS の5役を置き、手番0・6・9（VERIFY→REVIEW）は同じ人物が通しで見るとよい――事故の再現条件と、直ったことの確認は、同じ目で比べたほうが取り違えにくい。**IMPL-CORE（手番1・2）だけは1人に固定すること。** ここを2人に分けても、4-1 で見たとおりファイル競合が減らないうえ、`other_session_owns()` の呼び出し規約を2人がそれぞれ思い出す手間が増えるだけである。

---

## 5. レビューとテストの計画

**事故を再現してから、直ったことを示す。** 順番を逆にしない――直す前の壊れ方を実物のコマンドで見ていないと、「直った」がただの主張になる。

### 5-1. 事故を再現する（実装前。手番0）

環境変数 `CLAUDE_CODE_SESSION_ID` を手で変えれば、1台の端末で2つのセッションを模擬できる（`check_autofinish.py` の `run()` がすでにこの形を使っている）。`AGENT_DASHBOARD_DATA_HOME` を隔離して、本物の `missions/` には一切触れない。

```bash
SCRATCH="<scratchpad>"
HOME_DIR="$SCRATCH/dash_repro_home"
WORK="$SCRATCH/dash_repro_cwd"
rm -rf "$HOME_DIR" "$WORK"
mkdir -p "$HOME_DIR" "$WORK"
export AGENT_DASHBOARD_DATA_HOME="$HOME_DIR"
export AGENT_DASHBOARD_LANG=ja
export PYTHONIOENCODING=utf-8
UP="C:\Users\yuta_\OneDrive\Python_files\Subagent Dashboard\update_state.py"
cd "$WORK"

# ---- 甲（セッション sess-kou）: start → add ×3 → done ×2（正常）----
CLAUDE_CODE_SESSION_ID=sess-kou python "$UP" start --title "甲のミッション" --model claude-opus-5
SLUG=$(cat "$HOME_DIR/missions/.current")
CLAUDE_CODE_SESSION_ID=sess-kou python "$UP" add  --id SCOUT-A --name "偵察A" --model claude-sonnet-5 --mission "甲の調査A"
CLAUDE_CODE_SESSION_ID=sess-kou python "$UP" add  --id SCOUT-B --name "偵察B" --model claude-sonnet-5 --mission "甲の調査B"
CLAUDE_CODE_SESSION_ID=sess-kou python "$UP" add  --id SCOUT-C --name "偵察C" --model claude-sonnet-5 --mission "甲の調査C"
CLAUDE_CODE_SESSION_ID=sess-kou python "$UP" done --id SCOUT-A --headline "Aの結果"
CLAUDE_CODE_SESSION_ID=sess-kou python "$UP" done --id SCOUT-B --headline "Bの結果"
python -c "import json; print(json.load(open('$HOME_DIR/missions/$SLUG/state.json',encoding='utf-8'))['mission']['phase'])"
# -> running（甲はまだ SCOUT-C の完了を待っている）

# ---- 乙（セッション sess-otsu）: 同じディレクトリで start ----
CLAUDE_CODE_SESSION_ID=sess-otsu python "$UP" start --title "乙のミッション" --model claude-opus-5
echo "終了コード: $?"
RUN_ID=$(ls "$HOME_DIR/missions/$SLUG/history" | tail -n 1)
python -c "import json; print(json.load(open('$HOME_DIR/missions/$SLUG/history/$RUN_ID/state.json',encoding='utf-8'))['mission']['phase'])"
# 【現状の壊れ方1】終了コードは 0。甲の記録が phase=running のまま history/ へ押し出される

# ---- 甲が気づかず add を続ける ----
CLAUDE_CODE_SESSION_ID=sess-kou python "$UP" add --id PLAN-A --name "追加機体" --model claude-opus-5 --mission "気づかず打った add"
echo "終了コード: $?"
python -c "import json; print([a['id'] for a in json.load(open('$HOME_DIR/missions/$SLUG/state.json',encoding='utf-8'))['agents']])"
# 【現状の壊れ方2】終了コードは 0。乙の state.json（=いまの $SLUG/state.json）に PLAN-A が混ざる

# ---- 甲が SCOUT-C の完了を打つ ----
CLAUDE_CODE_SESSION_ID=sess-kou python "$UP" done --id SCOUT-C --headline "Cの結果"
echo "終了コード: $?"
# 【現状の壊れ方3、観測できるので一番マシ】終了コードは 1。SCOUT-C は乙の state.json に居ない
```

**期待する出力**: 壊れ方1〜3がそのとおり再現すること。再現しなければ、直った・直っていないの比較が成立しないので、条件（`SLUG` の取り違え、`AGENT_DASHBOARD_DATA_HOME` の隔離漏れなど）を疑って作り直す。

### 5-2. 直ったことを示す（実装後。手番6）

**5-1 とまったく同じコマンド列**を、`update_state.py` を差し替えたあとにもう一度流す。今度は次のように変わっているはずである。

```bash
# ---- 乙の start（同じコマンド）----
CLAUDE_CODE_SESSION_ID=sess-otsu python "$UP" start --title "乙のミッション" --model claude-opus-5
echo "終了コード: $?"
# 期待: 終了コード 1。owner_conflict_message()（starting=True）が出る。
# 甲の state.json は history/ へ動いていない（$HOME_DIR/missions/$SLUG/state.json が
# まだ甲の記録のままであること、$HOME_DIR/missions/$SLUG/history/ に新しい runId が
# 増えていないことを確認する）

# ---- --force を付ければ、いままでどおり通る ----
CLAUDE_CODE_SESSION_ID=sess-otsu python "$UP" start --title "乙のミッション" --model claude-opus-5 --force
echo "終了コード: $?"
RUN_ID=$(ls "$HOME_DIR/missions/$SLUG/history" | tail -n 1)
python -c "
import json
m = json.load(open(r'$HOME_DIR/missions/$SLUG/history/$RUN_ID/state.json', encoding='utf-8'))['mission']
print(m['phase'], m.get('interruptedBy'))
"
# 期待: 終了コード 0。押し出された記録の phase は running のまま（§3-4 のとおり finishedAt は書かない）。
# interruptedBy.at に押し出された時刻、.title に「乙のミッション」、.sameSession に false が入る

# ---- 甲が気づかず add を続ける（--force を付けていない）----
CLAUDE_CODE_SESSION_ID=sess-kou python "$UP" add --id PLAN-A --name "追加機体" --model claude-opus-5 --mission "..."
echo "終了コード: $?"
# 期待: 終了コード 1。owner_conflict_message()（starting=False）が出て、suggest_project() の
# 案内どおりの --project 名が本文に含まれる。$SLUG/state.json（乙の記録）に PLAN-A は増えない
```

**期待する出力がそのとおりになれば、この事故は再現しなくなったと言える。** `--force` を付けたときだけ、いままでどおりの「押し出し」が起きる。

### 5-3. `check_autofinish.py` へ足すべき試験項目

既存の [1]〜[9] は変更しない（`running_missions_here()` は本計画で1行も変えないので、§2-7 のとおりそのまま通るはず）。**その直後に、以下を新設の番号で足す。** `run()` / `current_slug()` / `state_of()` / `phase_of()` は既存のものをそのまま使い、次の1つだけ補助を増やす。

```python
def latest_history_state(home: Path, slug: str) -> dict:
    """history/ に一番最後に増えた runId の state.json を読む。押し出された記録の検査に使う。"""
    hist = home / "missions" / slug / "history"
    run_id = sorted(p.name for p in hist.iterdir())[-1]
    return json.loads((hist / run_id / "state.json").read_text(encoding="utf-8"))
```

- **[10] `start` は、稼働中で他セッションの記録を押し出さない。** 甲（`sess-kou`）で `start` → `add`。乙（`sess-otsu`）で同じディレクトリへ `start` を打ち、終了コード 1・`history/` が増えていないこと・`missions/<slug>/state.json` が甲の記録のままであることを確かめる。**5-1 の再現手順そのものを、そのままアサーションへ落とす。**
- **[11] `[10]` に `--force` を付ければ、いままでどおり押し出す。** `history/` が1件増え、その `mission.phase` は `"running"` のまま、`mission.interruptedBy` に `at`/`by`/`title`/`sameSession=false` が入ることを確かめる。
- **[12] 完了済みの記録の退避には `interruptedBy` を書かない。** 甲が `finish` してから乙が `start`。`history/` に増えた記録に `interruptedBy` キーが**存在しない**ことを確かめる（`_stamp_interrupted()` が `phase != "running"` で早期リターンする経路）。
- **[13] `add` / `done` / `finish` / `log` は、他セッションが `start` した記録には書けない。** 乙が `start` したあと、甲（`sess-kou`）で4コマンドそれぞれを打ち、すべて終了コード 1・`state.json` に変化が無いことを確かめる。`done` は `find_agent` の「居ません」ではなく、持ち主違いの案内が先に出ることも見る（§2-2 の「入れる位置」の注意そのもの）。
- **[14] `[13]` に `--force` を付ければ書ける。** 4コマンドとも終了コード 0 になり、`state.json` に反映されることを確かめる。
- **[15] 同じセッションからは、他セッションが `start` した記録にも今までどおり書ける。** `[13]` と同じ状況で、`session="sess-otsu"`（乙自身）から `add` を打つと成功することを確かめる――サブエージェントが親と同じセッションIDを受け継ぐ経路の代理検査（§3 の1番目）。
- **[16] `sessionId` の有無の組み合わせ4通りで、すべて通る。** `session=""` で `start` した記録に `session="sess-x"` で `add`／`session="sess-x"` で `start` した記録に `session=""` で `add`／両方 `""`／両方同じ値、の4通りを回し、いずれも終了コード 0 であることを確かめる（§6 の表をそのままテストに落としたもの）。
- **[17] `suggest_project()` は同じセッションからは同じ名前を返す。** `[10]` の拒否メッセージから `--project` の行を2回抜き出し、同じ文字列であることを確かめる。
- **[18] `_hook_target()` の絞り込み（提案B）。** 同じディレクトリで甲・乙それぞれが `start` して2本とも稼働中にしたあと、`hook` サブコマンドへ `CLAUDE_CODE_SESSION_ID=sess-kou` で PreToolUse の JSON を渡すと、**甲の `state.json` にだけ**機体が増えることを確かめる（絞り込み前は「決められない」で両方とも増えない――[6] 相当の状況が2本ある場合に退化していないことの検査）。

### 5-4. レビュー（読み合わせ）

- **REVIEW**: `other_session_owns()` の呼び出しが、`cmd_add`/`cmd_done`/`cmd_finish`/`cmd_log` の4か所すべてで「`read_state()` の直後・書き込みより前」になっているか。特に `cmd_done` は `find_agent` の `die()` より前、`cmd_finish` は `snapshot_orphans()` より前（§2-2 の注意）。
- **REVIEW**: `--force` を読む5コマンド（`start`/`add`/`done`/`finish`/`log`）と、読まない `autofinish` の扱い（`cmd_autofinish` の `one.force = False`）が、§2-8 の記述どおりになっているか。
- **REVIEW**: `_stamp_interrupted()` が `phase != "running"` の記録に触っていないか（§5-3 [12]）。`finishedAt` を書いていないか。
- **REVIEW**: コメントが「なぜ」を書いているか。取らなかった選択肢（§2-0）まで残っていること。
- **検査スクリプト**: `check_wiring.py` / `check_i18n.py` / `check_livefeed.py` / `check_agents.py` / `check_autofinish.py` / `check_autoreg.py` / `check_lang.py` を全部通す。**今回の変更で反応しうるのは `check_i18n.py`（原文を追加したので、`i18n_data_update.py` 側の訳が揃っているか）と `check_autofinish.py`（§5-3 の追加分）の2本。** 残り5本は、この計画が触らない領域（画面の markup 構造、livefeed、孫エージェントの自己申告、言語切り替えそのもの）を見ているので、通らない理由が無い。

---

## 6. 検証項目のチェックリスト

実装後に1つずつ潰す。§2-5 の表と同じ規則だが、ここでは**組み合わせを1行1項目に開いて**チェックできる形にする。

### 6-1. `start`（前が稼働中のとき、退避してよいか）

| # | 記録の `sessionId` | 呼び手の `CLAUDE_CODE_SESSION_ID` | 期待する挙動 | 確認 |
| --- | --- | --- | --- | --- |
| 1 | 無 | 無 | 退避して進む（後方互換） | [ ] |
| 2 | 無 | 有 | 退避して進む（後方互換） | [ ] |
| 3 | 有 | 無 | 退避して進む（呼び手の素性が不明。旧い導入・hook 未配線を殺さない） | [ ] |
| 4 | 有 | 有・**一致**（同じセッションの締め忘れ） | 退避して進む（今までどおり。警告も今までどおり） | [ ] |
| 5 | 有 | 有・**不一致** | **拒否（終了コード 1）**。`--force` を付ければ退避して進み、`interruptedBy.sameSession=false` が付く | [ ] |

### 6-2. `add` / `done` / `finish` / `log`（いまの記録に書いてよいか）

| # | 記録の `sessionId` | 呼び手の `CLAUDE_CODE_SESSION_ID` | 期待する挙動 | 確認 |
| --- | --- | --- | --- | --- |
| 6 | 無 | 無 | 通す（後方互換） | [ ] |
| 7 | 無 | 有 | 通す（後方互換） | [ ] |
| 8 | 有 | 無 | 通す（人が別の端末から手で打つ運用を殺さない） | [ ] |
| 9 | 有 | 有・**一致**（司令塔本人、またはそのセッションを受け継いだサブエージェント） | 通す（大多数がここ。§1-5） | [ ] |
| 10 | 有 | 有・**不一致** | **拒否（終了コード 1）**。`--force` を付ければ通る | [ ] |

**4番と9番が、この計画がいちばん壊してはいけない行である。** ここを拒否側に倒すと、締め忘れの日常的な打ち直し（4番）や、サブエージェントからの `add`（9番）が止まり、道具そのものが使えなくなる。

### 6-3. 周辺の整合

- [ ] `hook` サブコマンド（`cmd_hook`）は、上の表のどの組み合わせでも `other_session_owns()` を一切通らない（§3-6・§2-5）。渡す JSON の `session_id` の有無に関わらず、いままでどおり `_hook_target()` の絞り込みだけで書き先を決める
- [ ] 提案Bを入れた場合、同じディレクトリに2本稼働中で片方だけが呼び手のセッションのものなら、hook はそちらへ書く（§5-3 [18]）。2本とも呼び手のものでない、または絞り込めない場合は、いままでどおり「決められない」で何も書かない
- [ ] `autofinish` は本計画で1行も変えていない（`running_missions_here()` は無傷）。`check_autofinish.py` の [1]〜[9] が全部通る
- [ ] `reset` / `remove` / `status` / `projects` / `history` / `lang` に照合が入っていない（§2-0 で採らない方針。§7 で再確認）
- [ ] `phase` の値は `"standby"` / `"running"` / `"done"` の3つのままで、`interruptedBy` は `mission` の別鍵として存在する（`dashlib.PHASES` に変更が無いこと自体を grep で確認する）
- [ ] 押し出された記録（`interruptedBy` 付き）の `phase` は `"running"` のまま。`finishedAt` は書かれていない
- [ ] 完了済みの記録が退避されたとき（通常の `finish` 後の `start`）には `interruptedBy` が付かない
- [ ] 押し出す側（`start` に `--force`）・押し出される側（過去に居たセッション）のどちらから見ても、退避された記録は `history/` から今までどおり読める（`cmd_history` が壊れていない）
- [ ] 画面（`public/index.html`）で、`interruptedBy` を持つ履歴タブにマウスを乗せると、`phase.interrupted_tip`（新設）がいつ・何に割り込まれたかを表示する。**「未完」の札の文字自体は変わっていない**（§2-4-4 のとおり `phaseText()` は触っていない）
- [ ] `interruptedBy` を持たない履歴タブ（従来どおりの締め忘れ）は、今までどおり `phase.unfinished_tip` が出る（新設した分岐で既存の表示が塗り替わっていない）
- [ ] `python check_i18n.py` が 0 で終わる（`owner_conflict_message()` / `add_force_arg()` で増えた原文の ja/zh/ko 訳が `i18n_data_update.py` に揃っている）
- [ ] `public/i18n.js` の4言語ブロック（en/ja/zh/ko）すべてに `phase.interrupted_tip` があり、`{when}`/`{title}` の差し込み名が4言語とも一致している
- [ ] `記録の奪い合いを止める計画.md` が対象にした6コード+10文書ファイル以外に、`git diff --stat` で意図しない変更が出ていない
- [ ] `public/index.html` の改行が LF のまま（`git diff --stat` で無関係な行が動いていない。§4 の注意どおり、他の改修計画と時間を分けて編集したことも合わせて確認する）

---

## 7. やらないこと

§2-0 の「採らない」表は**設計として選ばなかった代案**を並べたもので、ここは重複させない。ここに並べるのは、**この計画の実装が触れる関数・ファイルの外側**という線引きである。

- **`reset` / `remove` に照合を入れない。** §2-0 で明記済みの決定で、実装のどの手番でも `cmd_reset()`（`update_state.py:1686`）・`cmd_remove()`（`:1723`）には触らない。`cmd_reset()` が `other_session_owns()` を経由しないことは§8で事実として残す。

  **`demo`（§2-9）を直したことで、この線引きの理由に1つ書き足す。** 当初の理由は「どちらも人が明示的に打つ破壊的コマンドで、事故の経路ではない」だったが、`demo` も「人が明示的に打つコマンド」でありながら、読み合わせで実際に事故の経路になっていた（§2-9・§8）。両者を区別できる点は、**コマンド名が破壊を予告しているかどうか**である。`reset` / `remove` はコマンド名そのものが「消す」と言っており、打った本人はその場で何が起きるかを承知している——万一事故が起きるとしても「別セッションの記録を消してしまった」という**対象の取り違え**であって、「表示テストのつもりが記録を消えていた」という**副作用の見落とし**ではない。`demo` は名前が「表示テスト」としか言っておらず、無条件上書きという副作用は名前から読み取れなかった。この差が、今回 `demo` だけを先に直し、`reset` / `remove` を対象外のままにしている理由である。**対象の取り違えというリスク自体は `reset` / `remove` に残ったままであり、無いことにはしない**（§8 に事実として記載）。
- **`server.py` / `livefeed.py` を触らない。** どちらもこの計画の対象範囲（`update_state.py` から書かれる `mission.sessionId` / `mission.interruptedBy` と、それを読む `dashlib._build_state()`）の外側にある。`server.py:186-187` の `_phase_of` は §1-7 の丸めの一部として**参照**はするが、コードは変えない。
- **過去にすでに `history/` へ押し出されている記録へ、遡って `interruptedBy` を書き込まない。** `_stamp_interrupted()`（§2-4-1）は `archive_current_run()` が退避する**その場**でしか呼ばれない。マイグレーションスクリプトは作らない――「未完」のまま残っている過去の記録は、直せない実測でない値（§2-0 の「採らない」表）を新しく作らずに、そのまま「未完」で残す。
- **`autofinish` に `--force` を足さない。** §2-8 で明記済み。`cmd_autofinish` の `argparse.Namespace` に `force` 属性を用意する1行だけを足し、サブコマンドとしての `--force` 引数は増やさない。
- **`locks/` の仕組み（待ち上限・段階的バックオフ）を変えない。** `STATE_LOCK_WAIT_SEC` / `STATE_LOCK_STALE_SEC`（`dashlib.py:1955-1956`）に触らない。今回の事故は9分離れた逐次操作で、ロックとは無関係（§1-3 末尾）。
- **`check_wiring.py` / `check_lang.py` / `check_livefeed.py` / `check_agents.py` / `check_autoreg.py` の中身は変えない。** §5-4 のとおり「通ることを確認する」対象であって、書き換える対象ではない。
- **`dash.py` / `dash.cmd` / `dash` ラッパーを変えない。** §4 で確認したとおり `update_state.main()` へ委譲するだけなので、変える理由が無い。
- **サブエージェントの孫世代（さらにその配下）専用の追加照合を作らない。** §1-5 の実測は「親のセッションIDをそのまま受け継ぐ」ことを示しており、それが何世代続いても同じ値のはずである。世代ごとに別の識別子を通す仕組みは、確かめていない前提の上に作ることになるので作らない。
- **環境変数で保護を丸ごと無効化する仕組みを作らない。** §2-0 で明記済み。`--force` はコマンドごとに明示する形のみ。
- **`AGENT_DASHBOARD_PROJECT`（環境変数版の `--project`）の挙動は変えない。** `owner_conflict_message()` が案内する `--project` はコマンド行の引数としてのみ想定し、環境変数経由での自動解決は対象外にする。
- **新しいサブコマンドを作らない。** 「押し出しを取り消す」「持ち主を付け替える」のような復旧用コマンドは用意しない。取れる手段は `--force` で書く（新しく始める）か、押し出された記録は「未完」のまま履歴に残す（諦める）かの二択のままにする。
- **バージョンを上げるかどうかは、この計画では決めない。** `VERSION` / `extension/package.json` / `extension/CHANGELOG.md` は対象外。上げる場合の手順は別途、既存の慣習（`拡大率と区画の改修計画.md` §8 が触れている手順）に従う。
- **画面（`public/index.html`）の見た目を、§2-4-4 の1行を超えて変えない。** タブの札の文字（「未完」）そのもの、`normalizeTab` / `phaseText` / `PHASES` の丸めは§3で明記したとおり触らない。

---

## 8. 未確認のこと（推測で埋めていない部分）

- **`hook` から呼ばれたときに `CLAUDE_CODE_SESSION_ID` が渡るかは未測定。** `update_state.py` の `cmd_hook`（`:1057`）→ `_hook_main()`（`:1073`）は標準入力の JSON（`payload`）から `tool_name` / `tool_use_id` / `hook_event_name` / `cwd` は読んでいるが、**`session_id` は一度も読んでいない**（`_hook_main()` 全体・`_hook_target()` を grep して確認済み）。一方、同じリポジトリの `changelog_cli.py` は PreToolUse・Stop の両方で `data.get("session_id")`（`:207`, `:287`）を読んでおり、そこには値が入っている前提でコードが書かれている＝**hook の payload には `session_id` というキーで渡ってきている**ことがこのリポジトリの別の場所で裏付けられている。これを `_hook_target()`（提案B、§2-6）の絞り込みで**環境変数 `CLAUDE_CODE_SESSION_ID` の代わりに**使えるかどうかは確かめていない。渡ってきていれば `current_session()` より信頼できる第2の経路になりうるが、**本計画の範囲では手を付けない**（§2-6 のコメントに同旨のメモがある）。
- **`demo` が持ち主照合にも退避にも掛かっていなかったことは、当初の設計（§1〜§2-8）を書いた時点では気づいていなかった。** §2-2（`add`/`done`/`finish`/`log`）・§2-3（`start`）を実装したあとの読み合わせで、レビュアーが実際に `cmd_demo`（当時の `update_state.py:1480〜1641行目付近`）を読み、`dashlib.write_state(slug, state)` と `dashlib.set_current(slug)` を直接呼ぶだけで `other_session_owns()` の照合も `archive_current_run()` による退避も一切通らないことを実測して見つけた（§2-9）。§1（背景と現状）・§3（壊してはいけないもの）の記述は、いずれも `add` / `done` / `finish` / `log` / `start` の5コマンドだけを対象に事故を再現しており、`demo` は当初の監査対象に入っていなかった——**「最初から想定していた穴」ではなく、実装が一巡したあとの読み合わせで初めて見つかった漏れである。** §2-9 で `demo` にも同じ規則（`other_session_owns()` の照合・`--force`・`archive_current_run()` による退避・`under_state_lock`）を入れたことで、書き込み系の6コマンド（`start`/`add`/`done`/`finish`/`log`/`demo`）はすべて同じ規則で揃った。**この発見の経緯自体が示すとおり、「照合が要るコマンドの一覧」を最初に決め打ちすることには限界がある。** `hook`（§3の6番目で構造的に対象外と説明済み）・`reset`・`remove`（§7、上の追記を参照）以外に同種の漏れが無いかは、この計画のレビューでは全数を再確認していない。
- **`reset` が他セッションの稼働中の記録を消せることは事実として残す。** `cmd_reset()`（`update_state.py:1496-1528`）は `dashlib.write_state(slug, dashlib.empty_state(project))` を無条件に呼ぶだけで、`other_session_owns()` は一切経由しない。しかも `archive_current_run()` を通らないので、**退避すらせずにその場で空にする**（`cmd_reset` 自身が印字する `"The current record is emptied without being archived"` のとおり）。§2-0 の「採らない」表のとおり、今回は照合を入れない判断をしたが、**`start` より一段階乱暴な壊し方ができる操作が無防備なままであること自体は、直っていない事実として残しておく。**
- **セッションIDの実測は1回だけ。** §1-5 の実測（サブエージェントは親の `CLAUDE_CODE_SESSION_ID` を受け継ぐ）は Claude Code 2.1.259 / `CLAUDE_CODE_ENTRYPOINT=claude-vscode` / Windows 11 で、2026-09-03 に1回測っただけである。別のエントリポイント（`claude-cli` 等）・別の OS・将来のバージョンで同じ挙動になるかは確かめていない。**この前提が崩れる環境がもし存在すれば、§3 の1番目に書いたとおり、その環境ではサブエージェントからの `add`/`done`/`finish`/`log` が軒並み拒否される。** 導入後にこの兆候（サブエージェントが打ったはずのコマンドが理由不明で失敗する）が出た場合は、まずこの前提を疑うこと。
