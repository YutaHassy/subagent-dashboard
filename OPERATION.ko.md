# 운용 절차서(Claude용)

[English](OPERATION.md) | [日本語](OPERATION.ja.md) | [中文](OPERATION.zh.md) | **한국어**

서브에이전트를 시작하거나 완료시킬 때마다 이 절차대로 상태를 갱신한다.
**상태 변경은 반드시 `update_state.py` 를 거쳐서 하고, `state.json` 을 직접 편집하지 말 것.**
시각·세대·로그·요약 집계는 모두 CLI 가 자동으로 처리하므로, 손으로 쓰면 반드시 망가진다.

## 이 파일 안의 표기에 대하여

명령 예시는 `dash` 로 줄여서 적는다. 실체는 **이 파일과 같은 디렉터리**에 있는
런처(Windows 는 `dash.cmd`, macOS / Linux 는 `./dash`)이며, 다음 세 가지는 모두 같은 동작을 한다.

```bash
dash add --id SCOUT-A ...                       # 런처를 거쳐서
python <이 디렉터리>/dash.py add ...             # 런처의 본체를 직접
python <이 디렉터리>/update_state.py add         # CLI 를 직접
```

**실행할 때 쓰는 전체 경로가 붙은 명령 예시는 각 AI 코딩 CLI 의 운용 규칙 파일에 기록되어 있다.**
그쪽을 그대로 쓸 것(`dash install` 이 환경에 맞춰 생성한 것이다).
이 디렉터리의 실제 위치는 `dash projects` 출력의 맨 앞에도 표시된다.

## 대상 프로젝트는 자동으로 정해진다

**현재 디렉터리로부터 대상 프로젝트가 자동으로 판정된다.**
어느 프로젝트에서 작업하든 같은 명령을 그대로 실행하면 된다.
상태는 `missions/<slug>/` 아래에 프로젝트별로 분리되어 저장된다.

slug 는 「디렉터리 이름 + 전체 경로의 해시 6자리」이므로,
다른 위치에 있는 같은 이름의 디렉터리와 섞이는 일은 없다.

---

## 0. 시작(한 번만·전체 프로젝트 공통)

```bash
dash serve
```

표시된 URL 을 브라우저로 연다. **서버는 하나만 띄우면 된다.**
어느 프로젝트에서 작업하든 이 하나의 화면에 나온다.
포트가 사용 중이면 자동으로 다음 번호로 올라가므로, 시작 로그에 나온 URL 을 쓴다.

**어느 팀을 화면에 띄울지는 서버가 정한다.** 전환 조작은 없다.

| 화면 | 언제 |
| --- | --- |
| 대기 화면 | 탭이 한 장도 없을 때(아직 한 번도 `start` 하지 않음, 기록을 전부 지움) |
| 가동 중인 팀 | `start` 한 순간부터 |
| 완료된 팀 | `finish` 이후에도 그대로 계속 표시된다 |
| 다음 팀 | 다음 `start` 로 교체된다. 이전 팀은 화면에서 사라진다 |
| **여러 팀을 세로로 쌓기** | 병렬로 가동 중인 팀은 전부 동시에 표시된다(시작이 최근인 순) |

화면에 띄우는 것은 「**가동 중인 팀 전부 ＋ `missions/.current` 가 가리키는 팀**」이다. `.current` 를
바꾸는 것은 **`start`(그리고 `demo`)뿐**이며, `done` 이나 `finish` 로는 움직이지 않는다. 그래서
완료된 화면이 제멋대로 다른 프로젝트로 넘어가는 일은 없다.

여기에 더해, **가동 중인 팀이 움직이기 시작한 뒤에 완료된 팀**도 남긴다. 병렬로 돌린
둘 중 한쪽이 먼저 `finish` 했을 때, 그 팀은 이미 `.current` 가 아니므로 이 규칙이 없으면
완료된 순간에 사라져서 아무도 완료 보고를 읽을 수 없다. 반대로 순서대로 작업하고 있을 뿐인 경우
(이전 팀이 끝난 뒤에 다음을 `start`)에는 이전 팀의 완료가 다음의 시작보다 앞서므로,
그쪽은 요건대로 화면에서 내려간다.

`running` 인 채로 방치된 기록을 끝없이 계속 표시하지 않도록 **기본 3시간의 시간 창**이 있다.
`state.json` 의 갱신이 그보다 오래된 `running` 은 표시하지 않는다. 창은 환경 변수
`AGENT_DASHBOARD_ACTIVE_WINDOW`(초)로 바꿀 수 있다. 오랫동안 전혀 통지가 오지 않는
미션을 다룬다면 늘릴 것.

**아직 한 번도 `start` 하지 않은 빈 틀(「(미션 미시작)」)은, 달리 고를 수 있는 탭이
한 장도 없을 때만 표시한다.** `dash reset` 직후나 이름만 만들어진 프로젝트가 여기에 해당한다.
눌러도 아무것도 나오지 않는 탭이고 다음에 `start` 할 때까지 사라지지 않으므로, 내버려 두면 탭 줄에 쌓인다.
달리 아무것도 없을 때까지 숨기면 프로젝트의 존재 자체가 화면에서 사라지므로, 그럴 때는 남긴다
(미시작인 것끼리는 「다른 탭」으로 세지 않는다. 세면 2건만 있어도 둘 다 사라져서 대기 화면으로 떨어진다).
`start` 직후라 유닛이 0개인 것은 `running` 이므로 이 규칙에 걸리지 않는다.

이전 미션의 기록은 `missions/<slug>/history/<runId>/` 로 옮겨진다. **화면에서 내려가도
사라지지 않는다.** 탭에서 고르면 나중에 내용을 다시 볼 수 있다(목록은 `dash history`).

보관은 프로젝트별로 최근 20건. 넘친 만큼은 오래된 것부터 `trash/` 로 옮겨진다. 건수는 환경 변수
`AGENT_DASHBOARD_HISTORY_KEEP` 로 바꿀 수 있다(`0` 으로 하면 옮기지 않고 종전대로 덮어쓰기가 된다).
`0` 으로 해도 이미 쌓여 있는 `history/` 는 청소하지 않는다. 설정을 잠깐 바꿨을 뿐인데
과거 기록이 전부 휴지통으로 가는 것을 피하기 위해서다.

---

## 0.05. VSCode 확장에서 시작(권장. 사람이 하는 조작)

사람이 VSCode 를 쓰고 있다면 이것이 가장 손이 적게 간다. Claude 쪽에서 뭔가 할 필요는 없다.

```bash
dash ext install     # 한 번만. 그 뒤 VSCode 를 다시 읽어들인다
```

이후에는 왼쪽 끝 활동 표시줄에 있는 로봇 아이콘을 누르면 에디터 탭에 나온다.
명령 팔레트의 「Subagent Dashboard: 탭으로 열기」나 상태 표시줄의 🤖 Subagent Dashboard 도 마찬가지다.
왼쪽에 길쭉하게 두고 싶을 때는 설정 `agentDashboard.sidebarBehavior` 를 `embed` 로 한다.

남에게 나눠 줄 때는 `dash ext package`. `dist/` 에 나오는 `.vsix` 와 안내서를 메일에 첨부한다.
확장에는 대시보드 본체가 함께 들어 있어서, 받은 쪽에서
`~/.claude/agent-dashboard` 로 배치된다(처음에 확인을 받는다).

**Claude 가 알아 두어야 할 점만:**

- 확장은 서버의 생사를 `GET /api/env` 로 판정하고, `toolRoot` 가 일치했을 때만 「우리 서버」로 간주한다
- 확장은 빈 포트를 스스로 정한 다음 `dash.py serve --port <번호> --no-retry` 로 시작한다. **서버 쪽의 번호 올리기를 막아 두었으므로, 확장이 파악하고 있는 번호와 실제 번호는 반드시 일치한다**
- 확장이 시작한 서버는 VSCode 종료 시에 멈춘다. 터미널에서 `dash serve` 한 서버에는 손대지 않는다
- 따라서 **Claude 가 `dash serve` 를 띄운 뒤에 사람이 확장에서 열어도 중복 실행이 되지 않는다**(확장이 기존 것을 재사용한다)

자세한 내용은 `EXTENSION_PLAN.md`, 확장 사용법은 `extension/README.md` 를 볼 것.

---

## 0.1. 어느 프로젝트에서든 시작(Global Launch)

확장을 설치하지 않은 경우의 경로. 임의의 프로젝트 폴더에서 `Ctrl+Shift+D` 로 대시보드를 시작했을 때의 구조.

### 용도

- 여러 프로젝트를 동시에 진행하고 있다
- agent-dashboard 폴더를 매번 다시 여는 것이 번거롭다
- VSCode 의 키보드 단축키로 빠르게 접근하고 싶다

### 처리 흐름

```text
사용자가 Ctrl+Shift+D 를 누른다
        │
        ▼
  VSCode 가 키 바인딩을 실행
        │
        ▼
  python open_dashboard.py
        │
        ▼
  환경 변수 AGENT_DASHBOARD_HOME 에서
  agent-dashboard 의 위치를 검출
        │
        ▼
  python dash.py serve 를 시작
        │
        ▼
  http://127.0.0.1:3939 로 접속
  ↓ 헬스 체크(최대 5초까지 대기)
        │
        ▼
  브라우저 / SimpleWebService 로 연다
```

### 기술 상세

#### 위치의 검출

`install.py` 가 실행될 때, agent-dashboard 의 경로를 다음 우선순위로 검출·기록합니다:

1. **환경 변수 `AGENT_DASHBOARD_HOME`** 이 이미 설정되어 있음 → 그 경로를 사용
2. **환경 변수에 미설정** → `install.py` 를 실행한 디렉터리를 검출
3. **운용 규칙 파일에 삽입** → 에이전트가 자동 실행하는 명령에 전체 경로가 포함됨

#### 서버의 시작

`open_dashboard.py` 는 다음 순서로 서버를 시작합니다:

```python
# 1. agent-dashboard 의 위치를 확정(환경 변수의 설정 순서대로 탐색)
dashboard_home = AGENT_DASHBOARD_HOME or DEFAULT_PATH

# 2. 서버가 이미 시작되어 있지 않은지 확인
# http://127.0.0.1:3939 에 접속할 수 있는지 시도

if 서버가_응답하지_않음:
    # 3. 새 서버 프로세스를 시작
    subprocess.Popen([
        "python",
        f"{dashboard_home}/dash.py",
        "serve",
        "--detach"  # 백그라운드 시작
    ])

    # 4. 헬스 체크(최대 5초 대기)
    for i in range(50):  # 0.1초 x 50 = 5초
        if http://127.0.0.1:3939 가_응답:
            break
        sleep(0.1)

# 5. 브라우저를 연다
webbrowser.open("http://127.0.0.1:3939")
```

#### 포트 충돌 시의 처리

여러 사용자가 같은 머신에서 작업하고 있는 경우:

1. 사용자A 가 `Ctrl+Shift+D` → 포트 3939 에서 시작
2. 사용자B 가 `Ctrl+Shift+D` → 포트 3939 는 사용 중
3. 사용자B 의 서버는 3940 에서 시작
4. 각 사용자는 자신의 설정된 포트에 접속

**대시보드 안에 표시되므로, URL 이 바뀌어도 동작에 지장이 없습니다.**

#### IDE 통합의 예(VSCode)

`install.py` 가 `keybindings.json` 에 등록하는 내용:

```json
{
  "key": "ctrl+shift+d",
  "command": "agentDashboard.open"
}
```

확장이 가진 명령을 직접 호출합니다. 경로에도 터미널에도 의존하지 않으므로,
대시보드를 둔 위치가 바뀌어도 망가지지 않습니다. **확장을 설치하지 않으면
이 키는 아무 일도 일으키지 않습니다**(명령이 존재하지 않기 때문).

기록 위치는 VSCode 가 사용자 설정으로 실제로 읽는 곳입니다.

| OS | 위치 |
| --- | --- |
| Windows | `%APPDATA%\Code\User\keybindings.json` |
| macOS | `~/Library/Application Support/Code/User/keybindings.json` |
| Linux | `$XDG_CONFIG_HOME/Code/User/keybindings.json`(기본값 `~/.config/Code/User/`) |

`Ctrl+Shift+D` 는 VSCode 표준에서 「실행 및 디버그」에 할당되어 있습니다.
확장의 `contributes.keybindings` 로는 표준 할당과의 우선순위가 환경에 따라 달라지므로,
확실히 이기는 사용자 설정 쪽에 기록하고 있습니다.

> **0.2.4 까지의 버전에 대하여**
> 기록 위치를 `~/.vscode/keybindings.json` 으로 하고 있었습니다. 그곳은 확장을 두는 곳이라
> VSCode 는 키 바인딩을 읽지 않습니다. 즉 `Ctrl+Shift+D` 는 듣지 않고 있었습니다.
> 0.2.5 의 `install.py` 는 이 옛 위치에 자신이 기록한 항목이 있으면 정리합니다
> (사용자가 직접 기록한 항목에는 손대지 않습니다).

---

## 0.2. 지원하는 AI 코딩 CLI 와 설정

초기 설정(`install.py`)은 이 도구가 발견하는 모든 AI 코딩 CLI 에 운용 규칙을 쓰입니다.

### 지원하는 CLI 들

| CLI | 운용 규칙이 쓰이는 위치 |
| --- | --- |
| Claude Code | `~/.claude/CLAUDE.md` |
| Codex CLI | `~/.codex/AGENTS.md` |
| Gemini CLI | `~/.gemini/GEMINI.md` |
| GitHub Copilot CLI | `~/.copilot/copilot-instructions.md` |
| opencode | `~/.config/opencode/AGENTS.md` |
| Amp | `~/.config/amp/AGENTS.md` |
| Cline | `~/Documents/Cline/Rules/subagent-dashboard.md` |
| Roo Code | `~/.roo/rules/subagent-dashboard.md` |
| Windsurf | `~/.codeium/windsurf/memories/global_rules.md` |
| Qwen Code | `~/.qwen/QWEN.md` |

**Cline 과 Roo Code 는 규칙 파일들을 폴더에서 읽으므로**, 이 도구는 사용자 설정 파일을 편집하는 대신 그 폴더에 전용 파일을 하나 떨어뜨립니다.

**Cursor 와 Aider 는 의도적으로 표에 없습니다.** 이 두 도구는 사용자 레벨의 자동 읽기 설정 파일이 없기 때문입니다(Cursor 는 저장소별 `AGENTS.md` 를, Aider 는 설정에 명시된 파일을 읽습니다). 저장소별 설정 파일을 가리키는 `--agent-file` 옵션으로 지정하세요.

### CLI 설정

특정 CLI 만 대상으로 하거나, 표에 없는 CLI 를 추가하려면 다음 명령을 씁니다.

```bash
python install.py --list-agents          # 발견된 CLI 들과 설정 상태를 표시
python install.py --agent codex          # 특정 CLI 만 대상으로 하기
python install.py --agent all            # 알려진 모든 CLI 에 쓰기 (설치 여부와 상관없이)
python install.py --agent-file <경로>    # 이 경로에도 쓰기 (새로운 CLI 나 사내용 도구)
```

`--agent-file` 로 추가한 경로는 `agents.json` 에 영구적으로 등록되어, 이후 기록의 갱신과 정리에 포함됩니다. 이 메커니즘으로 새로운 CLI 를 지원하면서 도구 버전 업그레이드를 기다릴 필요가 없습니다.

`agents.json` 은 수동으로도 편집할 수 있습니다. 기존 항목의 `key` 가 일치하면 내장 설정을 무시하므로, CLI 가 운용 규칙 파일 위치를 바꾼 경우 도구 업그레이드를 기다리지 않고 로컬에서 고칠 수 있습니다.

각 항목은 다음 필드를 가집니다:
- `key` — `--agent` 에서 쓰는 짧은 ID
- `label` — 화면에 표시되는 이름
- `home_env` — 폴더 위치를 바꾸는 환경 변수 (선택사항)
- `home` — 폴더 경로 (`~` 로 시작할 수 있음)
- `file` — 파일 이름 (하위 폴더를 포함할 수 있음)

**초기 설정 후 새로운 AI 코딩 CLI 를 설치한 경우, `python install.py` 를 다시 실행해야 합니다.** 설정 시점에 없던 CLI 에는 규칙이 쓰이지 않아서, 그 CLI 로 시작한 서브에이전트는 대시보드에 나타나지 않습니다. 이 상태는 `start` 또는 서버 시작 시에 경고로 나타나며, `python diagnose.py` 에서도 확인할 수 있습니다.

---

## 1. 미션 시작 시

서브에이전트를 한 대라도 시작하기 **전에**, 작업 중인 프로젝트의 디렉터리에서 한 번만 실행한다.
그때까지의 내용은 `history/<runId>/` 로 옮겨지고, 지휘탑만 있는 상태에서 시작한다
(다른 프로젝트에는 영향을 주지 않는다). **옮겨지므로 이전 미션은 나중에 다시 볼 수 있다.**
이전 미션에서 남은 손자 에이전트의 자가 보고 파일(`agents/*.json`)은 옮겨지는 곳으로 함께 이동한다.
넘기지 않는 이유는, 완료 통지가 오지 않은 손자 에이전트가 이후의 모든 미션에서
「가동 중」인 채로 계속 늘어서 있게 되기 때문이다.

옮기기에 실패해도 `start` 는 멈추지 않는다(경고를 내고 계속한다). 기록이 남아 있는데
시작할 수 없는 쪽이 더 곤란하기 때문이다.

```bash
dash start --title "리팩터링 영향 범위 조사" --model claude-opus-5
```

**같은 디렉터리에서 두 번째 미션을 동시에 돌릴 때, 미리 `--project` 로 기록 위치를
나눠 두면 충돌 자체가 일어나지 않는다.** 나누는 것을 잊더라도, 가동 중인 기록을 다른
세션이 밀어내려 하면 이제는 도구 쪽이 거부하고 멈춘다(그대로 붙여 넣을 수 있는
`--project` 안내가 함께 나온다). 다만 주인 표시가 없는 환경에서는 이 보호가 작동하지
않아 지금까지처럼 밀려나므로, 그 경우만은 사전에 나누는 것이 유일한 대책으로 남는다.
→ 「6.1. 같은 디렉터리에서 두 개 이상을 동시에 돌릴 때」

---

## 2. 서브에이전트를 시작한 직후

Agent 도구를 호출한 **직후에** 실행한다. 나중에 한꺼번에 적는 것이 아니라, 시작할 때마다 한 번씩.

```bash
dash add --id SCOUT-A --name "정찰A" --model claude-sonnet-5 --mission "src/ 아래의 API 호출 지점을 모두 찾아낸다"
```

| 옵션 | 생략 시 | 비고 |
| --- | --- | --- |
| `--id` | **필수** | `SCOUT-A` `A-1` 같은 짧은 식별자. 로그와 카드에 나온다 |
| `--name` | id 와 동일 | 화면에 크게 나오는 표시명 |
| `--parent` | `COMMAND` | 부모의 ID. 손자 에이전트를 만들게 한 경우에는 부모 에이전트의 ID 를 지정 |
| `--model` | 빈 값(화면에는 「알 수 없음」이라고 나온다) | 실제로 사용한 모델 ID |
| `--mission` | 빈 값 | 임무 내용. 카드에 두 줄로 표시된다 |
| `--status` | `running` | 출동 대기 상태로 늘어놓기만 할 거라면 `standby` |
| `--project` | 현재 디렉터리 | 다른 프로젝트를 대상으로 하고 싶을 때만 지정 |

**자유 기술은 한국어로 쓴다**(`--title` / `--name` / `--mission` / `--headline`). 적은 그대로 기록되고,
기록된 그대로 화면에 나온다——나중에 번역되지 않는다. 대화의 언어가 아니라 이 절차서의 언어에 맞출 것.
그 언어를 바꾸려면 `dash lang <en|ja|zh|ko>` 를 실행한다. **각 CLI 의 운용 규칙 파일의 기술도 그 자리에서 새 언어로
다시 쓰이므로**, 설정한 언어가 그대로 팀을 짜는 언어가 된다(반영되는 것은 다음 세션부터 —— 운용 규칙은
시작할 때 읽힌다). **이미 있는 기록은 쓰였을 때 그대로**이며, 나중에 다시 번역되지 않는다.

**이미 가동 중인 세션에는, 이 절차서로 언어가 바뀌었음을 알릴 수 없다.** 그래서 `start` 는 매번
「자유 기술은 이 언어로 쓴다」를 한 줄 출력하고, `start` / `add` / `done` / `finish` 는 넘겨받은 기술이
그 언어로 쓰이지 않은 것처럼 보일 때 경고한다. **이 절차서보다 명령의 출력을 믿을 것.** 출력은 지금
만들어진 것이고, 이 절차서는 마지막으로 언어를 설정했을 때 쓰인 것이기 때문이다. 경고가 나와도 쓰기는
멈추지 않는다. `--name` / `--mission` / `--headline` 을 고치려면 같은 `--id` 로 같은 명령을 다시 실행한다
(측정값은 유지된다). 미션 제목은 나중에 고칠 수 없다.

세대는 `--parent` 에서 자동으로 산출되므로 지정하지 않는다.
**「보고 대기」도 `--parent` 에서 자동으로 붙으므로**, 지휘탑이 대기에 들어갔음을 신고할 필요는 없다
(`--parent` 를 올바르게 지정하는 것이 그대로 보고 대기의 정확도가 된다).

**여러 대를 동시에 시작한 경우에는 대수만큼 `add` 를 실행한다.** 한 번에 묶는 방법은 없다.

---

## 3. 완료 통지를 받으면

```bash
dash done --id SCOUT-A --sec 42 --tokens 18400 --tools 11 --headline "호출 지점을 7개 파일 23건으로 특정"
```

| 옵션 | 생략 시 | 비고 |
| --- | --- | --- |
| `--id` | **필수** | |
| `--sec` | `startedAt` 부터의 실제 경과 초 | 통지에 소요 시간이 있으면 그것을 지정. 없으면 생략 |
| `--tokens` | `null` → 화면은 「—」 | **통지에 없으면 반드시 생략한다** |
| `--tools` | `null` → 화면은 「—」 | **통지에 없으면 반드시 생략한다** |
| `--headline` | 빈 값 | 결과의 한 줄 요약. 카드 아래쪽에 녹색 글자로 나온다 |

### 실측값에 대한 절대 규칙

**완료 통지에 포함되어 있지 않던 수치는 추정하지 말고 생략한다.** 생략하면 `null` 이 들어가고,
화면은 「—」로 표시한다. 그것이 올바른 상태이지, 메워야 할 결손이 아니다.
「대충 2만 토큰쯤 되겠지」라고 `--tokens 20000` 이라고 적는 것은
이 대시보드의 목적(실제로 무슨 일이 일어났는지를 본다)을 망가뜨리는 행위이므로 절대 하지 않는다.

**지휘탑(command post)의 모델도 같은 원칙을 따른다.** `dash start` 에서 `--model` 을 지정하지 않으면 화면에 「알 수 없음」으로 표시된다(숫자 칸의 「—」와는 다른 표시다).

---

## 4. 전원 완료되면

```bash
dash finish --headline "영향 범위를 특정하고 수정 대상을 12개 파일로 확정"
```

- 단계가 「완료」가 되고 요약이 나온다. **화면은 이대로 완료 상태로 남는다**(다음 `start` 까지)
- 유닛 수·총 토큰·소요 시간은 CLI 가 집계한다(`--headline` 이외에는 넘길 필요가 없다)
- 총 토큰은 `--tokens` 를 넘긴 유닛의 합계다. 전 유닛이 미취득이면 「—」가 된다
- `standby` 인 채로 남아 있던 유닛은 「미출동 상태로 종료」로 완료 처리된다

### 마무리 누락에 대한 감시

`finish` 를 잊고 치지 않아도 **아무것도 망가지지 않고 아무것도 멈추지 않는다.** 그래서 알아챌 수 없다.
내버려 두면 화면에는 「가동 중」이라고 계속 표시되고, 다음 `start` 에서 가동 중인 채로 이력으로 흘러가
이후 계속 「미완」인 기록으로 남는다(**나중에 완료로 만드는 방법은 없다**).

마무리 누락이 일어나는 것은 언제나 마지막 한 대를 `done` 으로 만든 직후에 보고를 정리하는 작업으로
의식이 옮겨 가는 순간이므로, 그 지점을 포함한 네 곳에서 알린다. **어느 것도 고치지 않고 알리기만 한다.**
나중에 `finishedAt` 을 적으면 그것은 실측이 아니게 된다.

| 어디서 | 언제 나오나 | 나오는 것 |
| --- | --- | --- |
| `done` | 마지막 한 대를 완료로 만든 직후 | `★ 이로써 N 대 전원이 귀환했습니다. …` ＋ 그대로 붙여 넣을 수 있는 `finish` 줄 |
| `status` | 마무리 누락인 동안 계속 | 단계가 「가동 중(**전원 귀환 완료·미종료**)」／마지막 귀환으로부터의 경과 시간 |
| `start` | 마무리를 잊은 채 다음을 시작했을 때 | `⚠️ 옮겨진 「…」은 N 대 전원 귀환 완료였지만 finish 가 실행되지 않았습니다.` |
| 화면 | 마무리 누락인 동안 계속 | 미션 바와 탭의 표찰이 호박색으로 바뀌고, 「**전원 귀환·미종료**」「**미종료**」가 깜빡인다 |

「마무리 누락」이라고 판정하는 것은 다음 조건이 전부 갖춰졌을 때뿐이다.

- 미션이 `running`(`done` 은 마무리 완료, `standby` 는 아직 시작하지 않음)
- 서브에이전트가 **1대 이상** 있다(0대면 「시작만 했을 뿐」이라 마무리할 단계가 아니다)
- 그 서브에이전트가 **전원 `done`**(한 대라도 가동 중이면 아직 기다리는 중이다)

지휘탑은 세지 않는다. `finish` 를 칠 때까지 `running` 인 채인 것이 올바른 모습이므로,
수에 넣으면 조건이 영영 갖춰지지 않아 경고가 한 번도 나오지 않게 된다.

귀환하지 않은 유닛을 남긴 채 다음을 `start` 한 경우는 「마무리 누락」이 아니라 **도중 포기**이므로,
`start` 는 `⚠️ …가동 중인 채로 끝났습니다(귀환하지 않은 유닛이 있습니다)` 라고 구분해서 말한다.
이 경고는 **같은 세션에서 다시 친 경우**(자기 자신의 마무리 누락), **다른 세션의 기록을
`--force` 로 밀어낸 경우**, 주인 표시가 없는 기록을 밀어낸 경우에 나온다. 주인이 다른
기록을 `--force` 없이 밀어내려 한 경우에는, 이 경고보다 먼저 거부로 멈춘다.
→ 「6.1. 같은 디렉터리에서 두 개 이상을 동시에 돌릴 때」

---

## 4.1. 마감할 때 "기록에 없는 기체"를 새겨 넣기

`finish` 와 `autofinish` 는 미션을 완료로 만들기 **전에**, 그 순간 가동 중이지만 기록에는 없는 기체를 `state.json` 의 `orphans` 에 새겨 넣는다.

이 처리가 없으면 마감하는 순간 그 기체는 화면과 이력에서 동시에 사라진다(가동 중인 미션만 실측을 읽기 때문이다). 있었다는 사실 자체가 남지 않는다.

새겨 넣은 기체는 완료 후의 화면에서도 이력에서도 마감했을 때와 같은 자리에 남는다. 계통수에 놓여 있던 것은 계통수에, 자리가 정해지지 않았던 것은 아래 구역에 남는다. 이미 움직이지 않으므로 **완료된 모습**으로 나온다. 아래 구역의 설명문도, 새겨 넣은 것뿐일 때는 과거형 문구로 바뀐다(해당 키는 `live.bay_note_frozen`).

새겨 넣은 실측값(소요·토큰·도구 횟수)도 카드에 나온다.

**유닛(기체) 수에는 세지 않는다.** 마감한 뒤에 다시 만들지 않는다.

`finish` / `autofinish` 의 출력에도 한 줄이 늘어난다:

```text
  기록에 없는 기체          {n}대 (당시 모습 그대로 기록에 새겼습니다)
```

---

## 4.2. 세션 종료 시 자동으로 마감하기(autofinish)

```bash
dash autofinish
dash autofinish --project <이름>
dash autofinish --headline "..."
```

**용도는 SessionEnd hook 에서 호출하는 것이다.** 세션이 끝나면 미션을 자동으로 마감한다. 마무리 누락은 이 도구에서 **나중에 고칠 수 없는 유일한 고장**이다 — 화면은 계속 「가동 중」이라고 나오고, 다음에 `start` 한 시점에 그 기록은 가동 중인 채로 이력으로 밀려나며, 그 뒤로는 완료로 만들 방법이 없다. 재촉은 이미 자리를 뜬 사람에게는 닿지 않으므로, 사람이 아니라 세션의 끝에 묶었다.

- **가동 중인 미션이 없으면 아무것도 출력하지 않고 끝난다.**
- **`--project` 를 붙이지 않으면, 그 디렉터리에서 가동 중인 미션을 전부 마감한다.** `--project` 로 기록 위치를 나눠서 병렬로 돌리던 쪽도 포함한다. **다른 폴더의 미션은 끌어들이지 않는다**(`project.path` 가 현재 디렉터리와 일치하는 것만).
- `--project <이름>` 을 명시하면 그 하나만.
- `--headline` 을 생략하면 「세션 종료로 자동으로 마감했습니다」에 해당하는 문구가 들어간다.
- 잠금은 하나씩 잡는다.
- **다른 세션이 연 미션은 마감하지 않는다.** 같은 폴더에서 Claude Code 를 두 개 띄우는 일은 흔한데, 실제로 **한쪽 세션이 끝났을 뿐인데 아직 작업 중이던 다른 쪽 미션이 완료 처리된** 사고가 있었다(2026-09-03 실기 확인). `start` 가 미션을 연 세션의 ID 를 `state.json` 의 `mission.sessionId` 에 기록해 두고(환경 변수 `CLAUDE_CODE_SESSION_ID` 에서 가져온다), `autofinish` 는 자신의 세션 ID 와 대조해서 **주인이 다른 미션은 마감하지 않고 조용히 건너뛴다.** 세션 ID 를 가져올 수 없는 환경에서는 지금까지처럼 마감한다(「모르면 마감하지 않는다」로 하면 이 장치 자체가 조용히 죽어 버린다 — 지나치게 닫히는 것은 다음 `start` 로 되돌릴 수 있지만 마감 누락은 되돌릴 수 없다는 저울질이다). 이 같은 규칙이 반대 방향(모를 때는 쓰는 쪽에 맡긴다)으로 `start` / `add` / `done` / `finish` / `log` / `demo` 에도 들어가 있다. → 「6.1. 같은 디렉터리에서 두 개 이상을 동시에 돌릴 때」
- **주의**: `/clear` 로도 SessionEnd 가 발생하므로, 작업 도중에 마감되는 일이 있을 수 있다. 지나치게 닫히는 것은 다음 `start` 로 다시 하면 되지만, **닫는 것을 놓치는 것은 고칠 수 없다.**
- 설정 예(`.claude/settings.local.json`):

```json
"SessionEnd": [
  {
    "matcher": "",
    "hooks": [
      { "type": "command", "command": "python 'C:\\path\\to\\update_state.py' autofinish; exit 0" }
    ]
  }
]
```

---

## 5. 보조 명령

실황을 한 줄만 추가한다. 판단이나 방침 전환을 로그에 남기고 싶을 때 쓴다.

```bash
dash log --who "지휘탑" --text "정찰B의 재실행을 판단"
```

현재 프로젝트의 상태를 표 형식으로 표시한다. 기록이 잘 되고 있는지 확인용.
손자 에이전트의 기록 위치의 절대 경로도 맨 끝에 나온다.

```bash
dash status
```

등록된 프로젝트를 목록으로 표시한다. `→` 가 현재 디렉터리의 대상이다.

```bash
dash projects
```

표시 테스트용 데이터를 넣는다(대기 중·가동 중·보고 대기·완료·손자 에이전트·실측값 없음이 전부 나온다).
겉모습을 조정하는 중에는 이것을 쓴다.

```bash
dash demo
```

초기화한다. `--purge` 를 붙이면 손자 에이전트의 자가 보고 파일도 삭제한다. 대상은 현재 프로젝트뿐이다.
`reset` 은 `missions/<slug>/` 를 남긴다. 지금 화면에 띄우고 있는 팀을 `reset` 하면 그대로
**대기 화면으로 돌아간다**(`.current` 는 움직이지 않으므로 다른 프로젝트로 넘어가지는 않는다).
`reset` 후에 남는 것은 「(미션 미시작)」인 틀뿐이므로, **달리 고를 수 있는 탭이 있으면
그 탭은 화면에 나오지 않는다**(0. 의 끝부분 참조). 과거 기록이 있는 경우 그쪽 탭은 남는다.

```bash
dash reset
dash reset --purge
```

기록째로 삭제한다. 기본값으로는 `trash/<slug>-<날짜시각>/` 로 옮길 뿐이므로,
폴더를 원래 위치로 되돌리면 복구할 수 있다. `--force` 를 붙였을 때만 완전히 삭제한다.
지금 화면에 띄우고 있는 팀을 지운 경우에는 다음 `start` 까지 대기 화면이 된다.

```bash
dash remove              # 대화식으로 확인한다
dash remove --yes        # 확인을 생략한다(비대화 환경에서는 --yes 가 필수)
dash remove --yes --force  # 휴지통으로 옮기지 않고 완전히 삭제한다
```

---

## 5.1. 기록을 지우기

`start` 할 때마다 탭이 하나씩 늘어나므로, 내버려 두면 쌓인다. 지우는 방법은 세 가지다.

| 조작 | 무슨 일이 일어나나 | 되돌릴 수 있나 |
| --- | --- | --- |
| 화면의 탭의 「✕」 | 그 탭 1건을 `trash/` 로 옮긴다 | 휴지통의 폴더를 되돌리면 복구할 수 있다 |
| 화면의 「완료된 기록 삭제」 | 「완료」라고 나오는 탭을 한꺼번에 `trash/` 로 옮긴다 | 위와 같음 |
| `dash remove` | `missions/<slug>/` 를 통째로(`history/` 까지) `trash/<slug>-<날짜시각>/` 로 옮긴다 | 위와 같음 |
| `dash remove --yes --force` | 완전히 삭제한다 | 되돌릴 수 없다 |

- 과거 기록은 `dash history` 로 목록을 볼 수 있다. 프로젝트의 목록은 `dash projects`.
- **화면에서 지울 수 있는 것은 「과거 기록」과 「완료된 현재 미션」이다.** 가동 중·대기 중인
  미션에는 「✕」가 붙지 않고, 요청이 도달해도 서버가 거절한다.
- 현재 미션(`state.json`)이 `history/` 로 옮겨지는 것은 같은 프로젝트에서 다음
  `start` 를 쳤을 때뿐이다. **한 번뿐인 미션은 완료해도 현재 자리에 계속 남으므로**,
  그대로도 화면에서 지울 수 있게 해 두었다.
- 그 프로젝트에 과거 기록이 한 건도 남지 않는 경우에는, 빈 폴더를 남기지 않고 프로젝트째
  `trash/` 로 옮긴다(결과는 `dash remove` 와 같다).
- 지금 화면에 띄우고 있는 프로젝트를 `dash remove` 로 지우면 `.current` 도 풀리고,
  다음 `start` 까지 대기 화면이 된다.
- 삭제는 `POST` 로만 받는다. `GET` 에는 405 를 반환하고 절대 지우지 않는다.
- 화면에서의 삭제에는 완전 삭제 경로가 없다. `permanent` 를 보내면 400 으로 거부된다.
  되돌릴 수 없는 조작은 CLI(`--force`)로만 할 수 있다.

---

## 6. 다른 프로젝트를 대상으로 하고 싶을 때

`--project` 에 slug(전방 일치라도 된다)를 넘긴다.

```bash
dash status --project learning
dash done --id SCOUT-A --project learning --headline "..."
```

전방 일치가 2건 이상에 걸리는 경우에는 오류가 나므로, `dash projects` 로 올바른 slug 를 확인한다.
환경 변수 `AGENT_DASHBOARD_PROJECT` 로도 지정할 수 있다(`--project` 쪽이 우선).

존재하지 않는 이름을 넘긴 경우에는 그 이름으로 새 프로젝트가 만들어진다.
즉 `--project 수동테스트` 처럼 디렉터리와 무관한 미션도 만들 수 있다.

---

## 6.1. 같은 디렉터리에서 두 개 이상을 동시에 돌릴 때

미션의 기록 위치는 **프로젝트(＝작업 디렉터리)마다 하나**밖에 없다. 2026-09-03, 같은
디렉터리에서 Claude Code 세션 두 개가 실제로 돌아가다가, 나중에 시작한 쪽이 아무도
알아채지 못한 채 먼저 시작한 쪽의 기록을 빼앗아 가는 사고가 실기에서 일어났다. 아래는
그 뒤에 들어간 보호 장치에 대한 설명이다.

**이제 `start` / `add` / `done` / `finish` / `log` / `demo` 는 쓰려는 기록의 주인
(`mission.sessionId`. `start` 를 친 세션의 `CLAUDE_CODE_SESSION_ID`)을 호출자 자신의
세션 ID 와 대조한다.** 정말로 다르다고 확인됐을 때만, 쓰지 않고 **종료 코드 1 로
거부한다**.

```
오류: 이 프로젝트의 기록은 지금 다른 세션이 사용하고 있습니다.
      대상: <프로젝트>
      여기 있는 기록은 「<현재 기록의 제목>」입니다(시작: <경과 시간> 전).
      여기서 start 하면 가동 중인 그 기록이 이력으로 밀려나고,
      이후 완료로 만들 수 없게 됩니다.
      아무것도 시작하지 않았습니다.
      자신의 작업에는 전용 기록을 준비하세요. 다음 4개 모두에 같은 --project 를 붙입니다:
        start --project "<고유한 이름>" --title "<작업명>" --model <자신의 모델ID>
      그래도 실행하려면 --force 를 붙이세요.
```

`add` / `done` / `finish` / `log` 가 거부될 때도 형태는 같고, 「밀려나고…」 부분이
「쓰면 기록이 섞입니다」로 바뀔 뿐이다. **`<고유한 이름>` 은 도구가 만들어서 안내한다**
(호출자의 세션 ID 로 정해지므로, 같은 세션에서 몇 번을 불러도 같은 이름이 된다).
고유한 이름을 스스로 생각할 필요도, 네 개 명령에 같은 이름을 애써 맞출 필요도 없다
——오류에 나온 줄을 그대로 네 번 붙여넣으면 된다.

- **그래도 밀어붙이고 싶을 때는 `--force` 를 붙인다.** `start` / `demo` 에 붙이면 지금까지처럼
  상대의 기록을 가동 중인 채로 이력으로 밀어낸다. `add` / `done` / `finish` / `log` 에
  붙이면 지금까지처럼 쓴다.
- **밀려난 기록에는 「무엇에·언제 밀려났는지」가 남는다**(`mission.interruptedBy`).
  이력에서 그 기록을 열면 나오는 「미완」 표찰에 마우스를 올리면 읽을 수 있다. **이
  표시는 `--force` 를 썼을 때만 붙는 것이 아니다.** 자기 자신의 마무리 누락(같은
  세션에서 이전 미션을 닫지 않고 다음 `start` 를 친 경우)에도 똑같이 붙는다——「미완」의
  이유를 나중에 추적할 수 있게 하는 장치이지, 다른 세션의 밀어내기 전용이 아니다.
- **서브에이전트는 이 거부에 걸리지 않는다.** 부모의 `CLAUDE_CODE_SESSION_ID` 를
  그대로 물려받으므로, 지휘탑이 `start` 한 미션에 서브에이전트가 `add` / `done` /
  `finish` / `log` 를 치는 것은 「같은 세션」으로 지금까지처럼 통과한다.
- **`demo` 는 거부되지 않을 때도 반드시 먼저 옮겨 둔다.** 화면 표시 테스트용 더미를
  넣는 명령으로, 다른 사람의 기록에 대해서는 위와 같은 거부가 걸린다. 거기에 더해
  **자기 세션의 기록에 대해서도, 덮어쓰기 전에 반드시 이력으로 옮겨 둔다**
  (`interruptedBy` 의 `by` 는 `demo`). 「더미를 넣는다」가 「그때까지의 기록을 지워도
  된다」는 뜻은 아니기 때문이다. 예전에는 대조도 옮기기도 없어서, 가동 중인 기록이
  이력에도 남지 않고 사라졌다.

### 보호가 작동하지 않을 때

**주인을 구별할 수 있는 것은, 밀어내는 쪽과 밀려나는 쪽 양쪽 모두에 `mission.sessionId`
가 갖춰져 있을 때뿐이다.** 다음 중 하나에 해당하면 도구는 「다른 세션인지」를 판단할 수
없어 **지금까지처럼 무방비로 통과시킨다**(「모르니까 멈춘다」로 하면 주인 표시가 없는
환경에서 도구 자체를 쓸 수 없게 되므로, 일부러 통과시키는 쪽으로 기울여 두었다).

- 기록이 `mission.sessionId` 를 갖고 있지 않다(0.9.1 이전에 `start` 된 기록)
- 이 단말에 `CLAUDE_CODE_SESSION_ID` 가 전달되지 않았다(hook 이 배선되지 않음, 사람이
  다른 단말에서 손으로 치는 운용 등)

이 경우에 밀려나는 일이 일어나 버렸을 때는 두 곳에서 알린다. **둘 다 고치지 않고
알리기만 하며**, 나중에 완료로 고치는 방법도 없다.

| 어디서 | 나오는 것 |
| --- | --- |
| `start` | `⚠️ 옮겨진 「…」은 가동 중인 채로 끝났습니다` 에 이어서, `--project` 로 나누는 절차 |
| `done` | 「현재 미션 『…』에 없습니다／최근 이력 『…』에 같은 ID 가 있습니다」＋ 여기서 `add` 하면 기록이 섞인다는 경고 |

`done` 의 안내는 **「아직 add 하지 않았다」인지 「밀려났다」인지를 구분해서 말한다.**
최근 5건의 이력에 같은 ID 가 있으면 밀려난 것으로 간주한다(`update_state.find_in_history`).
구분하지 않고 「먼저 add 하세요」라고만 말하면, 밀려난 쪽은 그 말대로 `add` 해서
**밀어낸 쪽 팀에 첫 번째의 유닛을 섞어 버린다.** 그래서 구분해서 말하고 있다. **이 경로는
자기 자신의 마무리 누락에도 통한다**(같은 세션에서의 쓰기는 애초에 거부 대상이 아니기
때문이다). 주인이 다른 경우의 거부와 헷갈리지 말 것.

### 사전에 `--project` 로 나눠 둬야 하는가

```bash
# 같은 디렉터리에서 병렬로 돌릴 때는, start 에 --project 로 고유한 이름을 붙여 나눈다
# (존재하지 않는 이름을 넘기면 그 이름으로 새 프로젝트가 만들어진다)
dash start --project PAVS_ER-issue51 --title "issue51 의 영향 범위 조사" --model claude-opus-5

# 그 미션의 add / done / finish 에도 전부 같은 --project 를 붙인다
dash add --project PAVS_ER-issue51 --id SCOUT-A --name "정찰A" --model claude-sonnet-5 --mission "..."
dash done --project PAVS_ER-issue51 --id SCOUT-A --headline "..."
dash finish --project PAVS_ER-issue51 --headline "..."
```

이 보호가 들어가기 전에는 이것을 **필수**라고 적어 두었다. 이유는 「사전에 나누지 않는
한, 나중에 다른 세션이 같은 디렉터리에서 시작한 순간 기록이 깨지고, 고칠 방법이 없다」는
것이었다. 하지만 이 말에는 무리가 있었다. **먼저 시작한 쪽은 나중에 다른 세션이 시작될
것을 예측할 수 없다.** 예측할 수 없는 일을 사전에 대비하라는 지시 자체가, 실제로 사고가
일어난 이유였다.

지금은 나누는 것을 잊어도 깨지지 않는다. **다른 세션과 충돌하는 쓰기는 그냥 거부될 뿐,
기록은 손상 없이 그대로 남는다.** 거부된 쪽은 그대로 붙여 넣을 수 있는 `--project` 안내를
읽고 다시 하면 된다. **그래서 더 이상 「필수」가 아니다.** 그래도 사전에 나눠 두는 의미는
남아 있다——충돌 자체가 일어나지 않으므로, 거부되고 다시 치는 수고가 사라진다. **미리
나눌 수 있으면 나눠 두면 좋다. 잊어도 도구가 멈춰 준다.**

유일한 예외는 위 「보호가 작동하지 않을 때」에 해당하는 경우로, 그 환경에서는 지금까지처럼
사전에 나누는 것이 유일한 대책이 된다.

- **`--project` 는 네 개 명령 전부에 붙인다.** 하나라도 빠뜨리면 그 명령만
  현재 디렉터리 쪽의 기록＝다른 한쪽 팀에 기록되어, 두 개의 기록이 섞인다.
- 서로 다른 디렉터리에서 도는 미션끼리는 기록 위치가 원래 다르므로 `--project` 는 필요 없다.
- 나눈 만큼은 각각 독립된 프로젝트로 취급된다. 화면에는 탭이 두 개 나란히 서고,
  가동 중인 팀은 동시에 표시된다.
- 환경 변수 `AGENT_DASHBOARD_PROJECT` 로도 지정할 수 있다(`--project` 쪽이 우선).
- **`autofinish` 는 이 둘을 한 번에 마감할 수 있다.** `--project` 없이 부르면
  **현재 디렉터리에서 가동 중인 미션을 전부** 마감한다(나눈 쪽도 포함). SessionEnd hook
  에서 한 번만 부르면 된다.

---

## 7. 손자 에이전트(서브에이전트 자신이 자식을 만드는 경우)

기록의 충돌을 피하기 위해, 손자 에이전트는 **자기 몫을 자기 전용 파일에 쓴다.**
기록 위치는 프로젝트마다 다르므로, **서브에이전트에게는 절대 경로를 넘긴다.**
경로는 `dash status` 의 맨 끝에 표시된다.

서브에이전트에게 주는 지시문에 다음 내용을 포함시킨다(`<손자 에이전트의 저장 위치>` 는 실제 경로로 바꾼다).

> 자식 에이전트를 시작했다면 다음 JSON 을
> `<손자 에이전트의 저장 위치>/<자식의 ID>.json` 에 써 주세요.
> 한 대당 한 파일입니다. 다른 파일은 절대 건드리지 마세요.
>
> ```json
> {
>   "id": "A-1-x",
>   "name": "주사A-1-x",
>   "parentId": "A-1",
>   "model": "claude-haiku-4-5",
>   "mission": "분류 결과의 중복을 제거",
>   "status": "running",
>   "startedAt": "2026-07-30T10:30:00+09:00",
>   "log": [{ "at": "2026-07-30T10:30:00+09:00", "who": "분석A-1", "text": "주사A-1-x 탄생" }]
> }
> ```
>
> 완료되면 같은 파일을 다음 형태로 덮어써 주세요.
> 실측할 수 없었던 값은 `null` 인 채로 두세요(추측으로 메우지 말 것).
>
> ```json
> {
>   "id": "A-1-x",
>   "name": "주사A-1-x",
>   "parentId": "A-1",
>   "model": "claude-haiku-4-5",
>   "mission": "분류 결과의 중복을 제거",
>   "status": "done",
>   "startedAt": "2026-07-30T10:30:00+09:00",
>   "finishedAt": "2026-07-30T10:30:22+09:00",
>   "result": { "elapsedSec": 22, "tokens": null, "toolCalls": null, "headline": "중복 3건을 제거" },
>   "log": [{ "at": "2026-07-30T10:30:22+09:00", "who": "주사A-1-x", "text": "귀환 — 중복 3건을 제거" }]
> }
> ```

서버 쪽이 `state.json` 과 `agents/*.json` 을 자동으로 병합하므로 화면 쪽의 설정은 필요 없다.
손자 에이전트의 카드에는 「자가 보고」 배지가 붙는다.

- `generation` 은 쓰지 않아도 된다(`parentId` 에서 다시 계산된다)
- ID 가 `state.json` 과 충돌한 경우에는 `state.json` 쪽이 채택된다
- JSON 이 깨진 파일은 무시되고 화면 오른쪽 아래에 경고가 나온다(다른 유닛의 표시는 멈추지 않는다)

---

## 8. 구조상의 제약(파악해 둘 것)

| 항목 | 실제 |
| --- | --- |
| 갱신의 시점 | 「시작 직후」와 「완료 통지 시」 두 시점뿐. 실행 중에는 무음 |
| 진척% | 실측 소스가 없으므로 표시하지 않는다. 진척 바도 두지 않았다(가동 중인지 아닌지는 로봇의 움직임과 카드의 색으로) |
| 보고 대기 | 「가동 중인 자식을 가진 가동 중인 부모」를 읽는 쪽에서 도출해 호박색으로 표시한다. `state.json` 에는 쓰이지 않으며 보고용 명령도 없다. 부모가 자식과 병행해 자기 작업을 하고 있는 경우에는 실태와 어긋난다 |
| 가동 중의 경과 시간 | 화면 쪽이 `startedAt` 에서 매초 세어 올려 표시하고 있다 |
| 토큰 수·도구 횟수 | 완료 통지에 포함된 경우에만 실측값. 없으면 「—」 |
| 화면의 갱신 | 1초마다 `/api/state` 를 캐시 무효로 가져와, 내용이 바뀌었을 때만 다시 그린다 |
| 화면에 띄울 팀의 결정 | 서버 쪽에서만 정해진다. 브라우저는 기억하지 않으므로 어느 단말에서 열어도 같은 팀이 나온다 |
| 병렬 가동 | 가동 중인 팀은 전부 세로로 쌓아 동시에 표시한다. 나열 순서는 시작이 최근인 순 |
| 기록 위치의 수 | 프로젝트(＝작업 디렉터리)마다 하나뿐. 같은 디렉터리에서 두 번째를 `start` 하면 첫 번째는 가동 중인 채로 이력으로 밀려나 이후 쓸 수 없다. 병렬로 돌릴 거라면 `--project` 로 나눈다(→ 6.1) |
| 본체와 운용 규칙의 버전 | 따로따로 낡는다. 본체를 갱신해도 각 CLI 의 운용 규칙은 `install.py` 를 다시 실행할 때까지 낡은 채로 남는다. 어긋나 있으면 `start` 와 서버 시작 시에 알림이 나온다(`diagnose.py` 의 「운용 규칙의 버전」에서도 확인할 수 있다) |
| 팀의 교체 | `/api/state` 의 `teams` 에서 없어진 팀은 그 자리에서 틀째 정리한다 |
| 같은 폴더에서 start 를 다시 했을 때 | slug 는 바뀌지 않으므로, `mission.startedAt` 의 변화를 보고 이전 미션의 로그를 버린다 |
| 통신이 끊겼을 때 | 직전의 표시를 유지하고, 오른쪽 위의 접속 표시등이 빨갛게 된다 |
| 계통수의 배치 | 부모는 자신이 만들어 낸 자식들의 세로 한가운데에 둔다. 지휘탑은 나무 전체의 위아래 한가운데. 나무가 화면에 들어갈 때만 세로 가운데로 모으고, 넘칠 때는 위쪽으로 붙인다 |
| 배치의 반영 방법 | `.agent` 의 `top` 만으로 움직인다. DOM 의 재정렬은 하지 않는다(요소를 움직이면 그 아래의 CSS 애니메이션이 0 으로 돌아가기 때문). `transform` 도 쓰지 않는다(탄생 애니메이션과 충돌한다) |

---

## 9. 파일 구성과 환경

이 디렉터리의 내용물. 경로는 전부 실행 시에 해결되므로 어디에 두어도 동작한다.

```text
├─ dash.cmd / dash    런처(Windows / POSIX)
├─ dash.py            통합 진입점
├─ server.py          배포 서버(전체 프로젝트 공통·하나만 시작한다)
├─ update_state.py    상태 갱신 CLI(상태에 대한 유일한 기록 창구)
├─ install.py         초기 설정(각 CLI 의 운용 규칙 파일에 기록)
├─ dashlib.py         공통 로직(경로 해결·프로젝트 식별·병합·서식)
├─ build_vsix.py      VSCode 확장을 조립해서 설치한다(dash ext ...)
├─ test_tabs.py       탭 목록의 동작 확인. python test_tabs.py 로 실행된다(기록에는 손대지 않는다)
├─ README.ko.md       도입 절차(사람용)
├─ OPERATION.ko.md    이 절차서
├─ EXTENSION_PLAN.md  확장 기능화의 계획과 설계(무엇을 확인하고 무엇을 정했는지)
├─ extension/         VSCode 확장의 소스. 빌드 공정 없음·node_modules 없음
│  ├─ package.json    확장 매니페스트(버전·명령·설정은 여기가 정본)
│  ├─ extension.js    확장 본체(순수 CommonJS)
│  ├─ test_extension.js  동작 확인. node extension/test_extension.js 로 실행된다
│  └─ media/          아이콘과 그 생성 스크립트
├─ public/
│  ├─ index.html      대시보드 화면
│  └─ manual.html     사용 설명서(사람용·서버를 거쳐 읽는다)
├─ dist/              조립한 .vsix. 다시 만들 수 있으므로 추적하지 않는다
├─ missions/          프로젝트별 기록(저장 위치는 환경 변수로 변경 가능)
│  └─ .current        지금 화면에 띄울 팀의 slug. start 와 demo 만이 바꿔 쓴다
└─ trash/             삭제한 프로젝트의 보관 장소. 되돌리고 싶을 때는 폴더를 missions/ 로 옮긴다
```

### 환경 변수

| 변수 | 효과 |
| --- | --- |
| `AGENT_DASHBOARD_HOME` | 기록의 저장 위치를 바꾼다(기본값은 이 디렉터리. 쓸 수 없으면 OS 의 사용자 데이터 영역) |
| `AGENT_DASHBOARD_PROJECT` | 대상 프로젝트를 고정한다(`--project` 쪽이 우선) |
| `PORT` | 서버의 기본 포트(`--port` 쪽이 우선) |
| `CLAUDE_CONFIG_DIR` | Claude Code 의 `CLAUDE.md` 가 있는 위치 |
| `CODEX_HOME` | Codex CLI 의 `AGENTS.md` 가 있는 위치 |
| `GEMINI_CLI_HOME` | Gemini CLI 의 `GEMINI.md` 가 있는 위치 |
| `COPILOT_HOME` | GitHub Copilot CLI 의 설정 파일이 있는 위치 |
| `OPENCODE_CONFIG_DIR` | opencode 의 `AGENTS.md` 가 있는 위치 |
| `AGENT_DASHBOARD_AGENTS_FILE` | 직접 추가한 CLI 목록이 저장되는 위치 (기본값: 기록 폴더의 `agents.json`) |
| `AGENT_DASHBOARD_LANG` | 출력의 언어(`en` / `ja` / `zh` / `ko`). 저장된 설정보다 우선된다 |

### 표시 언어

명령의 출력은 `en` / `ja` / `zh` / `ko` 를 지원한다. 정해지는 방식은 위에서부터 순서대로이며, 가장 먼저 정해진 것이 쓰인다.

1. 환경 변수 `AGENT_DASHBOARD_LANG`
2. `dash lang <코드>` 로 저장한 설정(`<기록의 저장 위치>/lang`)
3. 환경 변수 `LC_ALL` / `LC_MESSAGES` / `LANG`
4. OS 의 표시 언어
5. 영어

```bash
python <도구의 위치>/update_state.py lang        # 지금의 언어와 정해진 방식을 본다
python <도구의 위치>/update_state.py lang en     # 바꾼다
```

화면(대시보드와 사용 설명서)의 언어는 **브라우저 쪽**에서 따로 정해진다(오른쪽 위의 선택기. 처음에는 브라우저의 언어 설정에서 자동 판정하고, 이후에는 그 브라우저에 저장된다). **명령의 언어를 바꿔도 화면은 바뀌지 않는다.** 반대도 마찬가지다.

번역이 없는 문장은 영어 그대로 나온다. 번역 누락 때문에 명령이 멈추는 일은 없다.

### API

| 엔드포인트 | 반환하는 것 |
| --- | --- |
| `/api/state` | 화면에 띄울 팀의 상태(`teams`) ＋ 탭 목록(`tabs`) |
| `/api/run?slug=<slug>&runId=<runId>` | 과거 1회분의 완전한 상태. `runId` 를 생략하면 진행 중인 것. slug 에 일본어가 들어가므로 쿼리로 받는다 |
| `/api/projects` | 프로젝트 목록만 |
| `/api/env` | 실행 환경(각종 경로·플랫폼·Python 버전) |
| `POST /api/project/delete` | 프로젝트의 기록을 삭제(기본은 `trash/` 로 이동). 본문 `{"slug": "..."}`, `Content-Type: application/json` 필수 |
| `POST /api/history/delete` | 미션의 기록을 `trash/` 로 옮긴다. 본문 `{"runs": [{"slug","runId"}, ...]}` 또는 `{"phase": "done"}`. `runId` 에 `null` 을 넘기면 현재 미션(`state.json`)이 대상이 되지만, 지울 수 있는 것은 완료된 것뿐이다 |

`tabs` 는 1초마다 가져오므로 **가벼운 목록**으로 해 두었다(`runId` / `title` / `phase` /
`startedAt` / `finishedAt` / `projectName` / `agentCount` / `doneCount` / `isCurrent`).
손자 에이전트의 자가 보고까지는 열지 않는다. 내용이 필요한 것은 선택된 하나뿐이므로, 그것은 `/api/run` 으로 가져온다.

아직 한 번도 `start` 하지 않은 틀(`runId` 가 `null` 이고 `phase` 가 `standby` 이며 유닛 0대)은,
달리 반환할 탭이 있을 때는 `tabs` 에 포함하지 않는다. **그때 `teams` 에서도 같은 프로젝트를 뺀다.**
화면 쪽은 매초 「`tabs` 에 없는 팀은 정리한다」와 「`teams` 에 있는 팀은 만든다」를 둘 다 하므로,
여기가 어긋나면 같은 팀의 틀을 만들었다 부수기를 끝없이 되풀이한다.

`doneCount` 는 마무리 누락(`phase` 가 `running` 인 채로 전 유닛 `done`)을 탭에 표시하기 위해서만 있는 수이며,
**셀 수 없었을 때는 `0` 이 아니라 `null`** 을 반환한다. 과거 기록(`history/`)도 항상 `null` 이다.
`0` 은 「한 대도 돌아오지 않았다」는 의미를 가져 버리므로, 「모른다」와 구별할 수 없으면
화면이 거짓말을 하게 된다.

`runs` 에 잘못된 `slug` / `runId` 가 하나라도 섞여 있으면 **아무것도 지우지 않고 400** 을 반환한다.
`..` 의 혼입은 「일부 실패」가 아니라 요청 자체의 오류이므로, 도중까지 지워서 어중간한 상태를
남기지 않는다. 그저 존재하지 않을 뿐인 기록은 `failed` 에 넣고 나머지는 진행한다.

파괴적인 조작은 `POST` 로만 받는다. `/api/project/*` 에 대한 `GET` 은 `405` 와 `Allow: POST` 를 반환한다
(브라우저의 미리 읽기나 실수로 누른 링크로 기록이 지워지지 않도록 하기 위해서다).

### 의존 관계

외부 라이브러리는 일절 사용하지 않았다. `http.server` / `json` / `pathlib` / `argparse` /
`datetime` / `hashlib` / `unicodedata` / `webbrowser` 등, 전부 Python 표준 라이브러리다.
`public/` 의 HTML 도 CDN 참조 제로의 자기 완결형이다. Python 3.9 이상, Windows / macOS / Linux 에서 동작한다.

VSCode 확장도 같은 방침이다. `.vsix` 의 조립은 `zipfile` 만으로 끝내고 있으므로 npm 도
`vsce` 도 필요 없고, 확장 본체도 `require('vscode')` 와 Node 내장만 쓴다(`node_modules` 없음).
유일한 예외는 `extension/media/make_icons.py` 로, 그림을 다시 그릴 때만 Pillow 가 필요하다.
실행에는 관여하지 않으므로 위의 「외부 라이브러리 제로」는 무너지지 않았다.
