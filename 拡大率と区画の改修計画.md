# 拡大率の往復を止め、置き場所が決まらない機体の区画を小さくする

## Context

利用者から2つ来ています。

1. **拡大率の自動調整が定まらず、拡大と縮小を繰り返すことがある。**
2. **「置き場所が決まらない機体」のロボは半分の大きさに、カードも小型化し、所要時間・トークン・ツールの表示は消す。**

この2つは無関係に見えて、**同じ1本の結合でつながっています**。区画（`.orphan-bay`）のカードには木と同じ拡大率が掛かっており、区画が縦に伸び縮みすると木の枠の高さが動く。その枠を測って拡大率を決めているので、**拡大率が拡大率自身に返ってくる**。2 の小型化はこの返り幅を小さくしますが、**返ってくること自体は消えません**。だから 1 は 1 で直します。

編集対象は **`public/index.html` の1ファイルだけ**です（`install.py` と `build_vsix.py` はこのファイルを存在チェックとバイトコピーで扱うだけで、中身を見ていません。SCOUT-C A-1）。`public/i18n.js` は触りません——新しい文言を1つも増やさない設計にしてあります。

---

## 1. 背景と現状

### 1-1. 何が起きているか

`.tree-pane` は縦の flex 列で、**木（`.tree-scroll`）と区画（`.orphan-bay`）が同じ縦の取り分を奪い合います**（`:272` / `:373` / `:716`）。そして `applyZoom()` は木と区画の中身の両方に同じ `zoom` を掛けます（`:1566-1587`）。

```
.tree-pane（高さ H。上から降ってくるので拡大率に依らない）
├── .tree-scroll   ← zoom を掛ける。fitBox() はここの clientHeight を測る
└── .orphan-bay    ← 見出しと注記は等倍、.bay-body だけ zoom を掛ける
```

つまり **拡大率を上げる → 区画が物理的に伸びる → 木に回る高さが減る → 「収まらない」と判定して縮める → 区画が縮む → 木に回る高さが増える → 「余っている」と判定して拡げる**、という一周が閉じています。

`autoFitZoom()` の冒頭コメント（`:1727-1734`）が誇る「これは不動点なので1歩で着き、2歩目は必ず差なしで止まる」という証明は、**「枠の物理寸法は拡大率に依らない」という前提**の上に立っています。区画がある画面では、その前提が成り立ちません。

### 1-2. なぜ起きるか（数理・検算）

記号を置きます。すべて1つのチームについて。

| 記号 | 意味 | 拡大率に依るか |
| --- | --- | --- |
| `u` | 拡大率（`zoomLevel/100`） | — |
| `H` | `.tree-pane` の物理高さ | **依らない**（`flex:1` は `flex-basis:0`。寸法は上から降ってくる） |
| `h0` | `.bay-head` + `.bay-note` の物理高さ | 依らない（zoom は `.bay-body` にしか掛けない） |
| `b` | `.bay-body` の中身のローカル高 | 依らない |
| `P` | `.tree-scroll` の上下パディングの合計 = 20 + 36 = **56**（ローカル px、`:373-375`） | 依らない |
| `C` | 木の中身のローカル高（`fitContent()` の `h`） | **依らないはずだった（誤り。後述）** |

**[設計時点の前提が実測で崩れていました。]** `con.h`（`fitContent()` の `h`）は、§2-6 の実装後の実測で、**拡大率と同期して 374〜384px のあいだで動いている**ことが分かりました。原因は `chooseWrap()` が葉クラスタの折り返し本数 R を `fitBox()` の**ローカル**寸法（＝物理÷拡大率）で選び直すことです。拡大率が変わると枠のローカル寸法が変わり、勝つ R が変われば列のレーン本数が変わり、`t.contentH` そのもの——ひいては `con.h`——が変わります。この揺れ自体は、下で導く b・P による発散の原因ではありません。ただし、§2-6 の第4層の署名がこの `con.h` をそのまま使っていたため、**署名が往復のたびに動いてしまい、第4層が発動すべき相手にだけ発動できなくなりました**。経緯と直し方は §2-6' にまとめてあります。

`.bay-body` の物理高さは `b·u`、よって `.tree-scroll` の物理高さ `S = H − h0 − b·u`。

`fitBox()` は `clientHeight`（ローカル = 物理 ÷ `u`）から**ローカルのパディング**を引いた値を返します（`:1700-1704`、`scrollPad()` が読む computed padding は未ズーム値）。

```
box.h   = S/u − P = (H − h0 − b·u)/u − P
f       = FIT_MARGIN · box.h / C
next(u) = u · f = m·[ (H − h0) − (b + P)·u ] / C        （m = FIT_MARGIN = 0.98）
```

**これは傾き `−m(b+P)/C` のアフィン写像**です。司令塔の導出（傾き `−b/C`）とほぼ一致しますが、**パディング `P` が `b` と全く同じ形で効く**という差があります。したがって発散の条件は

```
m·(b + P) / C > 1   ⟺   b > C/0.98 − 56
```

司令塔の見立て（`b > C`）より **56 ローカル px ぶん、成立しやすい**方へずれます。区画のカードは現状1体で約 296px を占めるので、**木の中身が約 350px 未満（＝機体が2〜3体まで）なら、区画が1行あるだけで発散側に入ります**。「ことがある」という報告と整合します。

検算の副産物として、**パディング `P` は区画が無くても同じ形で効いています**。区画が隠れている（`b = 0`）ときの傾きは `−0.98·56/C` で、`C ≈ 500` なら約 `−0.11`。発散はしませんが、**「1歩で着く」も成り立っていません**（数歩で幾何級数的に落ち着く）。既存コメントの「約分できる」は、ローカル px のまま引かれるパディングを見落としています。同じ種類の見落としが2箇所にある、というのが本当のところです。

補足を3つ。**どれも「往復が起きるのは限られた条件だけ」という報告の裏付けになります。**

- **横は返ってきません。** 幅の枝は `next = m·(W − 36u)/con.w`（`W` は枠の物理幅。区画は幅に影響しない）で、傾きは `−35.3/con.w`。列は最低でも 330px あるので、幅で決まっているあいだは必ず収束します。**往復するには、往復の両端で縦が効いている必要があります。**
- **区画には上限があるので、写像は折れ線です。** `.orphan-bay { max-height: 50% }`（`:721`）と `.bay-body { max-height: 46vh }`（`:749`）に張り付くと、区画の物理高さは `u` に依らなくなり、その領域の傾きは 0 ＝ 一発で収束します。実際に起きるのは「張り付かない領域で発散し、張り付く領域に当たって跳ね返る」形の周期2で、**振幅は無限には育たず、2つの値の往復として見えます**。
- **区画を畳んである（`#app.bay-collapsed`）ときは `b` が実質 0** なので起きません。

### 1-3. 4層の防御が、この経路にだけ効かない理由

`autoFitZoom()` は4層で備えています（`:1735-1740`）。この経路に対しては次の状態です。

| 層 | 中身 | この経路では |
| --- | --- | --- |
| 1 | 不動点性 | **成立しない**（1-2）。前提が崩れている |
| 2 | `scrollbar-gutter: stable` | 効いている（別の振動源を断つもの。触らない） |
| 3 | 不感帯・拡大側の待ち | **効かない**。発散なので毎回不感帯を飛び越える。拡大側の 500ms は往復を止めず、**周期を作る律速要因**になっている |
| 4 | 同じ寸法のまま何度も動いたら手を引く | **原理的に発動しない**（下記） |

第4層の署名は `.tree-scroll` の**物理**寸法で組まれています（`:1801-1803`）。

```js
const phys = t.els.scroll.getBoundingClientRect();
const sig = con.w + 'x' + con.h + '/' +
            Math.round(phys.width) + 'x' + Math.round(phys.height);
```

コメントは「`getBoundingClientRect` は zoom に依らない物理 px を返すので、往復しても署名が動かない」と書いています。**その物理 px 自体が、区画を経由して拡大率ごとに動きます。** 署名が毎回変わるので `fitBurst` は 1 にリセットされ続け、`FIT_MAX_BURST`(4) に到達しません。**「最後の止め」が、それが止めるべき唯一の相手に対してだけ、定義上、発動できない。**

### 1-4. 採る方針と、採らなかった方針

**採る（司令塔の A・B・C。B は形を変えて採る）:**

- **A. 反転を検出したら二分で詰める。** 生の目標値をそのまま当てるのをやめ、**目標の向きが前回動いた向きと逆になったら、答えは前回の拡大率といまの拡大率のあいだにある**と見て中点を当てる。区間は反転のたびに半分になるので、原因が何であれ有限回で止まる。
  - **なぜ挟めると言えるか（検算）:** 写像 `g(u)` の増分は `g(u) − u = (1 + k)(u* − u)`（`k = m(b+P)/C > 0`、`u*` は不動点）。**係数 `1+k` は必ず正**なので、`g(u) − u` の符号は常に `u* − u` の符号と一致する——つまり**目標の向きは常に答えのある側を指します**。`z0` から `z1` へ上げた（`z0` では余っていた）直後に `z1` で下げろと出た（`z1` では溢れている）なら、答えは `[z0, z1]` の中。
- **B. 最後の砦を作り直す。** 署名の枠側を `.tree-scroll` ではなく **`.tree-pane`**（区画の外側・拡大率に依らない）で測る。これで署名は往復に対して不変になり、はじめて数が溜まる。
  - **ただし司令塔案の「反転の回数」ではなく「同じ署名のもとで動いた手数」で数えます。** 反転を数える方式は、反転せずにじりじり動き続ける経路（将来別の結合が入ったとき）を拾えないためです。手数なら反転の有無に関わらず有限で終わります。
  - **固定先も「見た中で最小の拡大率」ではなく「区間の下端 `fitLo`」にします。** 下端は「そこではまだ余っている＝収まると実測できた」値です。ただ小さいだけの値より根拠が強く、しかも**収まると分かっている中では一番大きい**ので、必要以上に縮めません。
- **C. 状態のリセット条件を1箇所にまとめる。** 木の形か枠が変わったとき（署名の変化）、自動フォーカスを入れ直したとき、タブを切り替えたとき。**捨て忘れると「昔の往復の記憶が正当な合わせ直しを抑え込む」**ので、捨てる場所を関数1つ（`resetFitTuning()`）に集めます。

**採らない:**

| 案 | 採らない理由 |
| --- | --- |
| SCOUT-A 案A（区画を flex 列から外す・高さを px で固定する） | 区画と木が縦に積まれて両方見える、というレイアウトを設計からやり直すことになる。固定 px 化する側も「いま何 px 要るか」を別途持つ実装が要り、要望2件に対して割に合わない |
| SCOUT-A 案B（`.bay-body` に zoom を掛けない） | `applyZoom()` のコメント（`:1572-1574`）が明示的に潰した状態に戻る。自動フォーカスは既定で入っているので、**区画のカードだけが大きく残る画面が常態**になる。「木の中と同じ姿で並べる」という区画の設計意図を正面から壊す |
| SCOUT-A 案C（署名を広げるだけ） | 往復した末に `console.warn` を出して自動フォーカスを手放すだけで、「振動しない」という要求に応えていない。**本計画では署名の作り直しは採るが、それは第4層の話で、往復そのものは A で消す** |
| SCOUT-A 案D（縮小側にも待ちを入れる） | 周期が伸びるだけで往復は止まらない。加えて「切れているのを直すのは急ぐ、余白を詰めるのは急がない」という設計意図（`:1773-1776`）と正面衝突する。実際に切れている時間を伸ばす退化 |
| **解析解を当てる**（区画の物理高さを測って `u* = m(H−h0)/(C + m(b+P))` を直接計算する） | 一発で着くので魅力的だが、**自動フォーカスが区画の DOM を知ることになる**。上限に張り付く領域では式が変わる（折れ線）ので場合分けも要り、将来3つ目の結合が入れば同じ形で破れる。**二分は原因を知らなくてよい**ので、そちらを採る |

---

## 2. 変更の設計（構想1：往復を止める）

編集するのは `public/index.html` の JS ブロックだけ、次の7箇所です（定数／状態変数／新しい関数1つ／`setAutoFit()`／`switchTab()`／`createTeam()` の `els`／`autoFitZoom()`）。

### 2-1. 定数（`:1382`）

**現在:**

```js
const FIT_MAX_BURST  = 4;     // 同じ寸法のまま何度も動いたら手を引く（最後の止め）
```

**変更後:**

```js
// 同じ形・同じ枠のまま動いてよい手数の上限（最後の止め）。旧 FIT_MAX_BURST(4) を
// 改名して増やした。**数える相手が変わったため。** 旧実装は「同じ寸法のまま何度も
// 動いた回数」だったが、その署名が往復のたびに動いてしまい一度も溜まらなかった
// （autoFitZoom のコメント参照）。署名を拡大率に依らない量で組み直すと今度は正しく
// 溜まるが、下の二分探索は正当な詰めに何手か使う——1〜500 の全域を二分で詰め切るのに
// 最大9手（2^9 = 512 > 500）、その前の生の目標値での数手を足して余裕を見た値。
const FIT_MAX_STEPS  = 16;
```

**追記（2回目の実装で加わった定数。この計画には無かった）:**

`FIT_MAX_STEPS` と二分（A）だけでは足りませんでした。§2-6' の経緯のとおり、木の素性（機体数・世代数・状態別数）で署名を組み直しても、**「顔ぶれと状態は同じなのに木の形だけ変わる」場合を署名は捉えられません**（例: 任務の文言が伸びてカードが1行高くなる）。そのまま区間（`fitLo`/`fitHi`）を持ち続けると、今度は「形は変わっているのに署名は同じなので区間の外へ出られず、ずれたまま直せない」という逆向きの不具合になります。そこで区間そのものに有効期限を足しました。

```js
// 二分で挟んだ区間の有効期限（安全弁）。署名（autoFitZoom 内）は木の素性で組んでいる
// ので拡大率では動かないが、**顔ぶれと状態が同じまま木の形だけ変わる**場合は捉え
// られない（例: 任務の文言が伸びてカードが1行高くなる）。捉えられないまま区間を
// 持ち続けると、「形は変わっているのに署名は同じなので区間の外へ出られず、ずれた
// まま直せない」という新しい不具合になる——これは作ってはいけない。だから区間を
// 作ってから一定時間を超えたら区間だけを手放す（署名と手数は捨てない）。9手・
// 200ms間隔で2秒以内に詰め切る設計なので、6秒あれば正当な詰めを邪魔しない。
const FIT_BRACKET_MS = 6000;   // ms
```

期限切れの扱いは「区間だけを手放す」であって、署名や手数（`fitSteps`）まで巻き戻す `resetFitTuning()` とは別物にしました。木の形自体は変わっていないかもしれない以上、「この木で往復した実績」を消す理由が無いからです。区間を手放したあとは、**必ず `laterAutoFit()` で測り直しを予約します**——完了して毎秒の再描画が止まった画面では、予約しない限り誰も測り直しません。

### 2-2. 状態変数（`:1501-1506`）

**現在:**

```js
let fitRaf       = 0;       // 予約中の rAF
let fitRecheck   = 0;       // 「あとでもう一度測る」予約（待ちに入ったまま忘れないための命綱）
let fitLastAt    = 0;       // 最後に自動で拡大率を変えた時刻
let fitGrowSince = 0;       // 「拡大してよい」状態が続き始めた時刻（0＝続いていない）
let fitBurst     = 0;       // 同じ寸法のまま何回動いたか
let fitBurstSig  = null;    // その寸法の署名
```

**変更後:**

```js
let fitRaf       = 0;       // 予約中の rAF
let fitRecheck   = 0;       // 「あとでもう一度測る」予約（待ちに入ったまま忘れないための命綱）
let fitLastAt    = 0;       // 最後に自動で拡大率を変えた時刻
let fitGrowSince = 0;       // 「拡大してよい」状態が続き始めた時刻（0＝続いていない）
let fitSteps     = 0;       // 同じ署名のまま自動で何手動いたか（旧 fitBurst）
let fitSig       = null;    // その署名（旧 fitBurstSig。組み方が変わっている）
let fitDir       = 0;       // 直前に動いた向き（+1 拡大 / −1 縮小 / 0 まだ動いていない）
let fitPrev      = 0;       // 直前に動く前の拡大率。反転したときに答えを挟む相手になる
let fitLo        = 0;       // 答えを挟んだ区間の下端（0＝まだ挟めていない）。**そこでは
                            //   まだ余っていた＝収まると実測できた**拡大率でもある
let fitHi        = 0;       // 同じく上端（0＝まだ挟めていない）
let fitBracketAt = 0;       // 区間を作った時刻（0＝区間が無い）。FIT_BRACKET_MS を過ぎたら
                            //   区間だけを手放す安全弁（**計画には無かった変数。§2-6' 参照**）
```

`fitBracketAt` は当初の計画に無かった変数です。§2-1 に追記した `FIT_BRACKET_MS` と対になっていて、区間を作った瞬間の時刻を控えておき、そこから一定時間が経っても詰み切らなければ区間だけを手放します。

### 2-3. リセットを1箇所にまとめる（`setAutoFit()` の手前に新設）

**新規追加:**

**実装ではこの関数を2つに割っています（計画には無かった分割。§2-1 の `FIT_BRACKET_MS` を足したときに要るようになりました）。** 区間の期限切れは「区間（`fitLo`/`fitHi`）だけを手放したい」場面で、署名 `fitSig` や手数 `fitSteps` まで一緒に戻す `resetFitTuning()` をそのまま呼ぶと、期限切れのたびに「この木で往復した実績」まで消えてしまいます。そこで区間まわりだけを `clearBracket()` として切り出し、`resetFitTuning()` はそれに `fitSig`/`fitSteps` のクリアを足したものにしました。

```js
/** 二分で挟んだ区間だけを手放す（fitSig / fitSteps には触らない）。
 *
 *  resetFitTuning() の中身から区間まわりだけを切り出したもの。区間の有効期限切れ
 *  （FIT_BRACKET_MS、autoFitZoom 内）は「木の形は変わっていないかもしれないが、
 *  区間が古くなったので測り直したい」場面で、**署名と手数は残したまま**区間だけを
 *  捨てたい——そのためだけにここを分けてある。resetFitTuning() はこれに
 *  fitSig/fitSteps のクリアを足したもの。 */
function clearBracket() {
  fitDir = 0; fitPrev = 0; fitLo = 0; fitHi = 0; fitBracketAt = 0; fitGrowSince = 0;
}

/** 自動フォーカスの調律をまっさらに戻す。
 *
 *  **捨て忘れが新しい不具合になる**ので、捨てる場所をここ1つに集めてある。捨て忘れると
 *  「昔の往復の記憶（区間）が、木が本当に変わったあとの正当な合わせ直しを抑え込む」形で、
 *  今度は**動かなすぎる**壊れ方をする。呼ぶのは次の3つだけ：
 *    - 署名が変わったとき（木の形か枠の物理寸法が変わった＝測り直すべき）
 *    - 自動フォーカスを入れ直したとき（＝「枠に合わせる」ボタンとして押されたとき）
 *    - タブを切り替えたとき（別の木なので、前の木で挟んだ区間は意味を持たない）
 *  区間の有効期限切れはここを呼ばない。署名と手数を残したいので、上の clearBracket()
 *  だけを呼ぶ（autoFitZoom 内）。
 *  @param {string} [sig] 署名。省略すると「署名も無かったことにする」
 */
function resetFitTuning(sig) {
  fitSig = (sig === undefined) ? null : sig;
  fitSteps = 0;
  clearBracket();
}
```

**`setAutoFit()`（`:1622`）の1行を差し替える:**

```js
// 現在
  fitBurstSig = null; fitBurst = 0; fitGrowSince = 0;   // 入れ直したら測り直す
// 変更後
  resetFitTuning();   // 入れ直したら測り直す
```

**`switchTab()`（`:1949` の `t.lastAvailH = -1;` の直前）に足す:**

```js
  // 別の木に移った。前の木で挟んだ区間は意味を持たないので捨てる。**署名が偶然一致する
  // ことがある**（同じミッションの「進行中」タブと、その記録タブは木の形も枠も同じ）ので、
  // 署名の変化に任せず、ここで明示的に捨てる。
  resetFitTuning();
  t.lastAvailH = -1;
```

### 2-4. `.tree-pane` を掴む（`createTeam()` の `els`、`:2591` の `scroll:` の隣）

**現在:**

```js
      scroll:   q('.tree-scroll'),
      canvas:   q('.tree-canvas'),
```

**変更後:**

```js
      // 自動フォーカスの署名を取る相手。**.tree-scroll ではなく、その親を測る。**
      // .tree-scroll の物理寸法は下の区画（.orphan-bay）の伸縮で拡大率ごとに動くが、
      // .tree-pane は flex:1（basis 0）なので寸法が上から降ってきて、拡大率に依らない。
      pane:     q('.tree-pane'),
      scroll:   q('.tree-scroll'),
      canvas:   q('.tree-canvas'),
```

### 2-5. `autoFitZoom()` の冒頭コメント（`:1727-1740`）

**いまのコメントは成立していない証明を語っています。そのまま残すと、次に読む人を同じ間違いへ案内します。** 差し替えます。

**現在（該当部分）:**

```js
 * 収束の要は**相対**で決めること。新しい拡大率 = いまの拡大率 × (枠 ÷ 中身)。
 * 中身のローカル寸法は zoom に依らず、枠のローカル寸法は 物理寸法 ÷ z なので、この式は
 * z を約分して 物理寸法 ÷ 中身 になる——**現在の拡大率に関係なく必ず同じ答えを返す**。
 * つまり不動点であり、どこから始めても1歩で着き、2歩目は必ず「差なし」で止まる。
 *
 * 旧実装の絶対式 newLevel = 100 / 比 は Z(n+1) = K / Z(n) の形で、Z(n+2) = Z(n)。
 * すなわち周期2で永久に往復する（100% ⇔ 39.68% を実測で再現している）。ここを相対に
 * するだけで、往復は「止まる」のではなく**起きなくなる**。
 *
 * その上で、丸め・スクロールバー・毎秒増える機体という現実の揺らぎに4層で備える：
 *   1. 上の不動点性（振動そのものを消す）
```

**変更後:**

```js
 * 収束の要は**相対**で決めること。新しい拡大率 = いまの拡大率 × (枠 ÷ 中身)。
 * 旧実装の絶対式 newLevel = 100 / 比 は Z(n+1) = K / Z(n) の形で、Z(n+2) = Z(n)。
 * すなわち周期2で永久に往復した（100% ⇔ 39.68% を実測で再現している）。相対にした
 * ことで**その**往復は起きなくなった。
 *
 * **ただし、これは不動点ではない。** ここにはかつて「z が約分されるので1歩で着く」と
 * 書いてあったが、成り立っていなかった。実際の1歩は次のアフィン写像になる（u = z/100）：
 *
 *     next(u) = m · [ (H − h0) − (b + P)·u ] / C
 *       H  … .tree-pane の物理高さ（flex:1 で上から降ってくるので拡大率に依らない）
 *       h0 … 区画の見出し＋注記の物理高さ（zoom は .bay-body にしか掛けていない）
 *       b  … .bay-body の中身のローカル高、  P … .tree-scroll の上下パディング 56px
 *       C  … 木の中身のローカル高、          m … FIT_MARGIN
 *
 * **実測でもう1つ分かったことがある。C も完全な定数ではない。** 拡大率と同期して
 * 374〜384px のあいだで揺れることを実測で確認した。原因は chooseWrap()（下で定義）が
 * fitBox() の**ローカル**寸法（＝物理 ÷ 拡大率）で葉クラスタの折り返し本数 R を選び直す
 * こと——拡大率が変わると枠のローカル寸法が変わり、勝つ R が変われば列のレーン本数が
 * 変わり、木の形＝ contentH そのものが変わる。**ここには元々「C はローカル座標なので
 * 拡大率に依らない」と書いてあったが、それは前提の誤りだった。** この揺れ自体は往復の
 * 主因ではない（発散はあくまで b・P の効きで起きる）が、「同じ形のまま動いたか」を判定
 * する第4層の署名に測った C をそのまま使うと、C 自身が揺れるせいで署名が往復のたびに
 * 変わり、第4層が発動すべき相手にだけ発動できなくなる——これが実際に起きていた（下の
 * autoFitZoom 本体、署名まわりのコメント参照。署名は測った px ではなく木の素性で組み
 * 直してある。経緯は §2-6' にまとめてある）。
 *
 * 傾きが 0 でない理由は2つある。ひとつは**枠の物理高さが拡大率の関数だから**——区画
 * （.orphan-bay）は木と同じ縦の取り分を奪い合っていて、その中身には木と同じ zoom が
 * 掛かっている（applyZoom のコメント参照）。もうひとつは**パディング P**で、clientHeight
 * はローカル値なのに、そこから引くパディングもローカル値なので、約分されずに残る。
 * 傾き −m(b+P)/C の絶対値が 1 を超えると**発散し、拡大と縮小を交互に繰り返す**
 * （木が小さく区画が大きいときに実際に起きる。P のぶん、区画が無くても傾きは 0 でない）。
 *
 * そこで、写像の性質に頼るのをやめて**挟み撃ち**にする。増分は g(u) − u = (1+k)(u* − u)
 * で係数 (1+k) は必ず正なので、**目標の向きは常に答えのある側を指す**。だから「前回動いた
 * 向きと逆の目標が出た」ことは「答えは前回の拡大率といまの拡大率のあいだにある」ことと
 * 同じで、中点を当てれば区間は毎回半分になる。整数なので 1〜500 の全域からでも9手で
 * 詰み切り、そこで必ず止まる。**原因を知らなくても止まる**のがこの方式の値打ちで、
 * 将来べつの「拡大率が枠の寸法を動かす」結合が入っても同じように効く。
 *
 * その上で、丸め・スクロールバー・毎秒増える機体という現実の揺らぎに4層で備える：
 *   1. 反転したら二分で詰める（往復を必ず有限回で終わらせる。区間には有効期限
 *      FIT_BRACKET_MS があり、詰め切れないまま長居したら区間だけを手放して測り直す）
 *   2. CSS の scrollbar-gutter:stable（バーの出入りで枠幅が揺れる古典的な振動源を断つ）
 *   3. 不感帯と、拡大側だけの待ち時間（1%刻みの±1ジッタと、増え続ける最中の脈動を吸う）
 *   4. 同じ署名のまま動きすぎたら、収まると実測できた拡大率へ固定して手を引く
```

**1番目の項目（区間の有効期限 `FIT_BRACKET_MS`）は当初の計画には無く、2回目の実装で足しました。** 2・3番目は元からある層で、変わっていません。経緯は §2-6' を参照してください。

### 2-6. `autoFitZoom()` 本体（`:1746-1816`）

**現在:**

```js
function autoFitZoom(t) {
  if (!autoFitOn || !t || !teams.has(t.key)) return;
  if (!t.root.hasAttribute('data-active')) return;

  const box = fitBox(t), con = fitContent(t);
  if (!box || !con) return;

  const f = Math.min(box.w / con.w, box.h / con.h) * FIT_MARGIN;
  if (!Number.isFinite(f) || f <= 0) return;

  const next = Math.max(MIN_ZOOM, Math.min(MAX_ZOOM, Math.round(zoomLevel * f)));

  const dead = Math.max(1, Math.round(zoomLevel * FIT_DEADBAND));
  if (Math.abs(next - zoomLevel) < dead) {
    fitGrowSince = 0; fitBurst = 0; fitBurstSig = null;
    return;
  }

  const now = performance.now();
  if (next > zoomLevel) {
    if (f < FIT_GROW_GAIN) { fitGrowSince = 0; return; }
    if (!fitGrowSince)     { fitGrowSince = now; laterAutoFit(FIT_GROW_MS); return; }
    if (now - fitGrowSince < FIT_GROW_MS) {
      laterAutoFit(FIT_GROW_MS - (now - fitGrowSince));
      return;
    }
  }
  fitGrowSince = 0;

  if (now - fitLastAt < FIT_MIN_GAP_MS) {
    laterAutoFit(FIT_MIN_GAP_MS - (now - fitLastAt));
    return;
  }

  const phys = t.els.scroll.getBoundingClientRect();
  const sig = con.w + 'x' + con.h + '/' +
              Math.round(phys.width) + 'x' + Math.round(phys.height);
  if (sig === fitBurstSig) {
    if (++fitBurst > FIT_MAX_BURST) {
      console.warn(tr('console.fit_stopped'), sig);
      return;
    }
  } else { fitBurstSig = sig; fitBurst = 1; }

  fitLastAt = now;
  setZoom(next, { auto: true });
  scheduleLayout(t);
}
```

**1回目の実装（この計画に最初に書いたとおりのもの）:**

このまま実装したところ、**往復は止まりませんでした**。実測は `27,41,34,34,35,35,37,35,37,35,35,...`。理由と直し方は §2-6' にまとめてあります。ここでは最終的に確定した2回目の実装を示します。

**変更後（2回目・実装で確定した内容）:**

```js
function autoFitZoom(t) {
  if (!autoFitOn || !t || !teams.has(t.key)) return;
  if (!t.root.hasAttribute('data-active')) return;

  const box = fitBox(t), con = fitContent(t);
  if (!box || !con) return;

  // 署名。**測った px ではなく、木の素性（データ）で組む。**
  // ここにはかつて con.w x con.h（fitContent() の戻り値＝木の中身のローカル実寸）を
  // 使い、「con はローカル座標なので拡大率に依らない」とコメントしていた（頭のコメント
  // 参照）。**その前提は実測で破れていた**：con.h が拡大率と同期して 374〜384px の
  // あいだで動いた。原因は chooseWrap()（後述）が fitBox() のローカル寸法（＝物理 ÷
  // 拡大率）で葉クラスタの折り返し本数 R を選び直すこと——拡大率が変わると枠のローカル
  // 寸法が変わり、勝つ R が変われば木の形＝ contentH そのものが変わる。測った px を
  // 署名にする限り、拡大率が動くたびに署名も動いてしまい、**この最後の止めが、止める
  // べき唯一の相手にだけ発動できなかった**（往復のたびに resetFitTuning() が発火して
  // 二分の区間が一度も蓄積されない。検証者の実測で確認済み）。
  //
  // 署名の役目は寸法の記録ではなく「同じ木が同じ枠に入っている」ことの同一視なので、
  // 拡大率にはそもそも汚染されないデータ由来の量で組み直す：機体数・世代数・状態別の
  // 機体数（running/done/standby、および running に重なる修飾である waiting を別枠で）。
  // カード高は状態で変わりうる（完了で結果行が生える等）ので、数が同じでも状態が動いた
  // ら別の木として扱う。枠側は変えない——.tree-scroll ではなく親の .tree-pane を測る
  // （.tree-scroll の物理高さは区画の伸縮で拡大率ごとに動くが、.tree-pane は実測でも
  // 不変だった。§6-3）。
  const agentsList = (t.state && t.state.agents) || [];
  let sigRun = 0, sigDone = 0, sigStandby = 0, sigWaiting = 0;
  const sigGens = new Set();
  for (const a of agentsList) {
    sigGens.add(genOf(a));
    if (a.status === 'running') sigRun++;
    else if (a.status === 'done') sigDone++;
    else sigStandby++;
    if (a.waiting) sigWaiting++;
  }
  const pane = t.els.pane.getBoundingClientRect();
  const sig = agentsList.length + 'n' + sigGens.size + 'g' +
              sigRun + '.' + sigDone + '.' + sigStandby + '.' + sigWaiting + 'w/' +
              Math.round(pane.width) + 'x' + Math.round(pane.height);
  if (sig !== fitSig) resetFitTuning(sig);   // 木の形か枠が変わった＝挟み直す

  const now = performance.now();

  // 区間の有効期限（安全弁。定数は上で定義）。**素性の署名は「顔ぶれと状態が同じまま
  // 木の形だけ変わる」場合を捉えられない**（例: 任務の文言が伸びてカードが1行高くなる）。
  // 捉えられないまま区間（fitLo/fitHi）を持ち続けると、「形は変わっているのに署名は
  // 同じなので区間の外へ出られず、ずれたまま直せない」という新しい不具合になる——これは
  // 作ってはいけない。だから区間を作ってから FIT_BRACKET_MS を超えたら区間だけを手放す。
  // **署名と手数（fitSig/fitSteps）は捨てない**——「この木で往復した実績」なので、直後に
  // また反転したら同じ手数の続きとして新しい区間を作ればよい。手放したら必ず
  // laterAutoFit() で測り直しを予約する：完了して毎秒の再描画が止まった画面では、
  // 予約しない限り誰も測り直さない。
  if (fitHi && now - fitBracketAt > FIT_BRACKET_MS) {
    clearBracket();
    laterAutoFit(FIT_MIN_GAP_MS);
  }

  // 最後の止め。上の層が全部破れても、ここで必ず有限回で終わる。
  // **固定先は fitLo。** そこでは「まだ余っている＝拡大したい」と出ていた拡大率なので、
  // 収まることが実測で裏付けられている。ただ小さいだけの値ではなく、収まると分かって
  // いる中で一番大きい値なので、必要以上に縮めない。
  if (fitSteps >= FIT_MAX_STEPS) {
    if (fitSteps === FIT_MAX_STEPS) {
      fitSteps++;                                    // 言うのも直すのも1回だけ
      console.warn(tr('console.fit_stopped'), sig);
      if (fitLo && zoomLevel !== fitLo) {
        fitLastAt = now;
        setZoom(fitLo, { auto: true });
        scheduleLayout(t);
      }
    }
    return;   // 署名が変わるまで手を引く
  }

  // 横と縦の両方を収める。列幅は 330px 固定で世代ぶんしか増えないが、縦は機体が増えるたび
  // 約152px ずつ伸びる。実測では縦が6体で溢れるのに対し、横は5世代必要——つまり普段
  // 効くのは縦のほう。横だけを見ていた旧実装は、最も起きる過密に何もしていなかった。
  const f = Math.min(box.w / con.w, box.h / con.h) * FIT_MARGIN;
  if (!Number.isFinite(f) || f <= 0) return;

  const raw = Math.max(MIN_ZOOM, Math.min(MAX_ZOOM, Math.round(zoomLevel * f)));

  // 不感帯は現在値に対する割合で見る。1%刻みなので、絶対値で置くと 500% では緩すぎ、
  // 5% では厳しすぎる。丸めの ±0.5 を必ず吸えるように最低 1%pt は確保する。
  // **判定するのは生の目標値。** 二分の中点で判定すると、いまが正しい位置なのに
  // 区間の反対側へ引っ張られて動いてしまう。
  const dead = Math.max(1, Math.round(zoomLevel * FIT_DEADBAND));
  if (Math.abs(raw - zoomLevel) < dead) {
    // 落ち着いた。ただし **区間（fitLo/fitHi）と署名は捨てない**。捨てると次のフレームで
    // 生の目標値がそのまま戻ってきて往復が再開する（生の目標値は、往復しているあいだ
    // ずっと遠くを指し続ける。落ち着いたのは二分で詰めた結果であって、生の目標値が
    // 近づいたからではない）。捨ててよいのは resetFitTuning() を呼ぶ3つの場面だけ。
    fitGrowSince = 0;
    return;
  }

  // 反転したら答えを挟む。以後はその区間を二分で詰める（頭のコメントの挟み撃ち）。
  const dir = raw > zoomLevel ? 1 : -1;
  if (!fitHi && fitDir && dir !== fitDir) {
    fitLo = Math.min(fitPrev, zoomLevel);
    fitHi = Math.max(fitPrev, zoomLevel);
    fitBracketAt = now;   // 区間ができた時刻。有効期限はここから数える（上のコメント参照）
  }
  let next = raw;
  if (fitHi) {
    // いまの位置が区間のどちら側かは、目標の向きが教えてくれる（向きは常に答えの側を
    // 指す）。その側の境界をいまの位置まで寄せてから中点を取る。
    if (dir > 0) fitLo = Math.max(fitLo, zoomLevel);
    else         fitHi = Math.min(fitHi, zoomLevel);
    // **中点は必ず切り捨てる（fitLo 側へ寄せる）。** Math.round だと .5 は上（fitHi 側）
    // へ丸まる（JS の仕様）。fitLo は「まだ余っていた＝収まると実測できた」側、fitHi は
    // 「収まらなかった」側（上のコメントの narrowing がそう作っている）ので、区間が
    // 隣り合う整数2つ（fitHi === fitLo + 1）まで詰まったとき、Math.round では中点 0.5 が
    // fitHi へ丸まり、**収まらない側で確定して return してしまう**。49体の木・1600x1000
    // で実際に踏んだ（fitLo:16 fitHi:17 のまま zoomLevel が17に固定され、49体中4体が
    // 46px はみ出し続けた。壊していないことの確認テストで発見）。切り捨てれば同じ場面で
    // 必ず fitLo（収まる側）に着地する。「切れているのを直すのは急ぐ」という設計の
    // 非対称性（頭のコメント参照）と向きが一致するので、最後の1手に限らず常に切り捨てる。
    next = Math.floor((fitLo + fitHi) / 2);
    if (next === zoomLevel) return;   // 区間が 1 未満に詰んだ＝ここが答え
  }

  // now はここより上（署名の直後）で1回だけ取っている——区間の有効期限判定と同じ
  // 時刻を使うため。
  if (next > zoomLevel && !fitHi) {
    // 拡大は急がない。機体は毎秒1体ずつ増えるので、空いた瞬間に戻すと画面が脈打つ。
    // 「切れているのを直す」は急ぎ、「余白を詰める」は急がない——利用者の損得が非対称。
    // 待つときは必ず「あとで測り直す」を予約する。予約しないと、完了して再描画が
    // 止まったミッションでは誰も測り直さず、待ちに入ったまま永久に拡大しなくなる。
    //
    // **区間を詰めている最中（fitHi あり）は待たない。** 区間があるということは署名＝
    // 木の形が変わっていないということで、この待ちが守ろうとしている「毎秒増える機体」
    // がそもそも居ない。しかも各手は必ず前の手より小さいので、待たなくても脈にならない。
    // 待たせると1手ごとに 500ms かかり、詰め切るまで数秒も画面が動き続けることになる。
    if (f < FIT_GROW_GAIN) { fitGrowSince = 0; return; }
    if (!fitGrowSince)     { fitGrowSince = now; laterAutoFit(FIT_GROW_MS); return; }
    if (now - fitGrowSince < FIT_GROW_MS) {
      laterAutoFit(FIT_GROW_MS - (now - fitGrowSince));
      return;
    }
  }
  fitGrowSince = 0;

  // 連発を止める。ただし**捨てない**：残り時間だけ待って測り直す（取りこぼし防止）。
  if (now - fitLastAt < FIT_MIN_GAP_MS) {
    laterAutoFit(FIT_MIN_GAP_MS - (now - fitLastAt));
    return;
  }

  fitSteps++;
  fitDir  = next > zoomLevel ? 1 : -1;   // 次の反転判定はこの向きと比べる
  fitPrev = zoomLevel;                    // 反転したとき、答えを挟む相手になる
  fitLastAt = now;
  setZoom(next, { auto: true });
  // 枠のローカル高さが変わったので縦の中央寄せを取り直す。ResizeObserver の発火に
  // 頼らないのは、zoom 変更で RO が飛ぶかがブラウザ依存だから（上のコメント参照）。
  scheduleLayout(t);
}
```

**1回目（§2-6 の頭に載せた元の計画）との差分は3つです。**

1. **署名から `con.w` / `con.h` を消し、木の素性（機体数・世代数・状態別数）に差し替えた。** 枠側は元の計画どおり `.tree-pane` のまま変えていません。
2. **区間の有効期限 `FIT_BRACKET_MS`（§2-1）と、それを判定する `if (fitHi && now - fitBracketAt > FIT_BRACKET_MS)` の1ブロックを足した。**
3. **二分の中点を `Math.round` から `Math.floor` に変えた。** これは署名とは無関係な、実装中に見つかった別のバグの修正です。

なぜこの3つが要ったのか、1回目を実際に動かして何が起きたかは次の §2-6' に時系列で記録してあります。

### 2-6'. 実装で分かったこと（1回目が効かなかった理由）

**この節は「§2-6 は最初から正しく書けていた」という誤解を持たせないための記録です。実際には2回書き直しています。時系列で残します。**

**1回目（この計画書に最初に書いたとおりの実装）**

- §2-6 の署名を、当初の計画どおり `con.w + 'x' + con.h + '/' + pane.width x pane.height` で組んで実装しました。§1-2 の前提（`C` はローカル座標なので拡大率に依らない）をそのまま信じた形です。
- 動かして実測すると、**往復は止まりませんでした**。拡大率の列は `27,41,34,34,35,35,37,35,37,35,35,...` で、34↔37 のあいだを終わらず動き続けました。
- 原因は `con.h`（`fitContent()` が返す `h`、実体は `bodyOffsetTop(t) + t.contentH`）が**拡大率と同期して 374〜384px のあいだで動いていた**ことです。`chooseWrap()` が葉クラスタの折り返し本数 R を `fitBox()` の**ローカル**寸法（＝物理÷拡大率）で選び直すため、拡大率が変わると枠のローカル寸法が変わり、勝つ R が変われば列のレーン本数が変わり、`t.contentH` そのものが変わります。**中身が枠に依存し、枠が区画に依存する**、§1 で扱っていたのとは別の結合がもう一重ありました。
- この結合が壊したのは収束そのものというより、**最後の止め**でした。署名に `con.h` を含めている限り往復のたびに署名が変わり、`resetFitTuning()` が毎回発火して二分の区間（`fitLo`/`fitHi`）が一度も蓄積されません。第4層（`fitSteps >= FIT_MAX_STEPS`）も同じ理由で一度も発動しませんでした——**止めるべき唯一の相手にだけ、この安全網が定義上効かない**という、§1-3 で旧実装の第4層について書いたのとまったく同じ形の穴が、書き直したはずの新しい署名にもそのまま残っていたことになります。

**直し方（2回目）**

- 署名から `con.w` / `con.h` を外し、**`t.state.agents` から数えた木の素性**（機体数・世代数・状態別の機体数 running/done/standby、および waiting）と、**枠側は変えず** `.tree-pane` の物理寸法で組み直しました。前者はデータ由来の量なので、拡大率がどう振れても1ビットも動きません。
- ただし素性の署名には弱点があります。**「顔ぶれと状態は同じなのに木の形だけ変わる」場合を捉えられません**（任務の文言が伸びてカードが1行高くなる、など）。区間を持ち続けるとこれが「形は変わっているのに署名は同じなので区間の外へ出られない」という逆向きの不具合になるため、**区間に有効期限 `FIT_BRACKET_MS = 6000ms`** を足しました。期限切れは区間だけを手放し（`clearBracket()`、署名と手数はそのまま）、必ず `laterAutoFit()` で測り直しを予約します。

**実装中に見つかった、計画外の実バグ**

- 二分の中点 `Math.round((fitLo + fitHi) / 2)` は、JS の丸め仕様（`.5` は上へ丸まる）のせいで、**区間の幅が1のとき常に `fitHi`（収まらない側）へ確定します**。1600×1000・49体の木で実際に踏み、`fitLo:16 fitHi:17` のまま `zoomLevel` が17に固定され、49体中4体が46pxはみ出したまま直りませんでした。「壊していないことの確認」テストで発見しています。
- `Math.floor` に変えることで、区間が詰み切る最後の1手は必ず `fitLo`（収まる側）に着地するようにしました。「切れているのを直すのは急ぐ」という設計の非対称性（§2-5 の頭のコメント）と向きが一致します。
- **この実バグは、1回目の実装が往復し続けていたあいだは踏んでいませんでした。** 署名が毎回リセットされるせいで区間がほとんど育たず、「幅1まで詰まる」場面自体がほぼ発生していなかったからです。つまり**旧署名の不具合が、この丸めの不具合を偶然隠していました**——署名を直したことで、隠れていた別の不具合が露わになった形です。

**最終結果（実測）**

- 木3体＋孤児16体、900×500、0.3秒間隔で50点サンプリング: `57,34,38,37,36,36,36,36,...` で **36% に完全静止**しました。
- 反転してから静止するまでの手数は8、時間にして約1.8秒。設計目標「整数区間なので最大9手、2秒以内」（§2-7）を満たしています。

### 2-7. この設計で止まることの確認（机上）

- **健全な場合（区画が無い・小さい）**: 反転しないので `fitHi` は立たず、いまの経路と1手も変わらない。不感帯・拡大側の 500ms 待ち・200ms 間隔・「自動で決めた値は保存しない」は、どれもそのまま。
- **往復する場合**: 1回目の反転で区間が立つ。以後は中点なので区間が毎回半分になり、`hi − lo ≤ 1` になった時点で `next === zoomLevel` となって `return`。**整数区間なので最大9手**、`FIT_MIN_GAP_MS`(200ms) 間隔で 2 秒以内に静止する。
- **それでも止まらない場合**: `fitSteps` が 16 に達したら `fitLo` へ固定し、`console.warn` を1回出して手を引く。署名が変わるまで再開しない。
- **木が毎秒変わっている最中**: 署名が変わるので区間は毎回作り直される＝生の目標値に戻る。**これは正しい挙動**（形が変わったのだから測り直すべき）だが、**その間は往復が止まらないことがある**という限界でもある。形が落ち着いた瞬間から、必ず有限回で止まる。この限界は仕様として受け入れる——止めるには「形が変わっても古い区間を使い回す」しかなく、それは新しい不具合（動かなすぎる自動フォーカス）と引き換えになるため。
- **この机上の確認は、そのままでは成立しませんでした（実測で判明）。** 上の4点は「署名が拡大率に汚染されない」ことを前提にしています。§2-6 に最初に書いたとおりに実装した1回目はこの前提が崩れていて（`con.h` が拡大率と同期して動いていた）、実際には往復が止まりませんでした（`27,41,34,34,35,35,37,35,37,35,35,...`）。§2-6 に載せた最終版（署名を木の素性で組み直し、`FIT_BRACKET_MS` を足し、二分の中点を `Math.floor` に変えたもの）で測り直すと、8手・約1.8秒で静止しています。経緯は §2-6' を参照してください。

---

## 3. 変更の設計（構想2：区画の小型化）

**CSS だけで完結します。JS は1行も触りません。** 理由は SCOUT-B の結論どおりで、`.c-metrics` の中の要素（`entry.time` / `timeK` / `tok` / `tool`）は `buildAgentDom()` が掴んだ参照を `updateAgent()` と `updateAgentClock()` が**毎秒**書きに行くため、DOM から消す道を選ぶと**木と共用のその経路に手を入れる**ことになるからです（消せば `null` 参照でクラッシュ、生成後に `.remove()` すれば「見えない要素へ毎秒書き続ける」という分かりにくさが残る）。

### 3-1. ロボは 47px（要件どおり半分）

`--robot-w: 94px → 47px`。高さは `.robot { height: calc(var(--robot-w) * 166 / 120) }`（`:466`）が追従して 130px → 65px になります。`:464-465` のコメントが「直書きにすると `--robot-w` を変えた瞬間に等倍関係が崩れる」と書いているとおり、**この実装は最初から `--robot-w` の変更に追従するよう設計されています**。

### 3-2. カードは 176px（「半分」にはしない。根拠）

**要件で「2分の1」と言われているのはロボだけで、カードは「小型化」です。** そして半分（116px）にすると、**中身が読めなくなるどころか、入らなくなります**。実際に入るものから下限を出します。

| 何 | 幅の見積り | 出どころ |
| --- | --- | --- |
| モデル札の文字（実データ最長 `claude-haiku-4-5-20251001` = 25文字） | 25 × 5.5 = **137.5px** | `.badge { font-size: 10px; font-family: Consolas, monospace }`（`:652`）。Consolas の送りは 0.55em |
| 札の余白と枠 | + 14 + 2 = **153.5px** | `.badge { padding: 2px 7px; border: 1px }` |
| カードの余白と枠（下記の詰めた値で） | + 20 + 2 = **175.5px** | `.orphan-bay .card { padding: 8px 10px 9px }` |

→ **下限 175.5px。採る値は 176px。** これを割るとモデル札が `.card { overflow: hidden }`（`:629`）に切られます。

**116px では、モデル札どころか `.c-id` すら入りません。** 区画のカードの `.c-id` は `ORPHAN_ID_PREFIX`（`LIVE:`）+ 実機の agentId 17桁（例 `LIVE:a06a817d47fb7fcfb`）で 22文字＝**121px**。しかも `.c-id` は `flex: none`（`:650`）なので縮まず、**先に潰れるのは隣の名前のほう**です。

ついでに分かったこと。**いまの 232px でも、区画のカードの名前は 78px（14px の全角で5文字半）しか取れていません**（206 − 121 − 7）。木のカードの `.c-id` は `SCOUT-A` のような短い記録上のIDなので、この潰れは**区画だけで起きています**。そこで `.c-top` を区画でだけ折り返し、IDを独立行に落とします。**カードは小さくなるのに、名前は今より読めるようになります**（見積りは154px = 全角11文字。実装後の実測値は §3-6）。

**文字サイズは下げません。** 区画は「記録に無い機体がいる」ことを読ませるために出しているものなので、縮めるのは余白・器・ロボであって、文字ではない、という線を引きます。

### 3-3. CSS（現在 → 変更後）

**現在（`:760-777`）:**

```css
/* 木の中の .agent は layoutTree が top/left を書く絶対配置。区画では並べて折り返すので
   通常のフローに戻し、ロボを上・カードを下に積む。木のほうの規則は触らない。 */
.orphan-bay .agent {
  position: static; width: var(--card-w);
  flex-direction: column; align-items: center; gap: 4px;
  transition: none;
}
/* 木では「線がロボの上辺へ落ちる」ために上端固定にしてある（.robot-slot のコメント）。
   区画に線は無いので、カードの真上へ中央寄せする。 */
.orphan-bay .robot-slot { align-self: center; }
```

**変更後（上の2規則はそのまま。`.orphan-bay .robot-slot` の直後に追記する）:**

```css
/* ---- 区画の機体は小さく並べる。**ロボは木の半分、カードは「入る中で一番小さい幅」。**
   ロボもスロットもカードも、寸法はすべて --robot-w / --card-w から calc()/var() で
   導かれている（.robot の height、.robot-slot の width、.card の width、上の
   .orphan-bay .agent の width）ので、ここで2つ上書きすれば配下だけが揃って縮む。
   JS のレーン幅（AGENT_W）は :root からしか読まないので、系統樹には一切届かない。

   **transform: scale() は使えない。** .agent は誕生アニメーション pop が
   fill-mode:both で transform を専有していて（上の .agent のコメントと同じ罠）、
   静的な scale を足しても 0.68 秒のあいだ無視され、.born が外れた瞬間にガクッと縮む。 */
.orphan-bay {
  --robot-w: 47px;    /* 木の 94px の半分。高さは calc(--robot-w * 166 / 120) が追従する */
  /* カードは半分（116px）にはしない。**入る文字が無くなるため。** モデル札は等幅 10px で、
     実データの最長 claude-haiku-4-5-20251001（25文字）が 137.5px。札の余白と枠で 153.5px、
     カードの余白と枠で 175.5px——ここが下限で、割るとモデル名が overflow:hidden に切られる。
     116px では札どころか、下の .c-id（LIVE: + 17桁 = 121px）1つも入らない。 */
  --card-w: 176px;
}
/* 所要時間・トークン・ツールは区画では出さない（要件）。
   **DOM からは消さず、CSS で隠す。** .c-metrics の中の要素は buildAgentDom が掴んだ参照を
   updateAgent と updateAgentClock が毎秒書きに行く（entry.time/timeK/tok/tool）。木と共用の
   関数なので、生成を分岐させれば null 参照で落ち、生成後に remove すれば「見えない要素へ
   毎秒書き続ける」ことになる。display:none なら要素は生きたまま、見た目だけ消える。
   中に CSS アニメーションを持つ要素は無いので、display:none で巻き戻る心配も無い
   （ロボを畳むのに高さ 0 を使っているのは、そちらにはアニメーションがあるから）。 */
.orphan-bay .c-metrics { display: none; }
/* 区画の ID は "LIVE:" + 実機の agentId 17桁で、10px の等幅でも 121px を専有する。
   .c-id は flex:none なので縮まず、狭いカードでは**隣の名前のほうが 0 まで潰れる**
   （いまの 232px でも名前は 78px しか無い）。区画だけ ID を独立行に落として、
   名前に幅を全部渡す。カードは小さくなるのに、名前は今より読める。 */
.orphan-bay .c-top { flex-wrap: wrap; }
.orphan-bay .c-id  { flex: 1 0 100%; }
/* 区画のカードに任務は無い（orphanAgent が mission:'' を固定で入れる。記録が無いので
   任務を名乗れない）。木では updateAgent がそこへ「（任務未記載）」と書くが、**区画では
   その行ごと出さない。** 記録が無いと分かっている機体に「記載が無い」と毎行言わせても、
   すぐ上の「記録に無い」の札と同じことを繰り返しているだけで、狭いカードの高さを食う。
   .c-metrics と同じく **DOM からは消さず CSS で隠す**——entry.mission は木と共用の
   updateAgent が毎回書きに行く参照なので、消すと null 参照で落ちる。 */
.orphan-bay .c-mission { display: none; }
/* 余白も幅に合わせて詰める（232 → 176 なので、左右は 12 → 10）。 */
.orphan-bay .card { padding: 8px 10px 9px; }
```

**この規則は当初の計画から変わっています。** 最初は「任務は空の1行として残す」（`min-height: 0` で高さだけ潰す）でしたが、実装を見た利用者の判断で「その行ごと出さない」（`display: none`）に変えました。§3-7 に理由と、`.c-live` を残す判断との対比をまとめてあります。

**さらに `.bay-body`（`:747-751`）の `gap` を1つだけ変えます:**

```css
/* 現在 */
  flex: 1 1 auto; display: flex; flex-wrap: wrap; align-items: flex-start; gap: 16px 18px;
/* 変更後（1体あたりの専有幅がおよそ 3/4 になるので、隙間も同じ向きに詰める） */
  flex: 1 1 auto; display: flex; flex-wrap: wrap; align-items: flex-start; gap: 12px 14px;
```

### 3-4. この変更で見えなくなるもの（承知の上で消す）

要件どおりに消しますが、**「どうせ空だから消す」ではありません**。実際には値が入っています。正直に書いておきます。

| 表示 | 区画のカードで、いま何が出ているか | 消したあと |
| --- | --- | --- |
| 経過 | **出ている。** 記録が無いので開始時刻は名乗れないが、代わりに実測のログ幅（`a.live.elapsedSec`）を出している（`:3050-3053`） | 稼働中は `.c-live` に沈黙の長さが残る。締めたあとは**見えなくなる** |
| トークン | 締めたあと（`frozen`）だけ実測値が出る（`frozenResult()` `:2196-2203`） | 稼働中は `.c-live` に `≈` 付きで残る。締めたあとは**見えなくなる** |
| ツール | 同上 | 同上 |

つまり**締めたあとの区画のカードからは、3つとも数字が消えます**。区画は「記録に無い機体がいる」ことを知らせるのが仕事で、その機体の成績を読む場所ではない、という整理です。

### 3-5. 振動への影響（**緩和であって、修正ではない**）

小型化すると発散条件 `b > C/0.98 − 56` の左辺 `b`（`.bay-body` の中身のローカル高）が小さくなります。概算で、1体あたりの高さが 296px → 約 190px、1行に並ぶ体数が約 1.3 倍になるので、**`b` はおよそ半分**になります。つまり発散側に入るには、いまの倍の「記録に無い機体」が要ることになります。

**これは発生確率を下げるだけで、返ってくる経路そのものは残ります。** `b` が 0 になるわけではない以上、木が十分小さければ同じことが起きます。**構想1の修正を代替しません。** 逆に言えば、**小型化を先に入れると振動の再現が難しくなる**ので、検証は構想1を先に測ります（§5）。

### 3-6. 実装後の実測（確定した値）

§3-1〜3-3 の値は、実装前の見積り（`--robot-w`/`--card-w` の計算と、文字幅からの下限計算）でした。実装後に実測して確定しています。

| 何を | 見積り（§3-1〜3-3） | 実測（確定） |
| --- | --- | --- |
| 区画のロボ幅 | 木の半分（`--robot-w: 47px`） | **45.11px 対 木のロボ幅 90.23px**（拡大率96%時）。比はちょうど半分 |
| 区画のカード幅 | 176px（下限175.5pxから採用） | **176px のまま。木側のカード幅は変更前後で1pxも変わっていない** |
| `.c-name`（名前）の実効幅 | 見積り154px（§3-2） | **78px → 133px に拡大。** 見積りとの差は、ボーダーや box-sizing など見積りに入れていない要素による |
| 区画のカード高 | — | **97.66px**（任務の行を消したあと。§3-7） |
| 木のカード高 | — | **144.48px。変更前後で不変** |
| `.bay-body` の gap | `16px 18px → 12px 14px` | 実装どおり（§3-3 に反映済み） |

`176px` という下限は「実データの最長モデル名 `claude-haiku-4-5-20251001` でもモデル札が `overflow:hidden` に切られない」ことを目的に出した値です。実測でもこの目的は満たされています。ブラウザのコンソールエラーは0件でした。

### 3-7. 消す行と残す行の線引き（利用者の判断）

区画のカードからは、要件どおり所要時間・トークン・ツール（`.c-metrics`）を消しました（§3-4）。実装を見た利用者の判断で、**もう2つ手を加えています。**

**消したもの: 任務の行（`.c-mission`）**

- 区画の機体には記録が無いので、`orphanAgent` は `mission: ''` を固定で入れます。木ではこれを `updateAgent()` が「（任務未記載）」という文言に変えてカードへ書きますが、**区画ではその行ごと出さないことにしました。**
- 理由は、記録が無いと分かっている機体に「記載が無い」と毎行言わせても、すぐ上に出ている「記録に無い」の札（`.badge.b-orphan`）と同じことを繰り返しているだけで、狭くしたカードの高さを食うだけだからです。
- `.c-metrics` とまったく同じ理由で、**DOM からは消さず CSS（`display: none`）で隠しています。** `entry.mission` は木と共用の `updateAgent()` が毎回書きに行く参照なので、DOM から消すと null 参照で落ちます。
- 実測: 区画のカード高 **97.66px**、木のカード高 **144.48px**（木は変更前後で不変）。区画の `.c-mission` は `display:none` かつ DOM には存在したまま、木の `.c-mission` は変更なし。コンソールエラー0件。

**残したもの: 稼働中の実測行（`.c-live`）**

- `.c-live`（`Read x  ツール 1 ≈1,600` のような実測行）は、当初は要件に無いものでしたが、**利用者の判断で残すことにしました。**
- 理由は、`.c-metrics`（完了通知として受け取って記録した値）とは出所が違い、**記録の無い機体について分かる唯一の実測情報だから**です。Claude Code がサブエージェントごとに書いているログから数えた実数で、記録に基づかない推定ではありません（§2-6 とは別の話で、こちらは自動フォーカスではなく画面表示の話です）。

**この2つは対になっています。** 記録が無いから名乗れないもの（任務）は消し、記録が無くても実測で分かるもの（何をしているか・トークン・ツール）は残す——「記録の有無」ではなく「本人が名乗れるか、実測で裏が取れるか」で線を引いている、という一貫した整理です。

---

## 4. 壊してはいけないもの

1. **`:root` の `--card-w` / `--robot-w`（`:32-33`）。** 上書きは `.orphan-bay` 配下だけ。ここを変えると系統樹のレイアウトが全部ずれる。
2. **JS のレーン幅計算 `AGENT_W`（`:3396-3404`）。** `getComputedStyle(document.documentElement)` から**モジュール読み込み時に1度だけ**読む定数で、`.orphan-bay` の上書きは見えない（それが安全である理由でもある）。`:root` を変えると、CSS と JS の両方を直さないと木の枝が全部ずれる。
3. **`updateAgent()` / `updateAgentClock()` が毎秒書き込む `entry.time` / `timeK` / `tok` / `tool`（`:2982-2988`, `:3054`）。** `setMetric()` に null チェックは無い。**DOM から消さない。**
4. **誕生アニメーション `pop` の transform 専有（`:439-445`）。** `.agent` に `transform: scale()` を足さない。
5. **`display: none` は配下の CSS アニメーションを巻き戻す**（`:754-756` のコメント）。`.c-metrics` は中にアニメーションが無いので `display:none` でよいが、**`.agent` / `.robot-slot` / `.robot` には使わない**。区画の畳みが高さ 0 で実装されているのはこの規則のため。
6. **`.orphan-bay { max-height: 50% }`（`:716-721`）と `.bay-body { max-height: 46vh }`（`:749`）。** 「木は何があっても半分を保てる」という設計。触らない（結果として、振動の振幅が無限に育たない歯止めにもなっている）。
7. **`scrollbar-gutter: stable`（`:370-375`）。** 別の振動源を断っている。触らない。
8. **`applyZoom()` が `.bay-body` にも zoom を掛けること（`:1576-1578`）。** 「木の中と同じ姿で並べる」という区画の設計意図。構想1はこれを**残したまま**成立するように作ってある。
9. **縮小は急ぎ、拡大は待つ、という非対称（`:1773-1776`）。** `FIT_GROW_MS` を縮小側へ広げない。
10. **`setZoom()` の既定が「利用者の操作」であること（`:1592-1596`）と、自動で決めた値を保存しないこと（`:1603`）。**
11. **文言（`public/i18n.js`）。** 新しいキーを増やさない設計にしてある（`console.fit_stopped` を流用）。`stat.elapsed` / `stat.took` / `stat.tokens` / `stat.tools` は**木のカードが使い続ける**ので削除禁止（SCOUT-C B-2）。
12. **`public/index.html` は LF。** `Path.write_text()` で書き戻すと全行 CRLF になり、触っていない行まで差分になる（過去に実際に踏んでいる）。Edit で直接編集するか `write_bytes()` を使う。
13. **配置済みコピー `~/.claude/agent-dashboard/public/index.html` を直接編集しない**（次の配置で消える）。反映は SCOUT-C A-3 の経路。

---

## 5. 作業の分割

`public/index.html` は単一ファイルです。**同じファイルを2人が同時に編集すると、後から書く側の Edit が「読んだ内容と一致しない」で落ちるか、最悪どちらかの変更を上書きします。** 領域（JS ブロックと CSS ブロック）が重ならなくても、順番を守ってください。

| 手番 | 担当 | やること | 前提 |
| --- | --- | --- | --- |
| 0 | VERIFY | **修正前の振動を録る**（§6-1）。木を小さく・区画を大きくした条件で、拡大率の列を記録して残す | 誰もファイルを編集していない状態（HEAD のまま） |
| 1 | IMPL-1 | **構想1**（§2 の7箇所）を実装してコミット | 手番0の記録が取れていること |
| 2 | VERIFY | **手番0とまったく同じ手順**で測り直し、収束することを示す | 手番1のコミット |
| 3 | IMPL-2 | **構想2**（§3 の CSS）を実装してコミット | **手番2が終わってから**。先に入れると `b` が小さくなり、振動の再現条件が変わって手番0との比較が成立しなくなる |
| 4 | VERIFY | 区画の見た目（§7 のチェックリスト）と、小型化後も収束すること | 手番3のコミット |
| 5 | REVIEW | 差分の読み合わせ（§6-3）と検査スクリプト7本 | 手番4 |

**手番1と手番3を同時に走らせないこと。** 分けたいなら、IMPL-1 がコミットしてから IMPL-2 が着手します。手番0→1→2 の順は動かせません（振動を直す前に振動を録らないと、直ったことを示せないため）。

---

## 6. レビューとテストの計画

**検証環境の作り方は SCOUT-C の報告（D-1〜D-4）に従います。** 要点だけ再掲します。

### 6-1. 振動を再現する条件（**木を小さく、区画を大きく**）

SCOUT-C は木49体＋孤児1体では再現しませんでした。**§1-2 の式がその理由を説明します**——`C`（木の中身）が大きく `b`（区画）が小さいので、傾きの絶対値が 1 を超えていません。**再現には逆の条件が要ります。**

```
発散する条件:  b > C/0.98 − 56     （b, C ともローカル px）
```

区画のカード1行ぶんが約 296px なので、**木の中身が 350px 未満（＝機体が 1〜3 体）で、区画に「記録に無い機体」が数体いる**と発散側に入ります。手順:

```bash
SCRATCH="<scratchpad>"
export AGENT_DASHBOARD_DATA_HOME="$SCRATCH/dashtest_data"
export AGENT_DASHBOARD_LIVE_ROOT="$SCRATCH/dashtest_live"
cd "$SCRATCH/dashtest_cwd"
UP="C:\Users\yuta_\OneDrive\Python_files\Subagent Dashboard\update_state.py"

# 木は小さく（指令塔＋1体）
python "$UP" start --project fit-osc --title "往復の再現" --model opus-5
python "$UP" add --project fit-osc --id SCOUT-A --name "偵察機A" --model sonnet-5 --mission "再現用"

# 区画は大きく（記録に無い機体を 4〜6 体）。make_orphan.py の AGENT_ID を変えて回す
python "$SCRATCH/make_orphan.py"     # ORPHAN0001 … 0006

cd "C:\Users\yuta_\OneDrive\Python_files\Subagent Dashboard"
python server.py --port 3940 --no-retry     # 必ず run_in_background で
```

窓は 1600×1000 程度（区画が `max-height:50%` に張り付かない大きさ）。**張り付くと傾きが 0 になって収束してしまう**ので、区画が上限まで伸びていないことを目視で確かめてから測ります。

計算による予測（`H−h0 ≈ 740`, `C ≈ 300`, `b ≈ 296` のとき）は `u(n+1) = 2.42 − 1.15·u(n)`、不動点 112%、傾き −1.15 で**ゆっくり発散**します。100% から始めると `100 → 127 → 96 → 131 → 91 …`。**この数列そのものを当てにはしません**（`b` も `C` も実測で決まる）。当てにするのは「同じ2値の周りを交互に動き続けるか、静止するか」だけです。

### 6-2. 何をもって「直った」とするか（実測の手順）

Playwright（`$SCRATCH/resize_test.py` と同じ形）で、`#zoom-display` の文字列を **200ms ごとに 20 秒**サンプリングして列を残します。

- **修正前（手番0）**: 向きの反転が3回以上あり、20 秒のあいだ一度も静止しない列が録れること。**録れなければ条件が足りない**ので、孤児を増やす／木を減らす／窓の高さを変えて、録れるまで条件を作る。**再現できないまま先へ進まない。**
- **修正後（手番2）**: **同じ条件・同じ手順**で、**2 秒以内に一定値へ落ち着き、残り 18 秒動かない**こと。
- あわせて `console` を拾い、`console.fit_stopped` の警告が出ていないこと（出たら二分が詰め切る前に上限に当たっている＝`FIT_MAX_STEPS` か実装を見直す）。
- **窓のリサイズ追随が死んでいないこと**: `resize_test.py` と同じく `1600x1000 → 900x500 → 1600x1000 → 700x400 → 1200x900 → 1600x1000` と振って、各サイズで拡大率が窓に素直に追随すること（SCOUT-C の変更前の実測は `15% → 15% → 3% → 14% → 2% → 9% → 15%`）。**同じサイズに戻したとき同じ値に戻ること**が、区間の捨て忘れが無いことの実測になる。

### 6-3. 署名が本当に拡大率に依らないことの実測（この設計の土台）

`.tree-pane` の物理寸法が拡大率で動かない、というのが構想1の第4層の前提です。**必ず実測してください**（`flex:1` の basis 0 からの推論であって、ブラウザで確かめてはいません）。

```js
// 自動フォーカスを切ってから、拡大率を変えて3回読む。3回とも同じ値であること。
document.querySelector('.team[data-active] .tree-pane').getBoundingClientRect()
```

同じ手順で `.tree-scroll` も読み、**そちらは動く**ことを確かめると、旧署名がなぜ効かなかったかの実測にもなります（区画が出ているときだけ動きます）。

### 6-4. レビュー（読み合わせ）

- **REVIEW**: 差分全体。特に「捨てる／捨てない」の3箇所（`resetFitTuning()` の呼び出し、不感帯で区間を捨てていないこと、`fitSteps` の増やし方）。ここを取り違えると、**動かなすぎる**という逆向きの不具合になる。
- **REVIEW**: コメントが「なぜ」を書いているか。このファイルの作法で、**取らなかった選択肢とその理由**まで残っていること。
- **検査スクリプト7本**（SCOUT-C C）: `check_wiring.py` / `check_i18n.py` / `check_livefeed.py` / `check_agents.py` / `check_autofinish.py` / `check_autoreg.py` / `check_lang.py` を全部通す。**今回の変更で反応しうるのは `check_wiring.py` だけ**（`need()` の対象は増やしていないので通るはず）。コミット本文に「7本すべて通過」と書く慣習がある。
- **`public/i18n.js` のキー整合を機械的に見る検査は存在しない**（`check_i18n.py` は Python 側だけを見る。SCOUT-C B-3）。今回はキーを増減しない設計なので、**増減していないことを目で確かめる**のがレビュー項目。

### 6-5. 実測した収束列（実績）

§6-1・6-2 は「どう測るか」の計画でした。ここには**実際に測った拡大率の列**を残します。1回目の実装がなぜ効かなかったかは §2-6' に経緯として書いてあるので、ここでは数だけを並べます。

| 段階 | 条件 | 拡大率の列（実測） | 結果 |
| --- | --- | --- | --- |
| 修正前 | 木3体＋孤児16体、900×500、0.3秒間隔で50点（＝15秒） | `27,41,34,37,35,35,35,35,37,35,37,...` | **止まらなかった。** 35 と 37 のあいだの往復が15秒間、減衰せずに続いた。周期はおよそ1.8〜2.1秒で、これは拡大側だけに掛かる `FIT_GROW_MS`(500ms) が作る周期と時間感覚が一致する |
| 1回目実装後 | 署名が `con.w x con.h` + `.tree-pane` 物理寸法（当初の計画どおり） | `27,41,34,34,35,35,37,35,37,35,35,...` | **止まらなかった。** 34↔37 のあいだを往復し続けた |
| 2回目実装後 | 署名を木の素性に差し替え、`FIT_BRACKET_MS` を追加、二分の中点を `Math.floor` に変更 | 木3体＋孤児16体、900×500、0.3秒間隔で50点: `57,34,38,37,36,36,36,36,...` | **36% に完全静止。** 反転からの手数8、約1.8秒。設計目標「最大9手・2秒以内」（§2-7）を満たす |

3段とも**同じ条件**（木3体＋孤児16体、900×500、0.3秒間隔50点）で測ってあります。条件を揃えていない比較は比較になりません。

**振幅が2ポイント（35⇔37）と小さいのは、`.orphan-bay { max-height: 50% }` が区画の伸びを頭打ちにして、フィードバックの増幅を抑えているためと考えられます**（未検証の推測として明記します）。旧実装のコメントが残していた「100% ⇔ 39.68%」のような大振幅の往復を引き起こす条件は、今回の試験範囲では特定できませんでした。振幅が小さくても、**2秒周期で画面全体が脈打ち続ける**ので、利用者から見た不具合としては同じです。

---

## 7. 検証項目のチェックリスト

**構想1（往復）**

- [ ] 修正前の振動を録れた（反転3回以上・20秒静止なし）。列をログとして残した
- [ ] 同じ条件・同じ手順で、修正後は2秒以内に静止し、18秒動かない
- [ ] `console.fit_stopped` の警告が出ていない（正常系で最後の砦に頼っていない）
- [ ] `.tree-pane` の `getBoundingClientRect()` が拡大率 50%/100%/200% で同じ値
- [ ] 窓を `1600x1000 → 900x500 → 1600x1000 → 700x400 → 1200x900 → 1600x1000` と振り、各サイズで木が枠に収まる。**同じサイズに戻したとき同じ拡大率に戻る**
- [ ] 区画を畳んだ状態（`bay-collapsed`）でも自動フォーカスが正しく効く
- [ ] 区画が無いミッション（記録に無い機体が0体）で、いままでどおり1〜2手で落ち着く
- [ ] 機体が毎秒増えている最中に画面が脈打たない（拡大側の 500ms 待ちが生きている）
- [ ] タブを切り替えて戻ると、その木に合った拡大率へ合わせ直す（区間の持ち越しが無い）
- [ ] 「自動」ボタンを切って手で拡大率を変え、もう一度「自動」を押すと枠に合わせ直す
- [ ] 完了して再描画が止まったミッションでも、窓を広げると拡大し直す（`laterAutoFit` の命綱が生きている）
- [ ] `MIN_ZOOM`(1%) / `MAX_ZOOM`(500%) に張り付く条件（極端に大きい木／極端に小さい木）でも止まる

**構想2（区画）**

- [ ] 区画のロボが木のロボのちょうど半分に見える（レターボックスや潰れが無い）
- [ ] 区画のカードから所要時間・トークン・ツールの3つが消えている
- [ ] 区画のカードから任務の行（「（任務未記載）」）も消えている（`.c-mission` は DOM に存在したまま `display:none`）
- [ ] 区画のカードで、稼働中の実測行（`.c-live`）は要件どおり出続けている（§3-7 の「残す」判断）
- [ ] **木のカードには3つとも今までどおり出ている**（経過↔所要の切り替えも含む）
- [ ] 言語を切り替えても木のカードの「経過／所要」が正しく訳される（`entry.timeK` が生きている）
- [ ] 実データで一番長いモデル名（`claude-haiku-4-5-20251001`）の札が切れずに出る
- [ ] 区画のカードで名前が読める（いまより読めるようになっている）。IDは独立行に出る
- [ ] 区画のロボの誕生アニメーション（`pop` と `ring`）が今までどおり出て、途中で大きさが飛ばない
- [ ] 区画を開閉してもロボの呼吸・瞬き・手の動きが 0 から作り直されない
- [ ] 「記録に無い機体」が10体以上いるとき、区画がスクロールで収まる（`overflow:auto` が効いている）
- [ ] 締めた記録（`frozen`）のタブでも区画のカードが崩れない
- [ ] 系統樹側のカード幅・ロボ幅・レーンの間隔が1pxも変わっていない（変更前後のスクリーンショットで比較）
- [ ] 小型化したあとでも、木をさらに小さく・孤児をさらに増やせば振動は起きない（構想1が効いている）

**共通**

- [ ] 検査スクリプト7本すべて EXIT=0
- [ ] `public/index.html` の改行が LF のまま（`git diff --stat` で無関係な行が動いていない）
- [ ] ブラウザのコンソールにエラーが0件
- [ ] 4言語（en/ja/zh/ko）で画面が崩れない（札の文字数が言語で変わるため、区画のカード幅は特に）

---

## 8. やらないこと

- **区画に zoom を掛けるのをやめない**（SCOUT-A 案B）。区画の設計意図を壊すため。
- **区画を flex 列から外さない**（SCOUT-A 案A）。レイアウトの作り直しになるため。
- **縮小側に待ち時間を入れない**（SCOUT-A 案D）。切れている時間を伸ばす退化のため。
- **`fitBox()` に区画の高さを直接引かせない／解析解を当てない。** 自動フォーカスが区画の DOM を知ることになり、上限に張り付く領域で式が変わり、将来3つ目の結合が入れば同じ形で破れるため。
- **系統樹側の見た目は1pxも変えない。** ロボの色（0.9.2 で入れたばかり）、カード幅、レーン間隔、線の描き方。
- **区画の文字サイズを下げない。** 縮めるのは余白・器・ロボまで。
- **`.c-metrics` を DOM から消さない。** CSS で隠すだけ。
- **`.orphan-bay { max-height: 50% }` と `.bay-body { max-height: 46vh }` を触らない。**
- **文言（`public/i18n.js`）を1つも増やさない・減らさない。**
- **`FIT_MARGIN` / `FIT_DEADBAND` / `FIT_GROW_GAIN` / `FIT_GROW_MS` / `FIT_MIN_GAP_MS` の値を変えない。** 変えるのは `FIT_MAX_BURST` → `FIT_MAX_STEPS` の1つだけで、それは**数える相手が変わったから**。
- **ロボのグロー（`filter: drop-shadow`）を区画用に調整しない。** 半分にすると滲みが相対的に強く見えるが、これは美観の話で、要件に無い。気になるかどうかは実物を見てから別途決める。
- **進捗表示・並べ替え・折り畳みなど、区画への機能追加をしない。**
- **バージョンを上げるかどうかは、この計画では決めない。** 上げる場合の手順は SCOUT-C E に整理されている（`extension/package.json` の version → `build_vsix.py build` が `VERSION` を追随 → `extension/CHANGELOG.md` に節を追記）。

---

## 9. 未確認のこと（推測で埋めていない部分）

- ~~`.tree-pane` の物理寸法が拡大率に依らないこと。CSS（`flex: 1` ＝ basis 0、`min-width/min-height: 0`）からの推論で、ブラウザで測ってはいない。構想1の第4層の土台なので、§6-3 で必ず実測すること。~~ **【確認済み】** §6-3 の手順で実測し、拡大率を変えても `.tree-pane` は不変でした（§2-6 の署名コメント参照）。ただし §2-6' のとおり、**この前提が壊れていたのは `.tree-pane` ではなく `con.h`（署名のもう一方の要素）のほうでした**——`.tree-pane` 側の見立ては最初から正しかったことになります。
- **`max-height: 46vh` が、zoom を持つ `.bay-body` の上でどう解決されるかの正確な物理 px**（SCOUT-A 3）。折れ線のどこで区画が上限に張り付くかは、実測でしか確定できない。振動の再現条件を作るときに影響する。
- **数値はすべて計算による見積り。** 文字幅（Consolas の送り 0.55em、全角＝フォントサイズ）から出した 137.5px / 121px / 175.5px と、高さの概算（1体 296px → 190px、`b` がおよそ半分）は、**ブラウザで測った値ではない**。`--card-w: 176px` は「実データの最長モデル名が切れない下限」として出した値なので、**実装後に実物で確かめる**こと（§7）。フォントが Consolas でない環境では送りが変わるため、切れる可能性は残る。**`--card-w: 176px` そのものは §3-6 で実測済み**（区画176px・木側は1pxも変わらず・最長モデル名も切れない）。ただし §3-2 の名前幅の見積り（154px）は実測（133px）とずれています——差の原因までは特定していません（§3-6 参照）。1体あたりの高さの概算（296px→190px）はこの計画書の入力に実測が含まれていないため、未確認のままです。
- **利用者が実際に踏んだ振動が、この経路だったかどうか。** ログや報告の実物は見ていない（SCOUT-A 3）。区画が非表示のときは `display:none` で flex 計算に関与しないので、この経路は**区画に出す機体が実在するときにしか起きない**。もし区画が出ていない画面でも振動したという報告があれば、別の経路が残っていることになる（ただし二分の詰めは原因を問わないので、その場合も止まるはず）。
- **`I18N.apply()` が `display:none` の要素を通常どおり処理すること**（SCOUT-B 8）。DOM API の一般的な仕様からの判断で、`public/i18n.js` の実装は読んでいない。
- ~~`Math.round((fitLo + fitHi) / 2)` の丸めが、区間が 1 のときに必ず上端へ行くこと（JS の `Math.round` は .5 を上へ丸める）に依存している。区間が詰み切る最後の1手の挙動なので、実装時にその1手を手で追うこと。~~ **【確認済み・実装時に踏んだ】** ここで懸念していたとおりのことが実際に起きました。`Math.round` は区間の幅が1のとき常に `fitHi`（収まらない側）へ丸まり、1600×1000・49体の木で `fitLo:16 fitHi:17` のまま固定されて49体中4体が46pxはみ出し続けました。`Math.floor` に変えて解決しています。経緯は §2-6' を参照。
