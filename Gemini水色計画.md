# Gemini のボディ色を水色にする

## Context

0.9.2 で機体のボディ色をモデル別に塗り分けました（赤 = Fable、オレンジ = Opus / Sol、薄緑 = Haiku / Luna、それ以外は白）。しかし**白は2つの意味を兼ねています**——「Sonnet / Terra」という判定できたモデルと、「判定できなかったもの」です。

`modelTone()` を見ると、Gemini は他の未知モデルと同じ扱いではありません。

```js
if (!low || low.includes('gemini')) return '';
```

空文字（モデル名が記録されていない）と `gemini` を含む文字列は、同じ `if` で同じ白に落とされています。しかし前者は「判定できない」のに対し、後者は`low.includes('gemini')` という**明示的な判定が成立したうえで**白にしています。つまり Gemini は「判定できたのに白」という、白の2つの意味のうち紛らわしい方に分類されている唯一のケースです。

ここを水色にすると、白の意味が「Sonnet / Terra と、判定できないもの」に寄り、「判定できたのに白」という状態が1つ減ります。

---

## 配色プレビュー

**動くプレビューを同じフォルダに置いてあります。ブラウザで開いて確認してください:**

[Gemini水色計画.html](Gemini水色計画.html)

このプレビューでは次のことが確かめられます。

- **5色 × 4状態**（待機／稼働／報告待ち／完了）を動いた状態で見比べる（白・赤・オレンジ・薄緑に水色を加えた5色）
- **ボタン1つで「現状＝ Gemini は白」と切り替え**て、変更の前後を比較する
- **濃さを4段階**（淡い／中／濃い／とても濃い）で切り替えて比較する（採用は③濃い）
- **大きさスライダー**で実寸 94px と拡大を行き来し、その濃さで見分けが付くか判断する
- **モデル名を入力**して、`gemini-3.1-pro-high` のような表記が水色になるかその場で試す
- そして、**稼働中／報告待ちの胸ライトが水色ボディの上で沈まないかを実寸で見る**（今回の計画でいちばん確かめるべき点です）

今回は静止画の SVG をこの文書には埋め込みません。プレビューの HTML で実物のアニメーションと実寸を見られるので、そちらを見てください。代わりに、下の色の値の表を厚くしてあります。

---

## 追加するグラデーションの値

採用するのは **③濃い**、色相 **H=198**、トーン名は **`aqua`** です（数値は決定済みで変更しません）。

既存の白・赤・オレンジ・薄緑と同じ「卵 stop 0・0.5・1 / 腕 stop 0・1」の3ストップ構造のまま、水色を1列足します。

| トーン | 卵 stop 0 / 0.5 / 1 | 腕 stop 0 / 1 |
| --- | --- | --- |
| 白（既存・変更なし） | `#ffffff` / `#f4f8fc` / `#d4dfec` | `#ffffff` / `#dbe5f1` |
| 赤（Fable） | `#fff2f4` / `#ffbcc6` / `#d9697d` | `#fff2f4` / `#f4a0ae` |
| オレンジ（Opus / Sol） | `#fff6ec` / `#ffd39b` / `#dd9440` | `#fff6ec` / `#f2b76c` |
| 薄緑（Haiku / Luna） | `#f4fcec` / `#c6e79b` / `#7cae4c` | `#f4fcec` / `#a9d178` |
| **水色（Gemini・新規）** | `#ebf5f9` / `#a8def5` / `#2d9dcd` | `#ebf5f9` / `#65bfe6` |

参考までに、プレビューで切り替えられる `aqua` の4段階すべての値も載せておきます（採用するのは③のみ）。

```js
aqua: {
  soft:   { egg: ['#ffffff', '#edf8fc', '#bedce9'], limb: ['#ffffff', '#d3ebf5'] },
  mid:    { egg: ['#f7fbfd', '#d2edf9', '#8fc4dc'], limb: ['#f7fbfd', '#addaee'] },
  strong: { egg: ['#ebf5f9', '#a8def5', '#2d9dcd'], limb: ['#ebf5f9', '#65bfe6'] },
  vivid:  { egg: ['#d7ebf4', '#77d1f8', '#1f86b2'], limb: ['#d7ebf4', '#37b4e9'] },
}
```

---

## この計画の中心 — 胸ライトと喧嘩しないか

**ここがいちばん厚く書くべき節です。**

1. **水色は「稼働中」を表すシアン（`--cyan: #3ee0ff`、H189.6 / S100 / L62.2）と近い色相です。** 稼働中は卵の外周にグロー `filter: drop-shadow(0 0 7px rgba(62,224,255,.28));` が乗り（`public/index.html:547`）、さらに**胸ライト `.rb-chest-running`（`fill: #3ee0ff;`、`public/index.html:513`）が卵の上に直接描かれます**（`<circle class="rb-chest-running ..." cx="60" cy="126" r="4.4"/>`、`public/index.html:2769`）。外周のグローと違い、これはシルエットの内側に乗る塗りなので、ボディ色そのものと直接ぶつかります。

2. **しかし同じ構図が既にオレンジ機体で出荷されています。** 報告待ちの状態では `public/index.html:560` の

   ```css
   .agent.waiting .rb-chest-running { fill: var(--amber); filter: drop-shadow(0 0 5px rgba(255,190,85,.9)); }
   ```

   により、**琥珀の胸ライト `#ffbe55`（H37.1）がオレンジのボディ（`.agent.mdl-orange .rb-egg`、`public/index.html:483`）の上に乗ります**。オレンジの egg[2] は `#dd9440`（H32.1）で、色相差はわずか5°前後。0.9.2 で問題なく出荷されています。

3. **つまりこのリポジトリの機体色は、もともと色相ではなく彩度と明度で状態表示を浮かせています。** 既定の白（egg[2] `#d4dfec`、H212.5 / S38.7 / L87.8）もシアンの胸ライト（H189.6 / S100 / L62.2）とは色相差 22.9° しかありません。彩度と明度の差だけで共存しているのが現状の設計です。水色もこの前例に倣えば成立するはずです。

4. **胸ライトが実際に乗る色を、卵のパスとグラデーションの幾何から求めました。** 卵のパスは

   ```
   M14 106 A46 78 0 0 1 106 106 A46 48 0 0 1 14 106 Z
   ```

   （`public/index.html:2763`）で、バウンディングボックスは x[14,106] / y[28,154] です。グラデーション `rbEggGrad` は `x1="0.12" y1="0" x2="0.85" y2="1"`（`public/index.html:1072`、objectBoundingBox）なので、実座標では始点 (25.04, 28.0) → 終点 (92.2, 154.0) のベクトルになります。胸ライトの中心 `cx="60" cy="126"`（`public/index.html:2767-2769`）をこのベクトルへ射影すると **t = 0.7209** になります（半径4.4ぶんの上下、cy=121.6〜130.4 でも t は 0.690〜0.752 に収まり、値はほぼ動きません）。

   つまり胸ライトの真下と比較すべきなのは3ストップの最後（`egg[2]`、offset=1）ではなく、**offset 0.5 と offset 1 の間を 44.2% 進んだ位置の補間色**です（(0.7209−0.5)÷(1−0.5) = 0.4418）。

5. **その実重なり位置での数値**（採用する③濃い）:

   | | 実重なり色 | 比較先の胸ライトとの dH / dS / dL |
   | --- | --- | --- |
   | オレンジ（出荷済み）× 琥珀の胸ライト | `#f0b773` | 4.4° / 19.4pt / 2.9pt |
   | 白（既定）× シアンの胸ライト | `#e6edf5` | 22.4° / 57.1pt / 31.0pt |
   | **水色 ③ × シアンの胸ライト** | `#72c1e3` | **8.4° / 33.1pt / 4.7pt** |
   | **水色 ③ × 報告待ちの琥珀の胸ライト** | `#72c1e3` | **161.0° / 33.1pt / 0.2pt** |

   **合格線は出荷済みのオレンジ**（実際に画面で成立している組み合わせ）です。水色は dH・dS・dL の3項目とも、オレンジの数値を上回っています（＝オレンジより離れている）。他の濃さの段階も同様に成立します——①（淡い）`#d8ecf4` で 7.5° / 44.0pt / 28.0pt、②（中）`#b4dbec` で 8.6° / 40.4pt / 19.4pt、④（とても濃い）`#50b0d9` で 8.3° / 35.7pt / 3.9pt。

   **ただし「水色 × 報告待ちの琥珀」だけは、成立の仕方が他の行と違います。** この組み合わせは明度差がほとんど無く（0.2pt）、水色のボディと琥珀の胸ライトはたまたまほぼ同じ明るさになります——つまりこの組み合わせだけは、明度では浮きません。代わりに色相差が 161.0°（ほぼ補色）あり、彩度差も 33.1pt あります。明度で浮かない分を、色相と彩度が引き受けている形です。合格線の出荷済みオレンジ×琥珀（dH 4.4° / dS 19.4pt / dL 2.9pt）と比べると、明度だけがわずかに下回ります（0.2pt < 2.9pt）が、色相と彩度は大きく上回ります。出荷済みの組み合わせは色相差 4.4° しかないところを明度 2.9pt で辛うじて浮かせていたのに対し、水色×琥珀はその逆で、明度をほぼ使わず色相で浮かせている形です。**どちらも「3つの軸のうち少なくとも1つで十分な差がある」という同じ条件を満たしているので、成立と判断します。**

6. 念のため他の状態光とも比較しておきます。待機グレイ `#b9c6d6`（`public/index.html:512`）との比較は、③の実重なり `#72c1e3` に対し dH 15.1° / dS 40.7pt / dL 11.4pt。完了ミント `#5fe6a4`（`public/index.html:514`）との比較は、egg[2] `#2d9dcd` に対し dH 47.3° / dS 9.0pt / dL 14.7pt。どちらも上のオレンジ×琥珀の合格線を上回っており、水色ボディの上でも4状態すべての胸ライトが「別の色として浮く」水準を保っています。

これらの数値は独立に検算済みです（`t=0.7209` での補間、各 dH/dS/dL とも上表の値と一致することを確認しています）。それでも**数値の上での合格と、実寸 94px で人の目に「点いて見える」かは別問題**なので、プレビューで実寸表示を必ず確認してください。

---

## なぜ H=198 なのか（却下した候補）

**この節も厚く書きます。** 検討して捨てた案は次のとおりです。

| 候補 | 却下理由 |
| --- | --- |
| H=170（ターコイズ・青緑） | 胸ライトからいちばん逃げられる色だが、**これは水色ではない**。青緑を水色と言い張るより、水色の帯の中で成立させるべき。実際、彩度と明度で逃げれば水色のままでも成立する |
| H=195 | 教科書的な水色そのものだが、稼働シアン（H189.6）との色相差が 5.0° しかなく余裕がない |
| **H=198（採用）** | JIS 的な水色の参照値 `#BFECFF`（H197.8）とほぼ一致。胸ライト側 7.9°・白側 14.5° と、どちらの下限からも余裕が残る |
| H=200 | バランスは良いが、水色の参照値からわずかに遠い |
| H=205 | 白（H212.5）との色相差が 7.4° まで縮む。既定の白は青みがかった灰白色なので、青に寄せすぎると白機体と混ざる |
| H=230 前後（青寄り） | 白との色相差 18°。既存のどの色より白に近くなり、いちばん避けたい方向 |
| 中間ストップを白へ寄せて彩度差を稼ぐ案 | 一度この形で作ったが、卵の上半分がほぼ白のまま右下だけ急に濃くなり、**兄弟の4色と別物の塗り**になった。中間ストップは兄弟の帯（赤 S100/L87、オレンジ S100/L80、薄緑 S61/L76）に収める |

**白との弁別は明度で作ります。** 水色③の egg[2] `#2d9dcd`（L49.0）と白の egg[2] `#d4dfec`（L87.8）の明度差は **38.8pt** で、既存3色の対白明度差（赤 24.7 / オレンジ 32.0 / 薄緑 38.8）のうち**最大の薄緑と同水準**です。色相が近いぶんを明度で補っている形になります。

---

## トーン名を `aqua` にした理由

`cyan` は既存の CSS 変数 `--cyan`（`public/index.html:26`、稼働中を表す H189.6）と字面が重なります。ボディ色は意図的に H198 へ寄せてあり数値としても別物なので、同じ単語で違う色相の2つの概念（機体の身元／稼働状態の表示）を指すことになり、後から読む人が `--cyan` と `.mdl-cyan` の色が食い違う理由を追わされます。`teal` は CSS 標準の teal が暗く沈んだ色（H180 / S100 / L25）で、採用する H198 とは印象が違いすぎます。そこで `aqua` を採用します。

---

## なぜこの実装なのか（制約）

0.9.2 の計画書（`機体色変更計画.md`）と同じ制約がそのまま効きます。要点だけ再掲すると、`#rb-defs` は**ページに1つだけ**置いて全機体が共有しており（`public/index.html:1067` のコメント）、グラデーションの計算は参照元ではなく `#rb-defs` 内の自分の位置で行われるため、**`stop-color: var(--rb-body)` のような変数切り替えは使えません**。単色 fill に落とすと立体感が消えるので却下、残る手は「色ごとにグラデーションを増やし、CSS で参照先を切り替える」だけです。今回はこの1色（水色）を、既存の赤・オレンジ・薄緑と同じやり方で1組追加するだけなので、新しい制約は増えません。

---

## 変更箇所

編集対象は `public/index.html`、`build_vsix.py`、README 4言語です。行番号はすべて実際に読んで確認したものです。

### 0. `build_vsix.py` の `PAYLOAD_SKIP` に `Gemini水色計画.md` を足す

`build_vsix.py:133` に `"機体色変更計画.md",` がすでにあります。その直後に1行足します。

```python
    "機体色変更計画.md",
    "Gemini水色計画.md",
```

これを忘れると、`SCAN_SUFFIXES = (".py", ".md", ".cmd")`（`build_vsix.py:142`）に `.md` が含まれるため、リポジトリ直下に置いた新しい `.md` は必ず同梱漏れ検査に引っかかり、「同梱するかどうかが決まっていないファイルがあります」でビルドが止まります。`.html`（`Gemini水色計画.html`）は `SCAN_SUFFIXES` に入っていないので走査対象外であり、こちらは何もしなくて構いません。0.9.2 のときも `機体色変更計画.md` で同じ対応をしています。

### 1. `#rb-defs` に `rbEggGradAqua` / `rbLimbGradAqua` を追加

`rbLimbGradGreen` の定義は `public/index.html:1105-1108` にあり、108行目の `</linearGradient>` の直後（109行目の `rbVisorGrad` の手前）に足します。座標は既存の赤・オレンジ・薄緑と同一、`stop-color` だけ上表の水色の値にします。

```xml
  <linearGradient id="rbEggGradAqua" x1="0.12" y1="0" x2="0.85" y2="1">
    <stop offset="0"    stop-color="#ebf5f9"/>
    <stop offset="0.5"  stop-color="#a8def5"/>
    <stop offset="1"    stop-color="#2d9dcd"/>
  </linearGradient>
  <linearGradient id="rbLimbGradAqua" x1="0" y1="0" x2="0" y2="1">
    <stop offset="0" stop-color="#ebf5f9"/>
    <stop offset="1" stop-color="#65bfe6"/>
  </linearGradient>
```

### 2. CSS に2行追加

`.agent.mdl-green .rb-arm { fill: url(#rbLimbGradGreen); }` は `public/index.html:486`。その直後（487行目の `.rb-socket` の手前）に足します。

```css
.agent.mdl-aqua   .rb-egg { fill: url(#rbEggGradAqua); }
.agent.mdl-aqua   .rb-arm { fill: url(#rbLimbGradAqua); }
```

### 3. `modelTone()` の Gemini の分岐の戻り値を変える

現在（`public/index.html:2722-2744`）:

```js
/** モデル名からボディ色のトーンを決める。'' は既定の白。
 *
 *  記録側は "claude-opus-5"、実機の meta.json は "opus" と、同じモデルが別表記で入る
 *  （livefeed.model_key と同じ事情）。そのため部分一致で拾う。
 *  **知らない表記は必ず '' に落とす。** 推測して別の色で塗ると、画面が黙って嘘をつく。
 *  Gemini を先に弾いているのは、全モデル白と決まっているため。将来 "sol" を含む
 *  Gemini のモデル名が出ても、下の表に吸われないようにする。 */
const MODEL_TONES = [
  [/fable/,                     'red'],
  [/opus/,                      'orange'],
  [/haiku/,                     'green'],
  [/sonnet/,                    ''],
  [/(^|[^a-z])sol([^a-z]|$)/,   'orange'],
  [/(^|[^a-z])luna([^a-z]|$)/,  'green'],
  [/(^|[^a-z])terra([^a-z]|$)/, ''],
];

function modelTone(model) {
  const low = String(model || '').toLowerCase();
  if (!low || low.includes('gemini')) return '';
  for (const [re, tone] of MODEL_TONES) if (re.test(low)) return tone;
  return '';
}
```

変更後:

```js
/** モデル名からボディ色のトーンを決める。'' は既定の白。
 *
 *  記録側は "claude-opus-5"、実機の meta.json は "opus" と、同じモデルが別表記で入る
 *  （livefeed.model_key と同じ事情）。そのため部分一致で拾う。
 *  **知らない表記は必ず '' に落とす。** 推測して別の色で塗ると、画面が黙って嘘をつく。
 *  空文字を先に弾くのは「モデル名が無い＝判定できない」ので白のまま、という別の理由。
 *  Gemini をその次に見ているのは、全モデル水色と決まっているため。将来 "sol" を含む
 *  Gemini のモデル名が出ても、下の表（Opus/Sol 用の正規表現）に吸われないようにする。 */
const MODEL_TONES = [
  [/fable/,                     'red'],
  [/opus/,                      'orange'],
  [/haiku/,                     'green'],
  [/sonnet/,                    ''],
  [/(^|[^a-z])sol([^a-z]|$)/,   'orange'],
  [/(^|[^a-z])luna([^a-z]|$)/,  'green'],
  [/(^|[^a-z])terra([^a-z]|$)/, ''],
];

function modelTone(model) {
  const low = String(model || '').toLowerCase();
  if (!low) return '';
  if (low.includes('gemini')) return 'aqua';
  for (const [re, tone] of MODEL_TONES) if (re.test(low)) return tone;
  return '';
}
```

`MODEL_TONES` 配列そのものは変えません（Gemini は `MODEL_TONES` を通る前の専用分岐のままです）。変わるのはコメントと、専用分岐の戻り値が `''` から `'aqua'` になる点だけです。

### 4. `updateAgent` のクラス除去リストに `'mdl-aqua'` を足す

現在（`public/index.html:2969`）:

```js
entry.root.classList.remove('mdl-red', 'mdl-orange', 'mdl-green');
```

変更後:

```js
entry.root.classList.remove('mdl-red', 'mdl-orange', 'mdl-green', 'mdl-aqua');
```

**ここを直し忘れると、いちど水色になったカードが別のモデルに変わっても水色のまま残ります。** hook 登録の機体は起動時に `subagent_type` しか入らず白判定で表示され、完了時に `resolvedModel` でモデルが書き換わる仕組みなので（0.9.2 計画書の「4. カードにクラスを付ける」節で説明済みの経路）、「Gemini だと思って水色に塗ったら、実は別モデルだった」という書き換えは実際に起こり得ます。`entry.tone` 自体は `buildAgentDom`（`public/index.html:2933` の `tone: null,`）で初期化済みで、この節の対象は `updateAgent` 側の除去リストのみです——ここに1色追加するのを忘れると、除去リストに載っていない `mdl-aqua` だけが古いクラスとして卵の上に残り続けます。

### 5. README 4言語の修正

0.9.2 で追加した「機体の色はモデルを表す」の節に「白 = Sonnet / Terra / Gemini とそれ以外」と書かれているので、Gemini を水色側へ移します。

- `README.ja.md:507`
  ```
  薄緑 = Haiku / Luna、白 = Sonnet / Terra / Gemini とそれ以外、という対応です。
  ```
  → `白 = Sonnet / Terra とそれ以外、水色 = Gemini、という対応です。` のように Gemini を独立させる。
- `README.md:480`
  ```
  Opus / Sol, pale green = Haiku / Luna, and white = Sonnet / Terra / Gemini and everything else.
  ```
  → `... pale green = Haiku / Luna, aqua = Gemini, and white = Sonnet / Terra and everything else.` のように分離する。
- `README.zh.md:528`
  ```
  浅绿 = Haiku / Luna，白 = Sonnet / Terra / Gemini 以及其他一切。
  ```
  → `浅绿 = Haiku / Luna，浅蓝 = Gemini，白 = Sonnet / Terra 以及其他一切。`
- `README.ko.md:510`
  ```
  연두 = Haiku / Luna, 흰색 = Sonnet / Terra / Gemini 와 그 밖의 나머지입니다.
  ```
  → `연두 = Haiku / Luna, 하늘색 = Gemini, 흰색 = Sonnet / Terra 와 그 밖의 나머지입니다.`

いずれも実際の文言は担当者の裁量に委ねますが、**「Gemini」という語をそのまま白の並びに残さないこと**が必須です。4ファイルとも同じ段落内の1文だけの修正で完結します。

### 6. 版番号は今回上げない

`extension/CHANGELOG.md` と `VERSION` / `extension/package.json` は、次の版として実際に出荷するときに上げます。この計画書では版番号を決めません（0.9.2 の計画書も同じ立場です）。

---

## 判定結果の検算

`機体色変更計画.md` の「判定結果の検算（実データ 22 種すべて）」を土台に、**Gemini 系だけが白から水色へ移る**ことを示します。記録に実在する Gemini の表記は次の3種です。

| 値（出現数） | 0.9.2 での判定 | 今回の判定 | |
| --- | --- | --- | --- |
| `gemini-3.1-pro-high`(4) | 白 | 水色 | ✅ `low.includes('gemini')` が真 |
| `gemini-3.6-flash-medium`(2) | 白 | 水色 | ✅ 同上 |
| `Gemini 3.1 Pro`(1) | 白 | 水色 | ✅ `toLowerCase()` で `"gemini 3.1 pro"` になってから判定するため、大文字始まりの表記も拾える |

他の値（`claude-opus*` → オレンジ、`claude-sonnet*` → 白、`claude-haiku*` → 薄緑、`gpt-5.6-sol` → オレンジ、`gpt-5.6-luna` → 薄緑、`gpt-5.6-terra` → 白、hook の `subagent_type` 各種 → 白、`不明`・空文字 → 白）はすべて 0.9.2 から**変わりません**。`modelTone()` の変更点は Gemini の専用分岐の戻り値だけで、`MODEL_TONES` 配列自体（fable / opus / haiku / sonnet / sol / luna / terra の判定順序と正規表現）には一切手を入れていないためです。

---

## 触らないもの

0.9.2 と同じです。

- **`extension/media/robot-standby.svg` / `activity-bar.svg`** — VSCode のアイコン用の静止画で、特定のモデルを表さないため白のまま
- **`public/manual.html`** — 状態（待機／稼働／報告待ち／完了）の見本であって、モデルの見本ではない。白のままで意味は正しい
- **worktree**（`-bundle` / `-mermaid` / `-livefeed`）— 別ブランチの実体。main をマージしたときに入る
- **`~/.claude/agent-dashboard`（配置版）** — 拡張機能が配置する側。ここを直接編集すると次の配置で消える

---

## 検証

0.9.2 の計画書の「検証」節を土台に、今回固有のものを足します。

1. **リポジトリのサーバーを直接起動して見る**（配置版を汚さずに確認できる）
   ```
   python "c:\Users\yuta_\OneDrive\Python_files\Subagent Dashboard\server.py"
   ```
   `GET /api/env` の `toolRoot` がリポジトリ側を指していることを確認してから画面を開く。
2. **稼働中と報告待ちの胸ライトが、水色ボディの上で「点いている」と読めるかを実寸 94px で見る。** 今回いちばん確かめるべきことです。`--project` を分けたデモ・確認用ミッションで `add --model gemini-3.1-pro-high` の機体を作り、稼働中→報告待ち→完了と状態を切り替えながら胸の光点が沈んでいないか目視する。
3. **報告待ちの水色機体で、琥珀の胸ライトが「点いている」と読めるかを個別に見る。** 「胸ライトと喧嘩しないか」の節に足した机上の数値（`#72c1e3` × `#ffbe55` で dH 161.0° / dS 33.1pt / dL 0.2pt、明度差ほぼ無しを色相差で補う組み合わせ）は計算上の話でしかないため、実際にプレビューと実寸表示で、報告待ち状態だけを単独で止めて琥珀の光点が水色の卵に沈んでいないかを目で確認する。
4. **水色の機体と白の機体を並べて、実寸で見分けが付くか確認する。** `gemini-...` の機体と `claude-sonnet-5` の機体を同じ画面に並べ、遠目・実寸の両方で判別できることを見る。
5. **`gemini-3.1-pro-high` / `Gemini 3.1 Pro` / `gemini-3.6-flash-medium` の3表記すべてが水色になるか**を、`add --model` にそれぞれの値を渡して確認する。
6. **モデルが後から書き換わる機体でクラスが正しく入れ替わるかを確認する。** 変更箇所4で挙げた落とし穴の実地確認です。hook 登録相当の機体（モデル未指定→白で開始）を作り、完了時に `--model gemini-...` で `resolvedModel` を上書きさせて、カードが白から水色に変わることと、逆に水色→他モデルへ書き換えたときに `mdl-aqua` が確実に外れることの両方を見る。
7. 待機／稼働／報告待ち／完了の4状態を切り替え、状態のグローと表情が今までどおり出ることを確認する（色の変更が状態表示を潰していないこと）。
8. 過去のミッションのタブを開き、`不明` や空文字の機体が引き続き**白**で出ることを確認する。

### 書き戻しの注意

`public/index.html` は **LF** です。Python の `Path.write_text()` で書き戻すと全行が CRLF になり、触っていない行まで差分になります。Edit ツールで直接編集するか、`write_bytes()` を使ってください（0.9.2 と同じ注意です）。
